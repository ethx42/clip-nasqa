---
phase: 06-testing-and-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - package.json
  - packages/core/src/__tests__/schemas.test.ts
  - packages/functions/src/__tests__/qa.test.ts
  - packages/functions/src/__tests__/moderation.test.ts
  - packages/functions/src/__tests__/rate-limit.test.ts
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03

must_haves:
  truths:
    - "Running `npm test` from repo root executes tests in core, functions, and frontend projects"
    - "Core Zod schema tests validate happy paths and edge cases (empty, too long, invalid types)"
    - "Lambda resolver tests run with mocked DynamoDB — no real AWS credentials needed"
    - "All tests pass with zero failures"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Root Vitest config with three inline project definitions"
      contains: "test.projects"
    - path: "packages/core/src/__tests__/schemas.test.ts"
      provides: "Zod schema validation tests"
      contains: "createSessionInputSchema"
    - path: "packages/functions/src/__tests__/qa.test.ts"
      provides: "Q&A resolver tests"
      contains: "addQuestion"
    - path: "packages/functions/src/__tests__/moderation.test.ts"
      provides: "Moderation resolver tests"
      contains: "banQuestion"
    - path: "packages/functions/src/__tests__/rate-limit.test.ts"
      provides: "Rate limit utility tests"
      contains: "checkRateLimit"
  key_links:
    - from: "vitest.config.ts"
      to: "packages/core/src/**/*.test.ts"
      via: "test.projects[0].include"
      pattern: "packages/core/src/\\*\\*/\\*.test.ts"
    - from: "vitest.config.ts"
      to: "packages/functions/src/**/*.test.ts"
      via: "test.projects[1].include"
      pattern: "packages/functions/src/\\*\\*/\\*.test.ts"
    - from: "packages/functions/src/__tests__/qa.test.ts"
      to: "DynamoDBDocumentClient"
      via: "aws-sdk-client-mock mockClient"
      pattern: "mockClient.*DynamoDBDocumentClient"
---

<objective>
Install the Vitest test framework, configure monorepo projects, and write unit tests for core Zod schemas and Lambda resolvers.

Purpose: Establish the test foundation so all subsequent test work builds on a working runner with proven patterns for mocking AWS SDK.
Output: Root vitest.config.ts, test scripts in package.json, passing schema tests and resolver tests.
</objective>

<execution_context>
@/Users/santiago.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/santiago.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-testing-and-ci/06-RESEARCH.md
@packages/core/src/schemas.ts
@packages/core/src/types.ts
@packages/functions/src/resolvers/index.ts
@packages/functions/src/resolvers/qa.ts
@packages/functions/src/resolvers/moderation.ts
@packages/functions/src/resolvers/rate-limit.ts
@packages/functions/src/resolvers/clipboard.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest toolchain and configure monorepo projects</name>
  <files>
    vitest.config.ts
    package.json
  </files>
  <action>
Install test dependencies at the root workspace level:

```bash
npm install -D vitest @vitejs/plugin-react jsdom @testing-library/react @testing-library/dom @testing-library/user-event @testing-library/jest-dom vite-tsconfig-paths aws-sdk-client-mock aws-sdk-client-mock-vitest
```

Create `vitest.config.ts` at repo root with three inline project definitions:

1. **core** — `environment: "node"`, include `packages/core/src/**/*.test.ts`
2. **functions** — `environment: "node"`, include `packages/functions/src/**/*.test.ts`
3. **frontend** — `environment: "jsdom"`, include `packages/frontend/src/**/*.test.{ts,tsx}`, plugins: `[tsconfigPaths(), react()]`, setupFiles: `["packages/frontend/src/__tests__/setup.ts"]`, `server.deps.inline: ["next-intl"]`

Use the exact `test.projects` API (NOT deprecated `vitest.workspace.ts`).

Add scripts to root `package.json`:

- `"test": "vitest run"` (NOT just `"vitest"` — must use `run` to avoid watch mode hanging in CI)

Do NOT add per-package test scripts — the root config handles everything.
</action>
<verify>
<automated>cd /Users/santiago.torres/codebases/personal/nasqa-clip && npx vitest run --reporter=verbose 2>&1 | head -20</automated>
<manual>Vitest discovers three projects (core, functions, frontend) even if no test files exist yet</manual>
</verify>
<done>vitest.config.ts exists with three project definitions; `npm test` runs without configuration errors</done>
</task>

<task type="auto">
  <name>Task 2: Write core schema tests and Lambda resolver tests</name>
  <files>
    packages/core/src/__tests__/schemas.test.ts
    packages/functions/src/__tests__/qa.test.ts
    packages/functions/src/__tests__/moderation.test.ts
    packages/functions/src/__tests__/rate-limit.test.ts
  </files>
  <action>
**Core schema tests** (`packages/core/src/__tests__/schemas.test.ts`):

Read `packages/core/src/schemas.ts` to discover all exported schemas. Write tests for each schema covering:

- Valid input parses successfully
- Empty/missing required fields are rejected
- Fields exceeding max length are rejected
- Extra unknown fields are stripped (Zod default behavior)
- Type coercion edge cases if any

Expected schemas to test: `createSessionInputSchema`, `createSnippetInputSchema`, `createQuestionInputSchema`, plus any others found in the file.

**Lambda resolver tests** — Read each resolver file first to understand exact function signatures, argument shapes, and DynamoDB operations. Then write tests:

`packages/functions/src/__tests__/qa.test.ts`:

- Call `mockClient(DynamoDBDocumentClient)` BEFORE importing resolver functions (import order critical — see RESEARCH.md Pitfall 1)
- Set `process.env.TABLE_NAME = 'test-table'` before imports (Pitfall 6)
- Test `addQuestion`: returns QUESTION_ADDED event with correct shape, rejects text exceeding limit
- Test `upvoteQuestion`: returns QUESTION_UPVOTED event, handles duplicate vote
- Test `addReply`: returns REPLY_ADDED event with correct shape

`packages/functions/src/__tests__/moderation.test.ts`:

- Test `banQuestion`: returns correct event type when host secret matches
- Test ban enforcement: banned fingerprint is rejected

`packages/functions/src/__tests__/rate-limit.test.ts`:

- Test `checkRateLimit`: passes when under limit, throws when over limit
- Test `checkNotBanned`: passes for clean fingerprint, throws for banned fingerprint

For all resolver tests:

- Use `beforeEach(() => ddbMock.reset())` to clear mock state between tests
- Mock `GetCommand` responses for rate limit/ban checks (resolve to `{ Item: undefined }` for "passes" cases)
- Mock `PutCommand` and `UpdateCommand` to resolve successfully
- Import `aws-sdk-client-mock-vitest` for `.toHaveReceivedCommand()` matchers
- Use `ddbMock.on(GetCommand, { Key: ... }).resolves(...)` for specific command matching where needed
  </action>
  <verify>
  <automated>cd /Users/santiago.torres/codebases/personal/nasqa-clip && npx vitest run --project core --project functions --reporter=verbose 2>&1 | tail -30</automated>
  <manual>All core and functions tests pass; no AWS credential errors (mocks working)</manual>
  </verify>
  <done>Schema tests cover all exported Zod schemas with happy/unhappy paths; resolver tests cover addQuestion, upvoteQuestion, addReply, banQuestion, checkRateLimit, checkNotBanned with mocked DynamoDB — all passing</done>
  </task>

</tasks>

<verification>
1. `npm test` exits 0 with all core and functions tests passing
2. No test requires real AWS credentials
3. `vitest.config.ts` uses `test.projects` (not deprecated workspace API)
4. Test script uses `vitest run` (not watch mode)
</verification>

<success_criteria>

- `npm test` runs all tests across core and functions projects and exits 0
- Core schema tests cover at least 3 schemas with valid/invalid input cases
- Resolver tests verify return shapes and error conditions with mocked DynamoDB
  </success_criteria>

<output>
After completion, create `.planning/phases/06-testing-and-ci/06-01-SUMMARY.md`
</output>
