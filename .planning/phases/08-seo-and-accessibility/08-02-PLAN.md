---
phase: 08-seo-and-accessibility
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - packages/frontend/src/components/session/session-shell.tsx
  - packages/frontend/src/components/session/qa-panel.tsx
  - packages/frontend/src/components/language-switcher.tsx
  - packages/frontend/src/components/session/new-content-banner.tsx
  - packages/frontend/src/app/[locale]/layout.tsx
autonomous: true
requirements:
  - A11Y-01
  - A11Y-02
  - A11Y-03

must_haves:
  truths:
    - "SessionShell uses semantic HTML: header for the session title bar, nav for the mobile tab bar, section with aria-label for clipboard and Q&A columns"
    - "Every icon-only button (language switcher, copy, delete, theme toggle, upvote, downvote) has an aria-label readable by screen readers"
    - "Language switcher buttons have aria-label describing the action and aria-current on the active locale"
    - "QAPanel has a permanently-mounted aria-live polite region that announces new question counts without conditional rendering"
    - "A skip-to-content link appears on first Tab press, targeting the Q&A feed scroll container via id"
    - "Tabbing through the session page reaches every interactive element in logical order with visible focus rings"
  artifacts:
    - path: "packages/frontend/src/components/session/session-shell.tsx"
      provides: "Semantic landmarks: header, nav, section with aria-labels"
      contains: "<header"
    - path: "packages/frontend/src/components/session/qa-panel.tsx"
      provides: "Permanently-mounted aria-live region and id anchor for skip link"
      contains: "aria-live"
    - path: "packages/frontend/src/components/language-switcher.tsx"
      provides: "ARIA labels on locale buttons"
      contains: "aria-label"
    - path: "packages/frontend/src/app/[locale]/layout.tsx"
      provides: "Skip-to-content link"
      contains: "skip"
  key_links:
    - from: "packages/frontend/src/app/[locale]/layout.tsx"
      to: "packages/frontend/src/components/session/qa-panel.tsx"
      via: "skip link href=#qa-feed targets id=qa-feed"
      pattern: "qa-feed"
    - from: "packages/frontend/src/components/session/qa-panel.tsx"
      to: "screen reader"
      via: "aria-live polite region text content swap"
      pattern: "aria-live.*polite"
---

<objective>
Add semantic HTML landmarks, ARIA labels, aria-live regions, skip-to-content navigation, and focus ring audit across all session components.

Purpose: Screen reader users can navigate the session page via landmarks, receive count-only announcements for new questions, and keyboard users can skip to the Q&A feed. All icon-only buttons are accessible.
Output: Updates to 5 existing files with semantic HTML and ARIA enhancements.
</objective>

<execution_context>
@/Users/santiago.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/santiago.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-seo-and-accessibility/08-RESEARCH.md

@packages/frontend/src/components/session/session-shell.tsx
@packages/frontend/src/components/session/qa-panel.tsx
@packages/frontend/src/components/session/new-content-banner.tsx
@packages/frontend/src/components/language-switcher.tsx
@packages/frontend/src/app/[locale]/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Semantic HTML in SessionShell and ARIA labels on LanguageSwitcher</name>
  <files>
    packages/frontend/src/components/session/session-shell.tsx
    packages/frontend/src/components/language-switcher.tsx
  </files>
  <action>
    **SessionShell** (A11Y-01) — Replace generic divs with semantic landmarks. CRITICAL: Do NOT add a `<main>` — the `[locale]/layout.tsx` already wraps children in `<main>`. SessionShell renders inside that `<main>`.

    Changes:
    1. The session title/controls bar div (`border-b border-border px-5 py-3.5`) becomes `<header>`.
    2. The mobile tab bar div (`flex border-b border-border lg:hidden`) becomes `<nav aria-label="Session panels">`.
    3. The two desktop column divs wrapping `clipboardSlot` and `qaSlot` become `<section aria-label={t("clipboard")}>` and `<section aria-label={t("qa")}>` respectively.
    4. The mobile single-panel wrapper div also uses `<section>` with the appropriate aria-label based on `activeTab`.
    5. The mobile tab buttons need `aria-label` attributes: `aria-label={t("clipboard")}` and `aria-label={t("qa")}` (they already have visible text, but ensuring the icon+text combo is described).

    **LanguageSwitcher** (A11Y-02) — Add descriptive ARIA labels:
    1. Each locale button gets `aria-label={`Switch to ${l.code === "en" ? "English" : l.code === "es" ? "Spanish" : "Portuguese"}`}`.
    2. Active locale button gets `aria-current="true"`.
    3. The wrapping div gets `role="group"` and `aria-label="Language"`.

  </action>
  <verify>
    <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx tsc --noEmit -p packages/frontend/tsconfig.json</automated>
    <manual>Verify SessionShell renders header, nav, section elements; LanguageSwitcher has aria-label on each button</manual>
  </verify>
  <done>SessionShell uses header, nav, section landmarks; LanguageSwitcher buttons have aria-label and aria-current</done>
</task>

<task type="auto">
  <name>Task 2: Skip-to-content link, aria-live region, and focus ring audit</name>
  <files>
    packages/frontend/src/app/[locale]/layout.tsx
    packages/frontend/src/components/session/qa-panel.tsx
    packages/frontend/src/components/session/new-content-banner.tsx
  </files>
  <action>
    **Skip-to-content link** (A11Y-03) — In `[locale]/layout.tsx`:
    1. Add a visually-hidden skip link before the `<header>` element: `<a href="#qa-feed" className="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-50 focus:rounded-xl focus:bg-emerald-500 focus:px-4 focus:py-2 focus:text-sm focus:font-bold focus:text-white focus:outline-none focus:ring-2 focus:ring-emerald-500 focus:ring-offset-2">Skip to Q&amp;A feed</a>`.
    2. The link uses Tailwind `sr-only` to be hidden until focused, then appears visually on first Tab press.

    **QAPanel aria-live region** (A11Y-02) — In `qa-panel.tsx`:
    1. Add `id="qa-feed"` to the scroll container div (`ref={scrollContainerRef}`) — this is the skip link target.
    2. Add a permanently-mounted `aria-live` region inside the component return, before the scroll container: `<div aria-live="polite" aria-atomic="true" className="sr-only">{announcement}</div>`.
    3. Add `const [announcement, setAnnouncement] = useState("")` state.
    4. In the existing `useEffect` that detects new questions (the one checking `questions.length`): when new questions arrive and `newCount > oldCount`, also call `setAnnouncement(\`${newCount - oldCount} new question${(newCount - oldCount) !== 1 ? "s" : ""}\`)`. Use the locked CONTEXT.md format: count-only announcements.
    5. In the `scrollToTop` callback, add `setAnnouncement("")` to clear the announcement after user scrolls up.
    6. In `handleScroll` when banner is dismissed (scrollTop <= threshold), also `setAnnouncement("")`.
    7. The visual `NewContentBanner` remains unchanged for sighted users — the `aria-live` region is a parallel screen-reader-only channel.

    **NewContentBanner** (A11Y-02) — Add `aria-hidden="true"` to the visual banner div to prevent double-announcement (the aria-live region in QAPanel handles screen reader output):
    1. Add `aria-hidden="true"` to the outer div of `NewContentBanner`.

    **Focus ring audit** (A11Y-03):
    - The design system already applies focus rings via Tailwind `focus:ring-2 focus:ring-emerald-500`. Verify all interactive elements in the modified files have visible focus rings. The skip-to-content link explicitly includes `focus:ring-2 focus:ring-emerald-500 focus:ring-offset-2`.
    - Ensure `NewContentBanner` (role="button") has a focus ring: add `focus:outline-none focus:ring-2 focus:ring-emerald-500 focus:ring-offset-1` to its className.

  </action>
  <verify>
    <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx tsc --noEmit -p packages/frontend/tsconfig.json && npm test 2>&1 | tail -10</automated>
    <manual>Tab through a session page — skip link should appear on first Tab, QAPanel scroll container has id="qa-feed", aria-live region is always in DOM</manual>
  </verify>
  <done>Skip-to-content link targets #qa-feed; QAPanel has permanently-mounted aria-live region with count-only announcements; NewContentBanner has focus ring and aria-hidden; all interactive elements have visible focus indicators</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p packages/frontend/tsconfig.json` passes
2. `npm test` passes (existing tests still green)
3. SessionShell renders `<header>`, `<nav>`, `<section>` — no `<main>` added (layout.tsx already has it)
4. QAPanel has `id="qa-feed"` on scroll container and permanently-mounted `aria-live="polite"` div
5. LanguageSwitcher buttons have `aria-label` and `aria-current`
6. Skip link at top of locale layout with `sr-only` / `focus:not-sr-only` classes
7. No duplicate `<main>` landmarks on any page
</verification>

<success_criteria>

- Semantic landmarks in SessionShell (header, nav, section) with no extra main element
- aria-live polite region in QAPanel permanently mounted, announcing count-only new question alerts
- Skip-to-content link hidden until Tab press, targets QAPanel scroll container
- Language switcher buttons have descriptive aria-labels and aria-current
- All modified interactive elements have visible focus rings
- TypeScript compiles and all existing tests pass
  </success_criteria>

<output>
After completion, create `.planning/phases/08-seo-and-accessibility/08-02-SUMMARY.md`
</output>
