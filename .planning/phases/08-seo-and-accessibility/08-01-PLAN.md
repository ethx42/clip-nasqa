---
phase: 08-seo-and-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/app/robots.ts
  - packages/frontend/src/app/sitemap.ts
  - packages/frontend/src/app/opengraph-image.tsx
  - packages/frontend/src/app/layout.tsx
  - packages/frontend/src/app/[locale]/layout.tsx
  - packages/frontend/src/app/[locale]/session/[slug]/page.tsx
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-04
  - SEO-05

must_haves:
  truths:
    - "Fetching /robots.txt returns Disallow rules for all locale session paths and a sitemap reference"
    - "Fetching /sitemap.xml returns entries for the landing page in all 3 locales with hreflang alternates"
    - "The root layout declares metadataBase as https://clip.nasqa.io with a title template"
    - "Session pages have generateMetadata producing dynamic title, description, noindex robots, and OG tags pointing to the static branded image"
    - "The landing page layout includes JSON-LD WebApplication structured data"
    - "An opengraph-image.tsx generates a static branded OG image with emerald-on-dark composition"
  artifacts:
    - path: "packages/frontend/src/app/robots.ts"
      provides: "robots.txt with session path disallow rules"
      exports: ["default"]
    - path: "packages/frontend/src/app/sitemap.ts"
      provides: "sitemap.xml with locale variants and hreflang"
      exports: ["default"]
    - path: "packages/frontend/src/app/opengraph-image.tsx"
      provides: "Static branded OG image via ImageResponse"
      exports: ["default", "alt", "size", "contentType"]
    - path: "packages/frontend/src/app/[locale]/session/[slug]/page.tsx"
      provides: "Dynamic generateMetadata for session pages"
      exports: ["generateMetadata"]
  key_links:
    - from: "packages/frontend/src/app/[locale]/session/[slug]/page.tsx"
      to: "packages/frontend/src/lib/session.ts"
      via: "getSession + getSessionData calls in generateMetadata"
      pattern: "getSession\\(slug\\)"
    - from: "packages/frontend/src/app/layout.tsx"
      to: "all pages"
      via: "metadataBase propagation"
      pattern: "metadataBase.*clip\\.nasqa\\.io"
---

<objective>
Add all SEO metadata infrastructure: robots.txt, sitemap.xml, OG image, dynamic session metadata, root metadataBase, and JSON-LD structured data.

Purpose: Search engines correctly index the landing page with rich previews while session pages are excluded from indexing. Social sharing on Slack/Twitter shows branded OG images and dynamic session titles.
Output: 4 new files (robots.ts, sitemap.ts, opengraph-image.tsx) + updates to root layout, locale layout, and session page.
</objective>

<execution_context>
@/Users/santiago.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/santiago.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-seo-and-accessibility/08-RESEARCH.md

@packages/frontend/src/app/layout.tsx
@packages/frontend/src/app/[locale]/layout.tsx
@packages/frontend/src/app/[locale]/page.tsx
@packages/frontend/src/app/[locale]/session/[slug]/page.tsx
@packages/frontend/src/lib/session.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create robots.ts, sitemap.ts, and opengraph-image.tsx</name>
  <files>
    packages/frontend/src/app/robots.ts
    packages/frontend/src/app/sitemap.ts
    packages/frontend/src/app/opengraph-image.tsx
  </files>
  <action>
    Create three new files in `packages/frontend/src/app/`:

    **robots.ts** (SEO-04):
    - Export default function returning `MetadataRoute.Robots`
    - `rules`: single object with `userAgent: "*"`, `allow: "/"`, `disallow: ["/en/session/", "/es/session/", "/pt/session/"]`
    - `sitemap: "https://clip.nasqa.io/sitemap.xml"`

    **sitemap.ts** (SEO-05):
    - Define `BASE_URL = "https://clip.nasqa.io"` and `LOCALES = ["en", "es", "pt"] as const`
    - Export default function returning `MetadataRoute.Sitemap`
    - Map over LOCALES to produce one entry per locale: `url: BASE_URL/locale`, `lastModified: new Date()`, `changeFrequency: "monthly"`, `priority: locale === "en" ? 1.0 : 0.8`
    - Each entry includes `alternates.languages` with all 3 locale URLs (hreflang per user decision)

    **opengraph-image.tsx** (SEO-02, SEO-03 — static branded image for both landing and session pages per CONTEXT.md decision):
    - Import `ImageResponse` from `next/og`
    - Export `alt = "clip — real-time clipboard and Q&A for live sessions"`
    - Export `size = { width: 1200, height: 630 }`
    - Export `contentType = "image/png"`
    - Default export renders an abstract branded composition: near-black background (#09090b — Zinc-950), centered "clip" wordmark in emerald-500 (#10b981) at 72px bold, with a subtle decorative emerald circle/glow element behind the text. Keep it minimal per Rams' principles — brand colors and shapes, no screenshot, no text beyond the logo (locked decision).

  </action>
  <verify>
    <automated>cd /Users/santiago.torres/codebases/personal/nasqa-clip && npx tsc --noEmit -p packages/frontend/tsconfig.json</automated>
    <manual>Verify robots.ts, sitemap.ts, and opengraph-image.tsx exist with correct exports</manual>
  </verify>
  <done>robots.ts disallows all 3 locale session paths and references sitemap; sitemap.ts returns 3 locale entries with hreflang; opengraph-image.tsx generates a branded emerald-on-dark PNG</done>
</task>

<task type="auto">
  <name>Task 2: Add metadataBase, generateMetadata, and JSON-LD structured data</name>
  <files>
    packages/frontend/src/app/layout.tsx
    packages/frontend/src/app/[locale]/layout.tsx
    packages/frontend/src/app/[locale]/session/[slug]/page.tsx
  </files>
  <action>
    **Root layout (layout.tsx)** — Update the existing `metadata` export:
    - Add `metadataBase: new URL("https://clip.nasqa.io")`
    - Change `title` to `{ default: "clip — real-time clipboard and Q&A", template: "%s — clip" }`
    - Keep `description` as-is or refine to "Share code snippets and collect audience questions in real time."
    - Add `openGraph: { siteName: "clip", type: "website" }`
    - Add `twitter: { card: "summary_large_image" }`

    **Locale layout ([locale]/layout.tsx)** — Add JSON-LD structured data:
    - Since the landing page is a client component (`"use client"`), inject JSON-LD in the locale layout (server component) so it's always in the static HTML shell.
    - Add a `<script type="application/ld+json" dangerouslySetInnerHTML>` before the header, containing WebApplication schema: `@context: "https://schema.org"`, `@type: "WebApplication"`, `name: "clip"`, `url: "https://clip.nasqa.io"`, `description: "Real-time clipboard and Q&A for live sessions"`, `applicationCategory: "PresentationApplication"`, `operatingSystem: "Web"`.
    - The product name in all metadata is "clip" (not "nasqa" — locked decision). Update the Link text in the header from "nasqa" to "clip".

    **Session page ([locale]/session/[slug]/page.tsx)** — Add `generateMetadata` (SEO-01, SEO-03):
    - Export async `generateMetadata` function accepting `params: Promise<{ locale: string; slug: string }>`
    - Await params, call `getSession(slug)`. If null, return `{ title: "Session not found" }`.
    - Call `getSessionData(slug)` to get question count for the OG description.
    - Build `status` string: if `session.isActive` then `"Live now — N questions asked"`, else `"Ended — N questions"`.
    - Return metadata object with:
      - `title: session.title` (template from root will append " — clip")
      - `description: status`
      - `robots: { index: false, follow: false }` (belt-and-suspenders noindex — locked decision)
      - `openGraph: { title: "${session.title} by clip", description: status, images: ["/opengraph-image"], type: "website" }` (static branded image — locked decision from CONTEXT.md, NOT dynamic)
      - `twitter: { card: "summary_large_image", title: "${session.title} by clip", description: status }`

  </action>
  <verify>
    <automated>cd /Users/santiago.torres/codebases/personal/nasqa-clip && npx tsc --noEmit -p packages/frontend/tsconfig.json && npm run build --prefix packages/frontend 2>&1 | tail -5</automated>
    <manual>Check that the build succeeds and metadata is correctly structured</manual>
  </verify>
  <done>Root layout has metadataBase pointing to clip.nasqa.io with title template; locale layout includes JSON-LD WebApplication; session page generateMetadata produces dynamic title/description with noindex and static OG image reference</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p packages/frontend/tsconfig.json` passes with no errors
2. `npm run build --prefix packages/frontend` succeeds
3. robots.ts, sitemap.ts, opengraph-image.tsx exist as new files in `app/`
4. Session page exports `generateMetadata` with `robots: { index: false }`
5. Root layout `metadata.metadataBase` is `https://clip.nasqa.io`
</verification>

<success_criteria>

- robots.txt disallows session paths and references sitemap
- sitemap.xml lists 3 locale landing page entries with hreflang alternates
- OG image is a branded emerald-on-dark static PNG
- Session pages have dynamic metadata with noindex
- JSON-LD WebApplication schema on landing page
- Product name is "clip" in all metadata
- TypeScript compiles and Next.js builds successfully
  </success_criteria>

<output>
After completion, create `.planning/phases/08-seo-and-accessibility/08-01-SUMMARY.md`
</output>
