---
phase: 18-header-and-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/components/app-header.tsx
  - packages/frontend/src/components/hamburger-menu.tsx
  - packages/frontend/src/components/session/live-indicator.tsx
  - packages/frontend/src/app/[locale]/layout.tsx
  - packages/frontend/src/app/globals.css
  - packages/frontend/src/components/session/session-shell.tsx
  - packages/frontend/messages/en.json
  - packages/frontend/messages/es.json
  - packages/frontend/messages/pt.json
autonomous: true
requirements:
  - HDR-01
  - HDR-04

must_haves:
  truths:
    - "The global layout renders a single 56px header bar instead of the previous two-bar layout"
    - "Theme toggle and language selector are accessible inside a hamburger dropdown menu"
    - "The session-shell internal header is removed — session pages no longer render a second bar"
    - "All pages use --header-height CSS variable for height calculations instead of hardcoded 73px"
  artifacts:
    - path: "packages/frontend/src/components/app-header.tsx"
      provides: "Unified header bar with logo, app name, slots for session context, hamburger, and profile"
      min_lines: 40
    - path: "packages/frontend/src/components/hamburger-menu.tsx"
      provides: "Dropdown panel with labeled theme toggle switch and inline language switcher"
      min_lines: 30
    - path: "packages/frontend/src/app/globals.css"
      provides: "CSS variable --header-height: 56px"
      contains: "--header-height"
  key_links:
    - from: "packages/frontend/src/app/[locale]/layout.tsx"
      to: "packages/frontend/src/components/app-header.tsx"
      via: "import and render AppHeader"
      pattern: "AppHeader"
    - from: "packages/frontend/src/components/app-header.tsx"
      to: "packages/frontend/src/components/hamburger-menu.tsx"
      via: "import and render HamburgerMenu"
      pattern: "HamburgerMenu"
    - from: "packages/frontend/src/components/session/session-shell.tsx"
      to: "packages/frontend/src/app/globals.css"
      via: "uses --header-height variable"
      pattern: "var\\(--header-height\\)"
---

<objective>
Create the unified 56px header bar component with hamburger menu, replace the old two-bar layout, and establish the CSS variable foundation.

Purpose: The current layout has two bars (73px global header + ~44px session header inside SessionShell). This plan creates the single AppHeader component that all pages share and moves theme/language controls into a hamburger dropdown.

Output: AppHeader and HamburgerMenu components, updated layout.tsx, SessionShell without its internal header, CSS variable for height calculations.
</objective>

<execution_context>
@/Users/santiago.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/santiago.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-header-and-navigation/18-CONTEXT.md
@packages/frontend/src/app/[locale]/layout.tsx
@packages/frontend/src/components/session/session-shell.tsx
@packages/frontend/src/components/theme-toggle.tsx
@packages/frontend/src/components/language-switcher.tsx
@packages/frontend/src/components/session/live-indicator.tsx
@packages/frontend/src/components/session/identity-editor.tsx
@packages/frontend/src/components/session/host-toolbar.tsx
@packages/frontend/src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AppHeader, HamburgerMenu, and rework LiveIndicator</name>
  <files>
    packages/frontend/src/components/app-header.tsx
    packages/frontend/src/components/hamburger-menu.tsx
    packages/frontend/src/components/session/live-indicator.tsx
    packages/frontend/src/app/globals.css
    packages/frontend/messages/en.json
    packages/frontend/messages/es.json
    packages/frontend/messages/pt.json
  </files>
  <action>
**AppHeader** (`app-header.tsx`) — "use client" component:
- Height: 56px (h-14), full-width, `bg-muted` background (slightly tinted vs page), soft drop shadow (`shadow-sm`), NO `border-b`.
- Position: static (not sticky), scrolls with content.
- Left side: Logo SVG (same clip logo from current layout.tsx, scaled to fit 56px — use `h-6 w-auto`) wrapped in a link. Next to it: "CLIP" text (bold, tracking-tight, indigo-500/amber-400 dark). Then a named slot (`sessionContext`) for session title + live indicator — rendered only when provided.
- Right side: Optional `shareSlot` (for host share button), then `HamburgerMenu`, then profile section (IdentityEditor trigger with first name display).
- Gap between share area and personal controls: extra spacing (gap-4 or ml-4) but no visible divider.
- Props interface:
  ```
  sessionContext?: React.ReactNode  // session title + live indicator
  shareSlot?: React.ReactNode       // host share button
  ```
- Import and render `HamburgerMenu` and `IdentityEditor` internally.
- Mobile (< 640px / `sm:` breakpoint): Hide "CLIP" text via `hidden sm:inline`. Session title (if present) centers via flex. Profile name hidden on mobile (icon only).
- Logo link: `href="/"` always. When `sessionContext` is provided (meaning we're in a session), wrap the Link click in a confirmation — use the `Dialog` from `@base-ui/react/dialog` (NOT window.confirm per CLAUDE.md rules). Add state for dialog open/close. On confirm, navigate to "/". On cancel, close dialog.

**HamburgerMenu** (`hamburger-menu.tsx`) — "use client" component:

- Uses `Popover` from `@base-ui/react/popover` (same pattern as HostToolbar).
- Trigger: `Menu` icon from lucide-react (three horizontal lines), 44px min touch target, `rounded-lg p-2` styling.
- Dropdown panel: `w-64`, drops down from trigger, `rounded-2xl border border-border bg-card p-4 shadow-xl`.
- Contents (vertical stack with `space-y-4`):
  1. **Theme toggle** — Labeled row: "Dark mode" text on left + a toggle switch on right. Use `useTheme` from next-themes. Render a simple toggle button (div with sliding circle, bg-indigo-500 when active). NOT just an icon — must show label text.
  2. **Language switcher** — Render the existing `LanguageSwitcher` component (inline EN | ES | PT style) with a small label above ("Language").
  3. **About** — Small text showing "clip v2.2" or similar app version info at bottom, muted color.
- Animation: The Popover handles its own open/close. No custom animation needed.

**LiveIndicator rework** (`live-indicator.tsx`):

- Change layout from horizontal (dot + text side by side) to vertical stack: pulsing dot on top, "LIVE" text below in `text-[10px] font-bold uppercase tracking-widest`.
- Keep the three states (connected/connecting/disconnected) with same colors.
- Keep the stale host activity display but move it to a separate line or tooltip (since vertical layout is compact).
- Wrap in a `flex flex-col items-center gap-0.5` container.
- The overall component should be compact enough to sit next to session title in the header.

**globals.css**: Add `--header-height: 56px` to the `:root` selector (or create one if it doesn't exist in the CSS custom properties section).

**i18n keys** — Add to all three locale files (en.json, es.json, pt.json) under the `"session"` namespace:

- `"darkMode"`: "Dark mode" / "Modo oscuro" / "Modo escuro"
- `"language"`: "Language" / "Idioma" / "Idioma"
- `"leaveSession"`: "Leave this session?" / "Salir de esta sesion?" / "Sair desta sessao?"
- `"leaveConfirm"`: "Leave" / "Salir" / "Sair"
- `"cancel"`: "Cancel" / "Cancelar" / "Cancelar" (check if already exists before adding)
- `"about"`: "About" / "Acerca de" / "Sobre"
  </action>
  <verify>
  <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx tsc --noEmit --project packages/frontend/tsconfig.json 2>&1 | head -30</automated>
  <manual>Verify AppHeader and HamburgerMenu files exist and export named components</manual>
  </verify>
  <done>AppHeader and HamburgerMenu components compile without type errors. LiveIndicator uses vertical stack layout. CSS variable --header-height defined. i18n keys added to all 3 locales.</done>
  </task>

<task type="auto">
  <name>Task 2: Replace old layout header and remove SessionShell internal header</name>
  <files>
    packages/frontend/src/app/[locale]/layout.tsx
    packages/frontend/src/components/session/session-shell.tsx
    packages/frontend/src/app/[locale]/page.tsx
    packages/frontend/src/app/[locale]/[code]/page.tsx
    packages/frontend/src/app/[locale]/[code]/host/page.tsx
    packages/frontend/src/app/[locale]/not-found.tsx
    packages/frontend/src/components/join/join-form.tsx
  </files>
  <action>
**layout.tsx** — Replace the inline `<header>` element (lines 42-66) with `<AppHeader />`. Remove the direct imports of `LanguageSwitcher` and `ThemeToggle` (they now live inside HamburgerMenu). The layout becomes:
```
<NextIntlClientProvider messages={messages}>
  {/* JSON-LD */}
  {/* Skip-to-content link */}
  <AppHeader />
  <main>{children}</main>
</NextIntlClientProvider>
```
Note: `AppHeader` receives no session-specific props here — session pages will provide those via a different mechanism (plan 02).

**session-shell.tsx** — Remove the entire `<header>` block (lines 67-76: the `<header className="border-b...">` containing title, liveIndicator, hostToolbar, IdentityEditor). The component should start directly with the mobile tab bar nav. Update the outer div from `h-[calc(100dvh-73px)]` to `h-[calc(100dvh-var(--header-height))]`. Remove `liveIndicator`, `hostToolbar`, and `title` from the props interface — these will be handled by AppHeader in plan 02. Keep `sessionCode`, `clipboardSlot`, `qaSlot`, `snippetCount`, `questionCount`, `isHost`.

**All pages with `calc(100dvh-73px)`** — Replace every occurrence with `calc(100dvh-var(--header-height))`:

- `packages/frontend/src/app/[locale]/page.tsx` (landing page hero section)
- `packages/frontend/src/app/[locale]/[code]/page.tsx` (session expired state)
- `packages/frontend/src/app/[locale]/[code]/host/page.tsx` (host expired state)
- `packages/frontend/src/app/[locale]/not-found.tsx` (if it has this pattern)
- `packages/frontend/src/components/join/join-form.tsx` (if it has this pattern)

**SessionLiveHostPage and SessionLivePage** — These pass `title`, `hostToolbar`, `liveIndicator` to SessionShell. Since those props are being removed from SessionShell in this task, temporarily remove those prop passes. The session-specific header content wiring happens in plan 02.

IMPORTANT: Do NOT remove the `liveIndicator` rendering from `SessionLiveHostPage` or `SessionLivePage` entirely — just stop passing them to SessionShell. We will re-wire them to AppHeader in plan 02.
</action>
<verify>
<automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx tsc --noEmit --project packages/frontend/tsconfig.json 2>&1 | head -30 && echo "---BUILD---" && cd packages/frontend && npx next build 2>&1 | tail -20</automated>
<manual>Run the dev server and check: (1) all pages show the new single 56px header bar, (2) hamburger menu opens with theme toggle and language switcher, (3) no second header bar appears on session pages</manual>
</verify>
<done>Layout.tsx renders AppHeader instead of inline header. SessionShell has no internal header. All hardcoded 73px replaced with CSS variable. TypeScript compiles and Next.js builds successfully.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npx next build` succeeds
- Grep for `73px` in tsx files returns zero matches (all migrated to CSS variable)
- AppHeader renders on all pages (landing, join, session, host)
- HamburgerMenu contains theme toggle and language switcher
</verification>

<success_criteria>

- Single header bar visible on all pages (HDR-01)
- Hamburger menu contains theme toggle and language selector (HDR-04)
- No hardcoded 73px height references remain in .tsx files
- TypeScript and Next.js build pass
  </success_criteria>

<output>
After completion, create `.planning/phases/18-header-and-navigation/18-01-SUMMARY.md`
</output>
