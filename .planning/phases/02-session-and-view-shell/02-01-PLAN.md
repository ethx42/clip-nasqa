---
phase: 02-session-and-view-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/app/globals.css
  - packages/frontend/src/app/layout.tsx
  - packages/frontend/next.config.ts
  - packages/frontend/src/middleware.ts
  - packages/frontend/src/i18n/request.ts
  - packages/frontend/src/app/[locale]/layout.tsx
  - packages/frontend/src/components/theme-toggle.tsx
  - packages/frontend/messages/en.json
  - packages/frontend/messages/es.json
  - packages/frontend/messages/pt.json
  - sst.config.ts
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "Theme toggle switches between light and dark mode with no layout shift"
    - "Light mode uses White background, Zinc-900 text, Zinc-200 borders"
    - "Dark mode uses Zinc-950 background, Zinc-50 text, Zinc-800 borders"
    - "Emerald-500 is the primary/accent color in both themes"
    - "Pages route under [locale] segment with browser-detected locale"
  artifacts:
    - path: "packages/frontend/src/app/globals.css"
      provides: "Zinc/Emerald CSS variable overrides for both :root and .dark"
      contains: "emerald"
    - path: "packages/frontend/src/components/theme-toggle.tsx"
      provides: "Sun/moon icon theme toggle client component"
      exports: ["ThemeToggle"]
    - path: "packages/frontend/src/app/layout.tsx"
      provides: "Root layout with ThemeProvider wrapping children"
      contains: "ThemeProvider"
    - path: "packages/frontend/src/middleware.ts"
      provides: "next-intl locale detection middleware"
      contains: "createMiddleware"
    - path: "packages/frontend/src/i18n/request.ts"
      provides: "next-intl request config for loading locale messages"
      contains: "getRequestConfig"
    - path: "packages/frontend/src/app/[locale]/layout.tsx"
      provides: "Locale layout with NextIntlClientProvider"
      contains: "NextIntlClientProvider"
  key_links:
    - from: "packages/frontend/src/app/layout.tsx"
      to: "next-themes ThemeProvider"
      via: "ThemeProvider wraps children with attribute='class'"
      pattern: "ThemeProvider.*attribute.*class"
    - from: "packages/frontend/src/middleware.ts"
      to: "packages/frontend/src/i18n/request.ts"
      via: "next-intl middleware routes to [locale] segment"
      pattern: "createMiddleware"
    - from: "sst.config.ts"
      to: "NasqaTable"
      via: "link: [table] on Next.js site resource"
      pattern: "link.*table"
---

<objective>
Wire the theme system (next-themes + Zinc/Emerald CSS vars), locale routing (next-intl middleware + [locale] segment), and SST Next.js resource linking so all subsequent pages have a working theme toggle, correct palette, i18n support, and access to the DynamoDB table name.

Purpose: Every page in Phase 2 depends on the root layout, theme, and locale routing being in place first.
Output: Root layout with ThemeProvider, locale middleware, theme toggle component, CSS variable overrides, SST table link, translation stubs.
</objective>

<execution_context>
@/Users/santiago.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/santiago.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-session-and-view-shell/02-RESEARCH.md
@packages/frontend/src/app/globals.css
@packages/frontend/src/app/layout.tsx
@packages/frontend/next.config.ts
@packages/frontend/components.json
@sst.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Theme system — CSS vars, ThemeProvider, theme toggle</name>
  <files>
    packages/frontend/src/app/globals.css
    packages/frontend/src/app/layout.tsx
    packages/frontend/src/components/theme-toggle.tsx
  </files>
  <action>
1. In `globals.css`, override the shadcn CSS variable values in `:root` and `.dark` blocks to match the Nasqa palette:
   - `:root` — `--background: oklch(1 0 0)` (White), `--foreground: oklch(0.21 0.006 285.88)` (Zinc-900), `--border: oklch(0.92 0.004 286.32)` (Zinc-200), `--primary: oklch(0.696 0.17 162.48)` (Emerald-500), `--primary-foreground: oklch(1 0 0)` (White). Also update `--card`, `--popover`, `--muted`, `--accent`, `--input`, `--ring` to Zinc tones matching the light palette.
   - `.dark` — `--background: oklch(0.09 0.005 286.29)` (Zinc-950), `--foreground: oklch(0.985 0 0)` (Zinc-50), `--border: oklch(0.269 0.006 286.03)` (Zinc-800), `--primary: oklch(0.696 0.17 162.48)` (Emerald-500 same), `--primary-foreground: oklch(1 0 0)`. Update `--card`, `--popover`, `--muted`, `--accent`, `--input`, `--ring` to Zinc dark tones.
   - Keep the existing `@theme inline`, `@import`, and `@layer base` blocks unchanged. Increase `--radius` to `0.75rem` for the "playful rounded" aesthetic per user decision.

2. In `layout.tsx` (root layout):
   - Import `ThemeProvider` from `next-themes`. Since ThemeProvider uses hooks, create a thin `providers.tsx` client component wrapper that wraps children with `<ThemeProvider attribute="class" defaultTheme="light" disableTransitionOnChange>`.
   - Add `suppressHydrationWarning` to the `<html>` element.
   - Keep existing Geist font setup.
   - Update metadata to `title: "Nasqa Live"`, `description: "Real-time clipboard and Q&A for live sessions"`.

3. Create `theme-toggle.tsx` as a `'use client'` component:
   - Import `useTheme` from `next-themes`, `Sun` and `Moon` from `lucide-react`.
   - Render a `<button>` with `aria-label="Toggle theme"` that toggles between light/dark.
   - Use shadcn-style classes: `inline-flex items-center justify-center rounded-md p-2 hover:bg-accent transition-colors`.
   - Handle mounted state to avoid hydration mismatch (common next-themes pattern: only render icon after `useEffect` sets mounted=true).
  </action>
  <verify>
    <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx tsc --noEmit -p packages/frontend/tsconfig.json</automated>
    <manual>Visual check: theme toggle renders sun/moon, clicking toggles dark/light classes on html element</manual>
  </verify>
  <done>CSS vars produce Zinc/Emerald palette in both light and dark modes. ThemeProvider wraps the app with class-based toggling. Theme toggle component renders and switches themes without layout shift.</done>
</task>

<task type="auto">
  <name>Task 2: i18n routing, SST table link, locale layout</name>
  <files>
    packages/frontend/src/middleware.ts
    packages/frontend/src/i18n/request.ts
    packages/frontend/src/app/[locale]/layout.tsx
    packages/frontend/next.config.ts
    packages/frontend/messages/en.json
    packages/frontend/messages/es.json
    packages/frontend/messages/pt.json
    sst.config.ts
  </files>
  <action>
1. Create `packages/frontend/src/middleware.ts`:
   - Import `createMiddleware` from `next-intl/middleware`.
   - Configure with `locales: ['en', 'es', 'pt']`, `defaultLocale: 'en'`, `localeDetection: true`.
   - Export `config.matcher` as `['/((?!api|_next|_vercel|.*\\..*).*)']`.

2. Create `packages/frontend/src/i18n/request.ts`:
   - Import `getRequestConfig` from `next-intl/server`.
   - Export default `getRequestConfig` that loads messages from `../../messages/${requestLocale}.json`.
   - Follow the next-intl 4.x pattern: `async ({ requestLocale }) => { const locale = await requestLocale ?? 'en'; return { locale, messages: (await import(\`../../messages/${locale}.json\`)).default }; }`.

3. Update `packages/frontend/next.config.ts`:
   - Import `createNextIntlPlugin` from `next-intl/plugin`.
   - Create the plugin: `const withNextIntl = createNextIntlPlugin('./src/i18n/request.ts')`.
   - Export `withNextIntl(nextConfig)` instead of bare `nextConfig`.

4. Create `packages/frontend/src/app/[locale]/layout.tsx`:
   - Server Component that receives `children` and `params: Promise<{ locale: string }>`.
   - Await params to get locale.
   - Import `NextIntlClientProvider` from `next-intl` and `getMessages` from `next-intl/server`.
   - Call `const messages = await getMessages()` and wrap children with `<NextIntlClientProvider messages={messages}>`.
   - Include the ThemeToggle in a `<header>` with the toggle right-aligned, plus a minimal nav bar placeholder.

5. Create translation stub files in `packages/frontend/messages/`:
   - `en.json`: `{ "landing": { "headline": "Share code and take questions — live.", "subtitle": "Create a session, share the link, start presenting.", "cta": "Create Session", "placeholder": "What's your talk about?" }, "common": { "loading": "Loading...", "error": "Something went wrong" }, "session": { "waitingForSpeaker": "Waiting for the speaker to share...", "clipboard": "Clipboard", "qa": "Q&A", "copySecret": "Copy Secret", "copied": "Copied!", "secretWarning": "Save this secret now — it won't be shown again.", "hostUrl": "Host URL", "participantUrl": "Participant URL", "qrCode": "QR Code", "sessionCreated": "Session Created!" } }`
   - `es.json`: Same structure with Spanish translations.
   - `pt.json`: Same structure with Portuguese translations.

6. Move the existing `packages/frontend/src/app/page.tsx` to `packages/frontend/src/app/[locale]/page.tsx` (it will be replaced in Plan 02 with the landing page, but needs to exist for route compilation). Update imports if needed. Delete the old `app/page.tsx`.

7. In `sst.config.ts`, add a Next.js site resource that links to the DynamoDB table:
   - Add after the existing resolver block: `const site = new sst.aws.Nextjs("NasqaSite", { path: "packages/frontend", link: [table] })`.
   - Add `siteUrl: site.url` to the return block.
   - This makes `Resource.NasqaTable.name` available as an env var in Next.js Server Actions/Components.

8. Install `random-word-slugs` in packages/frontend: `cd packages/frontend && npm install random-word-slugs`.
  </action>
  <verify>
    <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx tsc --noEmit -p packages/frontend/tsconfig.json && cd packages/frontend && npx next lint</automated>
    <manual>Run `cd packages/frontend && npx next dev` — visit http://localhost:3000, should redirect to /en and render the placeholder page with theme toggle in header</manual>
  </verify>
  <done>next-intl middleware routes all pages under [locale] with browser detection. Locale layout provides NextIntlClientProvider. Translation stubs exist for en/es/pt. SST config links DynamoDB table to Next.js site. random-word-slugs is installed.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p packages/frontend/tsconfig.json` passes
- `npx next lint` in packages/frontend passes
- Theme toggle component exists and is importable
- CSS variables in globals.css contain Emerald-500 for --primary in both :root and .dark
- middleware.ts exports createMiddleware with en/es/pt locales
- [locale] directory exists with layout.tsx
- messages/en.json, messages/es.json, messages/pt.json exist with translation keys
- sst.config.ts has Nextjs resource linked to table
</verification>

<success_criteria>
Theme system works with Zinc/Emerald palette in both modes. next-intl middleware detects browser locale and routes under [locale]. ThemeToggle renders in header. SST config wires the DynamoDB table name to the Next.js site. All translation stubs are in place.
</success_criteria>

<output>
After completion, create `.planning/phases/02-session-and-view-shell/02-01-SUMMARY.md`
</output>
