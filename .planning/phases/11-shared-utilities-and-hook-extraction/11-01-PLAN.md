---
phase: 11-shared-utilities-and-hook-extraction
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/lib/format-relative-time.ts
  - packages/frontend/src/__tests__/format-relative-time.test.ts
  - packages/frontend/src/components/session/question-card.tsx
  - packages/frontend/src/components/session/reply-list.tsx
  - packages/frontend/src/components/session/clipboard-panel.tsx
  - packages/frontend/src/components/session/snippet-card.tsx
  - packages/frontend/src/components/session/snippet-hero.tsx
autonomous: true
requirements:
  - STRUC-01

must_haves:
  truths:
    - "All five timestamp locations format identically given the same createdAt input"
    - "Changing the rounding rule in format-relative-time.ts updates every timestamp in the app"
    - "Server Components (SnippetCard, SnippetHero) use the non-i18n short-token variant without error"
    - "Client Components (QuestionCard, ReplyList, ClipboardPanel) use the i18n-aware variant with translator function"
  artifacts:
    - path: "packages/frontend/src/lib/format-relative-time.ts"
      provides: "Canonical formatRelativeTime utility"
      exports: ["formatRelativeTime"]
    - path: "packages/frontend/src/__tests__/format-relative-time.test.ts"
      provides: "Unit tests for all time buckets"
      min_lines: 40
  key_links:
    - from: "packages/frontend/src/components/session/question-card.tsx"
      to: "packages/frontend/src/lib/format-relative-time.ts"
      via: "import { formatRelativeTime }"
      pattern: "import.*formatRelativeTime.*from.*@/lib/format-relative-time"
    - from: "packages/frontend/src/components/session/reply-list.tsx"
      to: "packages/frontend/src/lib/format-relative-time.ts"
      via: "import { formatRelativeTime }"
      pattern: "import.*formatRelativeTime.*from.*@/lib/format-relative-time"
    - from: "packages/frontend/src/components/session/clipboard-panel.tsx"
      to: "packages/frontend/src/lib/format-relative-time.ts"
      via: "import { formatRelativeTime }"
      pattern: "import.*formatRelativeTime.*from.*@/lib/format-relative-time"
    - from: "packages/frontend/src/components/session/snippet-card.tsx"
      to: "packages/frontend/src/lib/format-relative-time.ts"
      via: "import { formatRelativeTime }"
      pattern: "import.*formatRelativeTime.*from.*@/lib/format-relative-time"
    - from: "packages/frontend/src/components/session/snippet-hero.tsx"
      to: "packages/frontend/src/lib/format-relative-time.ts"
      via: "import { formatRelativeTime }"
      pattern: "import.*formatRelativeTime.*from.*@/lib/format-relative-time"
---

<objective>
Extract all five duplicate `formatRelativeTime` implementations into a single canonical module at `packages/frontend/src/lib/format-relative-time.ts`, then replace every call site.

Purpose: Eliminate timestamp formatting duplication so a single rounding-rule change propagates everywhere. Two variants: i18n-aware (accepts translator `t`) for Client Components, and short-token (returns "now", "2m", "1h", "3d", "1w") for Server Components that lack access to `useTranslations`.

Output: One utility module, one test file, five updated components with zero local `formatRelativeTime` definitions.
</objective>

<execution_context>
@/Users/santiago.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/santiago.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-shared-utilities-and-hook-extraction/11-CONTEXT.md
@packages/frontend/src/components/session/question-card.tsx
@packages/frontend/src/components/session/reply-list.tsx
@packages/frontend/src/components/session/clipboard-panel.tsx
@packages/frontend/src/components/session/snippet-card.tsx
@packages/frontend/src/components/session/snippet-hero.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD — Create formatRelativeTime with tests</name>
  <files>
    packages/frontend/src/lib/format-relative-time.ts
    packages/frontend/src/__tests__/format-relative-time.test.ts
  </files>
  <action>
RED phase — Write tests first in `format-relative-time.test.ts`:

Test the overloaded function signature. The function has TWO call modes:

1. `formatRelativeTime(createdAt)` — returns short tokens: "now", "2m", "1h", "3d", "1w"
2. `formatRelativeTime(createdAt, t)` — uses translator `t(key, values?)` to return i18n strings

Per user decision on timestamp display rules:

- "now" for < 60 seconds
- "1m"–"59m" for minutes
- "1h"–"23h" for hours
- "1d"–"6d" for days
- "1w"+ for weeks

Test cases (mock `Date.now()` with `vi.useFakeTimers`):

- `createdAt` = now → "now"
- `createdAt` = 30s ago → "now"
- `createdAt` = 59s ago → "now"
- `createdAt` = 60s ago → "1m"
- `createdAt` = 120s ago → "2m"
- `createdAt` = 3599s ago → "59m"
- `createdAt` = 3600s ago → "1h"
- `createdAt` = 7200s ago → "2h"
- `createdAt` = 86399s ago → "23h"
- `createdAt` = 86400s ago → "1d"
- `createdAt` = 518400s ago → "6d"
- `createdAt` = 604800s ago → "1w"
- `createdAt` = 1209600s ago → "2w"

For i18n mode: create a mock `t` function that returns `"i18n:{key}:{count}"` format, verify:

- < 60s calls `t("timeJustNow")`
- 120s calls `t("timeMinutesAgo", { count: 2 })`
- 7200s calls `t("timeHoursAgo", { count: 2 })`
- 86400s calls `t("timeDaysAgo", { count: 1 })`
- 604800s calls `t("timeWeeksAgo", { count: 1 })`

Run tests — they MUST fail (module does not exist yet).

GREEN phase — Create `format-relative-time.ts`:

Export a single function with overloaded signatures:

```typescript
export function formatRelativeTime(createdAt: number): string;
export function formatRelativeTime(createdAt: number, t: (key: string, values?: Record<string, number>) => string): string;
export function formatRelativeTime(createdAt: number, t?: (key: string, values?: Record<string, number>) => string): string {
```

Implementation:

- `createdAt` is Unix epoch SECONDS (not ms) — consistent with existing codebase convention
- Compute `diffSeconds = Math.floor(Date.now() / 1000) - createdAt`
- Apply the bucket thresholds per user decision
- If `t` provided, call `t("timeJustNow")`, `t("timeMinutesAgo", { count })`, etc.
- If `t` not provided, return short tokens: "now", "{n}m", "{n}h", "{n}d", "{n}w"
- Add week bucket: >= 7 days returns weeks (this is NEW — existing code stops at days, user decision says "1w+")

Note on i18n: add "timeWeeksAgo" key to the i18n interface. The existing en/es/pt JSON files will need a `timeWeeksAgo` key added — do this inline.

Run tests — they MUST pass.

Commit after GREEN: `feat(11-01): add canonical formatRelativeTime utility with tests`
</action>
<verify>
<automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx vitest run packages/frontend/src/**tests**/format-relative-time.test.ts</automated>
</verify>
<done>All time bucket tests pass. formatRelativeTime exported from lib/format-relative-time.ts with both overloads working.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all five duplicate implementations with canonical import</name>
  <files>
    packages/frontend/src/components/session/question-card.tsx
    packages/frontend/src/components/session/reply-list.tsx
    packages/frontend/src/components/session/clipboard-panel.tsx
    packages/frontend/src/components/session/snippet-card.tsx
    packages/frontend/src/components/session/snippet-hero.tsx
    packages/frontend/messages/en.json
    packages/frontend/messages/es.json
    packages/frontend/messages/pt.json
  </files>
  <action>
For each of the five files:

1. **question-card.tsx**: Delete the local `formatRelativeTime` function (lines ~44-57). Add `import { formatRelativeTime } from "@/lib/format-relative-time"` at the top. The existing call `formatRelativeTime(question.createdAt, tSession)` stays unchanged — it matches the i18n overload.

2. **reply-list.tsx**: Delete the local `formatRelativeTime` function (lines ~18-29). Add the import. The existing call `formatRelativeTime(reply.createdAt, t)` stays unchanged.

3. **clipboard-panel.tsx**: Delete the local `formatRelativeTime` function (lines ~35-47). Add the import. The existing call `formatRelativeTime(snippet.createdAt, t)` stays unchanged.

4. **snippet-card.tsx**: Delete the local `formatRelativeTime` function (lines ~12-23). Add the import. Update the call to `formatRelativeTime(snippet.createdAt)` — no `t` parameter (Server Component, uses short tokens). This is the non-i18n overload.

5. **snippet-hero.tsx**: Delete the local `formatRelativeTime` function (lines ~13-24). Add the import. Update the call to `formatRelativeTime(snippet.createdAt)` — no `t` parameter (Server Component).

For i18n JSON files (en.json, es.json, pt.json): Add `"timeWeeksAgo"` key to the `session` namespace:

- en: `"timeWeeksAgo": "{count}w ago"`
- es: `"timeWeeksAgo": "hace {count}sem"`
- pt: `"timeWeeksAgo": "há {count}sem"`

After all replacements, verify no file in `packages/frontend/src/components/session/` contains a local `formatRelativeTime` function definition — only imports.

Commit: `refactor(11-01): replace all formatRelativeTime duplicates with canonical import`
</action>
<verify>
<automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx vitest run && npx tsc --noEmit -p packages/frontend/tsconfig.json</automated>
<manual>Grep for "function formatRelativeTime" in components/session/ — should return zero results</manual>
</verify>
<done>Zero local formatRelativeTime definitions remain in component files. All five components import from @/lib/format-relative-time. TypeScript compiles. All existing tests pass.</done>
</task>

</tasks>

<verification>
- `grep -r "function formatRelativeTime" packages/frontend/src/components/` returns empty
- `grep -r "from.*format-relative-time" packages/frontend/src/components/` returns 5 matches
- `npx vitest run` passes all tests including the new format-relative-time tests
- `npx tsc --noEmit -p packages/frontend/tsconfig.json` passes
</verification>

<success_criteria>

- Single canonical formatRelativeTime in lib/format-relative-time.ts
- All five consumer components import from the canonical location
- No local formatRelativeTime function definitions in any component file
- Short-token mode works for Server Components (no translator needed)
- i18n mode works for Client Components (accepts translator function)
- Week bucket added per user decision ("1w+")
- All tests pass (existing + new)
  </success_criteria>

<output>
After completion, create `.planning/phases/11-shared-utilities-and-hook-extraction/11-01-SUMMARY.md`
</output>
