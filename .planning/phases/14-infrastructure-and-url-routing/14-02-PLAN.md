---
phase: 14-infrastructure-and-url-routing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/types.ts
  - packages/core/src/schemas.ts
  - packages/core/src/__tests__/schemas.test.ts
  - packages/frontend/src/lib/session.ts
  - packages/frontend/src/actions/session.ts
  - infra/schema.graphql
  - infra/resolvers/subscription-onSessionUpdate.js
  - packages/functions/src/resolvers/index.handler
autonomous: true
requirements:
  - URL-01
  - URL-06
  - URL-07

must_haves:
  truths:
    - "createSession generates a 6-digit numeric code (100000-999999) instead of a word-pair slug"
    - "DynamoDB keys use SESSION#482913 format instead of SESSION#word-slug"
    - "The Session type uses 'code' field instead of 'slug'"
    - "GraphQL schema uses sessionCode instead of sessionSlug"
    - "random-word-slugs dependency is removed from package.json"
  artifacts:
    - path: "packages/frontend/src/actions/session.ts"
      provides: "Numeric code generation with collision retry"
      contains: "generateNumericCode"
    - path: "packages/core/src/types.ts"
      provides: "Renamed types: code instead of slug, sessionCode instead of sessionSlug"
      contains: "sessionCode"
    - path: "infra/schema.graphql"
      provides: "GraphQL schema with sessionCode arguments"
      contains: "sessionCode"
  key_links:
    - from: "packages/frontend/src/actions/session.ts"
      to: "DynamoDB"
      via: "PutCommand with SESSION#code key"
      pattern: "SESSION#.*code"
    - from: "packages/core/src/types.ts"
      to: "infra/schema.graphql"
      via: "sessionCode field alignment"
      pattern: "sessionCode"
---

<objective>
Replace word-pair slugs with 6-digit numeric session codes throughout the entire data model: types, GraphQL schema, Lambda resolvers, createSession action, and session.ts data layer.

Purpose: Foundation for the URL routing migration — all code must reference numeric codes before routes can be restructured. This is the deepest layer of the slug→code transition.
Output: All types renamed, GraphQL schema updated, code generation replaces slug generation, random-word-slugs removed.
</objective>

<execution_context>
@/Users/santiago.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/santiago.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-infrastructure-and-url-routing/14-RESEARCH.md
@.planning/phases/14-infrastructure-and-url-routing/14-CONTEXT.md
@packages/core/src/types.ts
@packages/core/src/schemas.ts
@infra/schema.graphql
@packages/frontend/src/actions/session.ts
@packages/frontend/src/lib/session.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename slug→code in @nasqa/core types, schemas, and GraphQL schema</name>
  <files>packages/core/src/types.ts, packages/core/src/schemas.ts, packages/core/src/__tests__/schemas.test.ts, infra/schema.graphql</files>
  <action>
1. In `packages/core/src/types.ts`:
   - Rename `SessionItem.slug` → `SessionItem.code` (and update PK/SK comments from `SESSION#slug` to `SESSION#code`)
   - Rename ALL `sessionSlug: string` fields to `sessionCode: string` across every type (SnippetItem, QuestionItem, ReplyItem, Snippet, Question, Reply, and all mutation input types)
   - Update the `Session` frontend type if present: `slug: string` → `code: string`
   - Search exhaustively — every occurrence of `slug` and `sessionSlug` in this file must be renamed

2. In `packages/core/src/schemas.ts`:
   - Rename `sessionSlug` → `sessionCode` in all Zod schemas (SnippetSchema, QuestionSchema, etc.)

3. In `packages/core/src/__tests__/schemas.test.ts`:
   - Update all test fixtures from `sessionSlug: "my-session-abc"` to `sessionCode: "482913"` (or any valid 6-digit code)
   - Update the "rejects missing required sessionSlug" test to reference `sessionCode`

4. In `infra/schema.graphql`:
   - Rename `slug: String!` → `code: String!` in SessionUpdate type
   - Rename ALL `sessionSlug: String!` → `sessionCode: String!` in every Mutation and Query argument
   - Rename `sessionSlug: String!` in Snippet, Question, Reply types → `sessionCode: String!`
   - Update the `onSessionUpdate(sessionSlug: String!)` subscription → `onSessionUpdate(sessionCode: String!)`

5. In Lambda resolvers (`packages/functions/src/resolvers/`):
   - Find every file that references `sessionSlug` in `event.arguments` or in DynamoDB key construction
   - Rename `event.arguments.sessionSlug` → `event.arguments.sessionCode` in every resolver
   - Rename any `slug` variable used for DynamoDB keys to `code`
   - The PK/SK pattern `SESSION#${slug}` becomes `SESSION#${code}`
   - Also rename `sessionSlug` field in DynamoDB PutCommand/UpdateCommand Item objects to `sessionCode`

6. In `infra/resolvers/subscription-onSessionUpdate.js`:
   - If this file references `sessionSlug` in filter expressions, rename to `sessionCode`

**CRITICAL:** Run `grep -r "sessionSlug\|\"slug\"" packages/ infra/ --include="*.ts" --include="*.js" --include="*.graphql"` after changes to ensure ZERO remaining occurrences. The rename must be exhaustive.
</action>
<verify>
<automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && grep -rn "sessionSlug" packages/ infra/ --include="_.ts" --include="_.js" --include="_.graphql" --include="_.tsx" | grep -v node_modules | grep -v ".planning" | wc -l</automated>
<manual>Output should be 0 — no remaining sessionSlug references</manual>
</verify>
<done>Every occurrence of slug/sessionSlug across types, schemas, GraphQL, and Lambda resolvers is renamed to code/sessionCode. Zero remaining references.</done>
</task>

<task type="auto">
  <name>Task 2: Numeric code generation and slug removal</name>
  <files>packages/frontend/src/actions/session.ts, packages/frontend/src/lib/session.ts, packages/frontend/package.json</files>
  <action>
1. In `packages/frontend/src/actions/session.ts`:
   - Remove the `import("random-word-slugs")` dynamic import
   - Add a `generateNumericCode()` function: `function generateNumericCode(): string { return String(Math.floor(Math.random() * 900000) + 100000); }`
   - Replace `const slug = generateSlug(2, { format: "kebab" })` with `const code = generateNumericCode()`
   - Update the DynamoDB PutCommand Item: `PK: \`SESSION#\${code}\``, `SK: \`SESSION#\${code}\``, `code` (was `slug`), `sessionCode` field if present
   - Update the redirect URL from `/${locale}/session/${slug}/host?raw=${rawSecret}` to `/${locale}/${code}/host?raw=${rawSecret}`
   - Rename variable `slug` → `code` throughout the function

2. In `packages/frontend/src/lib/session.ts`:
   - Rename the `Session` interface field `slug: string` → `code: string`
   - Rename function parameter `slug` → `code` in both `getSession` and `getSessionData`
   - Update DynamoDB key construction: `SESSION#${code}`
   - Update the return object in `getSession`: `code: item.code as string` (was `slug: item.slug`)
   - Rename `sessionSlug` → `sessionCode` in all item mappings inside `getSessionData`

3. Run `npm uninstall random-word-slugs --workspace=packages/frontend` to remove the word-slug dependency.

4. After all changes, cascade the `slug` → `code` rename to ALL frontend files that import from `@/lib/session` or reference `session.slug` or `sessionSlug` prop:
   - `SessionLivePage`, `SessionLiveHostPage` — prop `sessionSlug` → `sessionCode`
   - All component files that receive `sessionSlug` as a prop
   - `host-toolbar.tsx` — update participantUrl if it references slug
   - Use `grep -rn "sessionSlug\|session\.slug\|\.slug" packages/frontend/src/ --include="*.tsx" --include="*.ts"` to find every occurrence
   - TypeScript will catch any remaining references via `npx tsc --noEmit`

**IMPORTANT:** This task MUST update every consumer. The TypeScript compiler is the safety net — run `tsc --noEmit` and fix every error until it passes clean.
</action>
<verify>
<automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx tsc --noEmit 2>&1 | head -30 && grep -rn "random-word-slugs" packages/frontend/package.json | wc -l</automated>
<manual>TypeScript compiles with zero errors. random-word-slugs is not in package.json. No remaining slug references in frontend code.</manual>
</verify>
<done>createSession generates 6-digit numeric codes. random-word-slugs removed. All frontend code uses `code`/`sessionCode` instead of `slug`/`sessionSlug`. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- `grep -rn "sessionSlug" packages/ infra/ --include="*.ts" --include="*.js" --include="*.graphql" --include="*.tsx" | grep -v node_modules | grep -v ".planning"` returns 0 results
- `grep -rn "random-word-slugs" packages/frontend/` returns 0 results
- `npx tsc --noEmit` passes
- `createSession` in actions/session.ts calls `generateNumericCode()` and produces codes in range 100000-999999
- DynamoDB keys use `SESSION#${code}` format
- GraphQL schema uses `sessionCode` everywhere
</verification>

<success_criteria>
Complete slug→code rename across the full stack. TypeScript compiles. No word-slug references remain. Numeric code generation works with collision retry. random-word-slugs dependency removed.
</success_criteria>

<output>
After completion, create `.planning/phases/14-infrastructure-and-url-routing/14-02-SUMMARY.md`
</output>
