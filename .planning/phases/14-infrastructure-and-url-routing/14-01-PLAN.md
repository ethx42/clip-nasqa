---
phase: 14-infrastructure-and-url-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sst.config.ts
  - packages/frontend/src/lib/session.ts
  - packages/frontend/src/components/session/qa-panel.tsx
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - UX-01
  - UX-02

must_haves:
  truths:
    - "Lambda resolver is configured with 256MB memory and arm64 architecture"
    - "SSR session page load issues at most 2 DynamoDB calls instead of 4"
    - "Session pages export force-dynamic to prevent cross-request caching"
    - "QA panel sort debounce is 300ms instead of 1000ms"
    - "Question cards animate smoothly to new positions when votes change"
    - "Users with prefers-reduced-motion see instant reorder without animation"
  artifacts:
    - path: "sst.config.ts"
      provides: "Lambda memory and architecture config"
      contains: "memory.*256 MB"
    - path: "packages/frontend/src/lib/session.ts"
      provides: "React.cache-wrapped getSession and getSessionData"
      contains: "cache("
    - path: "packages/frontend/src/components/session/qa-panel.tsx"
      provides: "300ms debounce and reduced-motion-aware layout animation"
      contains: "useReducedMotion"
  key_links:
    - from: "packages/frontend/src/lib/session.ts"
      to: "React.cache"
      via: "import { cache } from 'react'"
      pattern: "import.*cache.*from.*react"
---

<objective>
Configure Lambda for shorter cold starts, deduplicate SSR DynamoDB reads via React.cache, add force-dynamic export, reduce QA sort debounce to 300ms, and make layout animations respect prefers-reduced-motion.

Purpose: Measurable backend perf improvements (halved SSR reads, shorter cold starts) and smoother QA vote-driven reordering — all independent of the URL migration.
Output: Updated sst.config.ts, session.ts with React.cache, qa-panel.tsx with 300ms debounce and reduced-motion support.
</objective>

<execution_context>
@/Users/santiago.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/santiago.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-infrastructure-and-url-routing/14-RESEARCH.md
@packages/frontend/src/lib/session.ts
@packages/frontend/src/components/session/qa-panel.tsx
@sst.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lambda arm64 + 256MB and React.cache SSR deduplication</name>
  <files>sst.config.ts, packages/frontend/src/lib/session.ts, packages/frontend/src/app/[locale]/session/[slug]/page.tsx, packages/frontend/src/app/[locale]/session/[slug]/host/page.tsx</files>
  <action>
1. In `sst.config.ts`, add `memory: "256 MB"` and `architecture: "arm64"` to the `ResolverFn` constructor (the `sst.aws.Function("ResolverFn", {...})` block). Place them after `handler`.

2. In `packages/frontend/src/lib/session.ts`:
   - Add `import { cache } from "react";` at top
   - Wrap `getSession` with `cache()`: `export const getSession = cache(async function getSession(slug: string): Promise<Session | null> { ... });`
   - Wrap `getSessionData` with `cache()`: `export const getSessionData = cache(async function getSessionData(slug: string): Promise<SessionData> { ... });`
   - Keep all internal logic identical — only the outer wrapping changes

3. In both `packages/frontend/src/app/[locale]/session/[slug]/page.tsx` and `packages/frontend/src/app/[locale]/session/[slug]/host/page.tsx`:
   - Add `export const dynamic = "force-dynamic";` after the imports, before `generateMetadata` / the default export

Note: The `slug` → `code` rename happens in Plan 02. This plan keeps the existing naming.
</action>
<verify>
<automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx tsc --noEmit 2>&1 | head -20</automated>
<manual>Confirm sst.config.ts has memory and architecture props; session.ts uses cache() wrapping; both page files export force-dynamic</manual>
</verify>
<done>Lambda configured at 256MB arm64; getSession and getSessionData wrapped in React.cache; both session page files export force-dynamic</done>
</task>

<task type="auto">
  <name>Task 2: QA debounce 300ms with reduced-motion-aware layout animation</name>
  <files>packages/frontend/src/components/session/qa-panel.tsx</files>
  <action>
1. Change the debounce timeout from 1000ms to 300ms. Find `setTimeout(() => setDebouncedQuestions(questions), 1000)` and change `1000` to `300`.

2. Add `useReducedMotion` import from `framer-motion`:
   `import { AnimatePresence, motion, useReducedMotion } from "framer-motion";`
   (or add `useReducedMotion` to existing framer-motion import)

3. Inside the QAPanel component body, add:
   `const prefersReduced = useReducedMotion();`

4. Update the `motion.div` wrapping each question card:
   - Change `layout` prop to `layout={!prefersReduced}` (disables layout animation when reduced motion preferred)
   - Update the `transition` prop to use conditional durations:
     ```
     transition={{
       duration: prefersReduced ? 0 : 0.3,
       layout: { duration: prefersReduced ? 0 : 0.3, ease: [0.4, 0, 0.2, 1] },
     }}
     ```
   - If the current transition uses a different ease or duration (e.g., 400ms / 0.4), update to 300ms / 0.3 to match the debounce

5. Ensure `initial`, `animate`, and `exit` props also respect reduced motion — set `initial={prefersReduced ? false : { opacity: 0, y: -8 }}` for new question fade-in.
   </action>
   <verify>
   <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx tsc --noEmit 2>&1 | head -20</automated>
   <manual>Verify qa-panel.tsx has 300ms debounce, useReducedMotion hook, and conditional layout/transition props</manual>
   </verify>
   <done>QA sort debounce is 300ms; layout animation duration is 300ms with ease-out; prefers-reduced-motion disables all animation (instant reorder)</done>
   </task>

</tasks>

<verification>
- `sst.config.ts` contains `memory: "256 MB"` and `architecture: "arm64"` in ResolverFn
- `session.ts` imports `cache` from `react` and wraps both exported functions
- Both session page.tsx files export `dynamic = "force-dynamic"`
- `qa-panel.tsx` has 300ms debounce timeout
- `qa-panel.tsx` uses `useReducedMotion()` and conditionally applies layout animation
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
TypeScript compiles. Lambda config shows 256MB + arm64. React.cache wraps both DynamoDB fetch functions. force-dynamic exported from both session pages. QA debounce reduced to 300ms with smooth layout animation respecting prefers-reduced-motion.
</success_criteria>

<output>
After completion, create `.planning/phases/14-infrastructure-and-url-routing/14-01-SUMMARY.md`
</output>
