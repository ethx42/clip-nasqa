---
phase: 14-infrastructure-and-url-routing
plan: 03
type: execute
wave: 2
depends_on:
  - 14-02
files_modified:
  - packages/frontend/src/app/[locale]/[code]/page.tsx
  - packages/frontend/src/app/[locale]/[code]/host/page.tsx
  - packages/frontend/src/app/[locale]/[code]/error.tsx
  - packages/frontend/src/app/[locale]/[code]/opengraph-image.tsx
  - packages/frontend/src/app/robots.ts
  - packages/frontend/src/components/session/host-toolbar.tsx
autonomous: true
requirements:
  - URL-02
  - URL-03
  - URL-05
  - URL-08

must_haves:
  truths:
    - "Navigating to /en/482913 renders the participant session page"
    - "Navigating to /en/482913/host renders the host session page"
    - "The QR code in the host toolbar resolves to the flat URL format (/en/482913)"
    - "The OG image at /en/482913 displays the grouped code format (482 913)"
    - "The old /en/session/word-slug route no longer exists"
    - "Navigating to /en/notacode falls through to not-found"
    - "Tab title shows session name + grouped code (e.g., 'My Session — 482 913 | nasqa')"
  artifacts:
    - path: "packages/frontend/src/app/[locale]/[code]/page.tsx"
      provides: "Participant session page at flat URL"
      contains: "notFound"
    - path: "packages/frontend/src/app/[locale]/[code]/host/page.tsx"
      provides: "Host session page at flat URL"
      contains: "force-dynamic"
    - path: "packages/frontend/src/app/[locale]/[code]/opengraph-image.tsx"
      provides: "OG image with numeric code display"
      contains: "formatCode"
  key_links:
    - from: "packages/frontend/src/app/[locale]/[code]/page.tsx"
      to: "packages/frontend/src/lib/session.ts"
      via: "getSession(code) and getSessionData(code)"
      pattern: "getSession\\(code\\)"
    - from: "packages/frontend/src/components/session/host-toolbar.tsx"
      to: "QR code URL"
      via: "participantUrl prop"
      pattern: "participantUrl"
---

<objective>
Migrate session routes from `/[locale]/session/[slug]/` to `/[locale]/[code]/`, move all route files, update the OG image to display grouped numeric codes, update the host toolbar QR code URL, and delete the old session route directory.

Purpose: Flat URL structure makes sessions accessible via short numeric URLs — the core user-facing routing change of this phase.
Output: New `[code]` route directory with all session pages, updated OG image, deleted old `session/[slug]` directory.
</objective>

<execution_context>
@/Users/santiago.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/santiago.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-infrastructure-and-url-routing/14-RESEARCH.md
@.planning/phases/14-infrastructure-and-url-routing/14-CONTEXT.md
@.planning/phases/14-infrastructure-and-url-routing/14-02-SUMMARY.md
@packages/frontend/src/app/[locale]/session/[slug]/page.tsx
@packages/frontend/src/app/[locale]/session/[slug]/host/page.tsx
@packages/frontend/src/app/[locale]/session/[slug]/opengraph-image.tsx
@packages/frontend/src/app/[locale]/session/[slug]/error.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move session routes to [code] and add formatCode utility</name>
  <files>packages/frontend/src/app/[locale]/[code]/page.tsx, packages/frontend/src/app/[locale]/[code]/host/page.tsx, packages/frontend/src/app/[locale]/[code]/error.tsx, packages/frontend/src/lib/format-code.ts</files>
  <action>
1. Create a `formatCode` utility at `packages/frontend/src/lib/format-code.ts`:
   ```
   export function formatCode(code: string): string {
     return `${code.slice(0, 3)} ${code.slice(3)}`;
   }
   ```

2. Create directory `packages/frontend/src/app/[locale]/[code]/` and `packages/frontend/src/app/[locale]/[code]/host/`.

3. Move (copy then delete) `session/[slug]/page.tsx` → `[code]/page.tsx`:
   - Rename all `slug` params to `code` in the function signature (`params: Promise<{ locale: string; code: string }>`)
   - Add a 6-digit guard at the top of both `generateMetadata` and the default export: `if (!/^\d{6}$/.test(code)) { notFound(); }` — import `notFound` from `next/navigation`
   - Add `export const dynamic = "force-dynamic";` after imports (from Plan 01's INFRA-03 requirement)
   - Update the metadata title to include the grouped code: use `formatCode(code)` in the title template
   - Update the tab title format: `${session.title} — ${formatCode(code)} | nasqa`
   - Pass `sessionCode={code}` instead of `sessionSlug={slug}` to `<SessionLivePage>`
   - Keep the session-expired fallback UI unchanged

4. Move `session/[slug]/host/page.tsx` → `[code]/host/page.tsx`:
   - Same `slug` → `code` param rename
   - Same 6-digit guard with `notFound()`
   - Add `export const dynamic = "force-dynamic";`
   - Update `participantUrl` construction: change from `${baseUrl}/session/${slug}` to `${baseUrl}/${locale}/${code}` — read `locale` from params (already available)
   - Pass updated props with `code` naming

5. Move `session/[slug]/error.tsx` → `[code]/error.tsx` (no content changes needed, just the file move).

6. Delete the entire `packages/frontend/src/app/[locale]/session/` directory after all files are moved.
   </action>
   <verify>
   <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && test -f packages/frontend/src/app/\\[locale\\]/\\[code\\]/page.tsx && test -f packages/frontend/src/app/\\[locale\\]/\\[code\\]/host/page.tsx && test ! -d packages/frontend/src/app/\\[locale\\]/session && echo "PASS" || echo "FAIL"</automated>
   <manual>New [code] directory has page.tsx, host/page.tsx, error.tsx. Old session directory is gone.</manual>
   </verify>
   <done>Session routes live at /[locale]/[code]/ with 6-digit validation guard, force-dynamic export, and formatCode utility. Old session/[slug] directory deleted.</done>
   </task>

<task type="auto">
  <name>Task 2: OG image migration, QR code URL update, and robots.ts cleanup</name>
  <files>packages/frontend/src/app/[locale]/[code]/opengraph-image.tsx, packages/frontend/src/components/session/host-toolbar.tsx, packages/frontend/src/app/robots.ts</files>
  <action>
1. Move `session/[slug]/opengraph-image.tsx` → `[code]/opengraph-image.tsx`:
   - Rename `slug` param to `code`
   - Add the 6-digit guard: `if (!/^\d{6}$/.test(code)) { notFound(); }`
   - Update the QR code URL inside the OG image from `https://clip.nasqa.io/session/${slug}` to the flat format — use `https://clip.nasqa.io/${locale}/${code}` (read locale from params)
   - Update any displayed text that shows the slug to show `formatCode(code)` (grouped 3+3 format like "482 913")
   - Import `formatCode` from `@/lib/format-code`

2. In `packages/frontend/src/components/session/host-toolbar.tsx`:
   - Verify the `participantUrl` prop is already being passed correctly from the updated host page (Task 1 handles the page-level change)
   - If the component constructs the URL internally, update to use `/${locale}/${code}` format
   - Ensure the QR code generation uses the flat URL

3. In `packages/frontend/src/app/robots.ts`:
   - Remove any disallow rules referencing `/session/` paths (they no longer exist)
   - Session pages already have `robots: { index: false, follow: false }` in their generateMetadata, so no new disallow rules needed for `[code]` paths
   - If there are sitemap references that mention session URLs, remove them

4. Run `grep -rn "/session/" packages/frontend/src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v ".planning"` to find any remaining hardcoded `/session/` URL references and update them to the flat format.
   </action>
   <verify>
   <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx tsc --noEmit 2>&1 | head -20 && grep -rn "/session/" packages/frontend/src/ --include="_.ts" --include="_.tsx" | grep -v node_modules | grep -v ".planning" | wc -l</automated>
   <manual>TypeScript compiles. Zero remaining /session/ URL references. OG image file exists at [code]/opengraph-image.tsx.</manual>
   </verify>
   <done>OG image relocated and displays grouped code. QR code resolves to flat URL. robots.ts cleaned of stale session path rules. No /session/ references remain.</done>
   </task>

</tasks>

<verification>
- `packages/frontend/src/app/[locale]/[code]/page.tsx` exists and validates 6-digit code
- `packages/frontend/src/app/[locale]/[code]/host/page.tsx` exists with force-dynamic
- `packages/frontend/src/app/[locale]/[code]/opengraph-image.tsx` exists and shows grouped code
- `packages/frontend/src/app/[locale]/session/` directory does NOT exist
- `grep -rn "/session/" packages/frontend/src/` returns 0 matches (excluding .planning)
- `npx tsc --noEmit` passes
- QR code URL uses flat format
</verification>

<success_criteria>
All session routes live under `[code]`. Invalid non-6-digit paths return 404. OG image shows grouped code format. QR code resolves to flat URL. Old session route directory is deleted. TypeScript compiles clean.
</success_criteria>

<output>
After completion, create `.planning/phases/14-infrastructure-and-url-routing/14-03-SUMMARY.md`
</output>
