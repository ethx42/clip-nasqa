---
phase: 17-edit-and-delete
plan: 03
type: execute
wave: 3
depends_on: [17-02]
files_modified:
  - packages/frontend/src/components/session/question-card-participant.tsx
  - packages/frontend/src/components/session/question-card-host.tsx
  - packages/frontend/src/components/session/reply-list.tsx
  - packages/frontend/src/components/session/snippet-card.tsx
  - packages/frontend/src/components/session/session-live-page.tsx
  - packages/frontend/src/components/session/session-live-host-page.tsx
  - packages/frontend/messages/en.json
  - packages/frontend/messages/es.json
  - packages/frontend/messages/pt.json
autonomous: true
requirements: [EDIT-01, EDIT-02, EDIT-03, EDIT-04, EDIT-05, EDIT-06, EDIT-07, EDIT-09, EDIT-10]

must_haves:
  truths:
    - "Author sees edit/delete buttons on their own question within 5 minutes"
    - "Edit/delete buttons disappear after 5-minute window without page reload"
    - "Host sees edit/delete buttons on all questions, replies, and snippets always"
    - "Clicking edit replaces text with textarea for inline editing"
    - "Clicking delete shows a Dialog confirmation (never window.confirm)"
    - "Edited posts show '(edited)' badge with timestamp tooltip"
    - "Edited snippets show '(edited)' badge"
  artifacts:
    - path: "packages/frontend/src/components/session/question-card-participant.tsx"
      provides: "Inline edit UI + delete dialog for own questions within 5-min window"
      contains: "isEditing"
    - path: "packages/frontend/src/components/session/question-card-host.tsx"
      provides: "Host edit/delete UI for any question (no time limit)"
      contains: "handleHostEditQuestion"
    - path: "packages/frontend/src/components/session/reply-list.tsx"
      provides: "Inline edit + delete for own replies (5-min) and host superuser"
      contains: "isEditing"
    - path: "packages/frontend/src/components/session/snippet-card.tsx"
      provides: "Edited badge on confirmed snippets; host editSnippet for confirmed"
      contains: "editedAt"
  key_links:
    - from: "packages/frontend/src/components/session/question-card-participant.tsx"
      to: "packages/frontend/src/hooks/use-session-mutations.ts"
      via: "handleEditQuestion/handleDeleteQuestion props"
      pattern: "onEdit|onDelete"
    - from: "packages/frontend/src/components/session/question-card-host.tsx"
      to: "packages/frontend/src/hooks/use-host-mutations.ts"
      via: "handleHostEditQuestion/handleHostDeleteQuestion props"
      pattern: "onEdit|onDelete"
    - from: "packages/frontend/src/components/session/snippet-card.tsx"
      to: "packages/frontend/src/hooks/use-host-mutations.ts"
      via: "handleEditSnippet prop"
      pattern: "onEditSnippet"
---

<objective>
Build the frontend UI for edit and delete — inline editing on questions/replies/snippets, delete confirmation dialogs, 5-minute window visibility with auto-hide timer, edited indicator badges, and all i18n strings.

Purpose: This is the user-facing layer. After this plan, authors can edit/delete their content within 5 minutes, hosts can edit/delete anything, and all changes are visually indicated.
Output: Working inline edit UI, delete dialogs, edited badges, auto-hiding edit window, i18n in all 3 languages.
</objective>

<execution_context>
@/Users/santiago.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/santiago.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-edit-and-delete/17-RESEARCH.md
@.planning/phases/17-edit-and-delete/17-01-SUMMARY.md
@.planning/phases/17-edit-and-delete/17-02-SUMMARY.md

@packages/frontend/src/components/session/question-card-participant.tsx
@packages/frontend/src/components/session/question-card-host.tsx
@packages/frontend/src/components/session/reply-list.tsx
@packages/frontend/src/components/session/snippet-card.tsx
@packages/frontend/src/components/session/session-live-page.tsx
@packages/frontend/src/components/session/session-live-host-page.tsx
@packages/frontend/messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Question and reply inline edit/delete UI with 5-min window</name>
  <files>packages/frontend/src/components/session/question-card-participant.tsx, packages/frontend/src/components/session/question-card-host.tsx, packages/frontend/src/components/session/reply-list.tsx, packages/frontend/src/components/session/session-live-page.tsx, packages/frontend/src/components/session/session-live-host-page.tsx</files>
  <action>
**question-card-participant.tsx:**
- Add props: `onEdit?: (questionId: string, text: string) => void`, `onDelete?: (questionId: string) => void`
- Add 5-minute window logic: compute `withinEditWindow = Math.floor(Date.now()/1000) - question.createdAt < 300`, `isOwn = question.fingerprint === fingerprint`, `canEdit = isOwn && withinEditWindow`
- Add useEffect + setTimeout auto-hide timer per Pattern 4 in research: when `canEdit` is true, schedule a forced re-render via `useReducer(n => n+1, 0)` when the window expires. Clean up timeout on unmount.
- Add inline edit state: `const [isEditing, setIsEditing] = useState(false)`, `const [editText, setEditText] = useState(question.text)`
- When `isEditing`: render `<textarea>` replacing the question text, with `resize-none`, `maxLength={500}`, plus Save and Cancel buttons (Pencil icon for edit trigger, Check for save, X for cancel — from lucide-react)
- Save handler: call `onEdit(question.id, editText)`, set `isEditing = false`
- Cancel handler: reset `editText` to `question.text`, set `isEditing = false`
- Delete handler: show `@base-ui/react/dialog` Dialog with title from i18n `confirmDeleteQuestion` and description `confirmDeleteQuestionDesc`, confirm button calls `onDelete(question.id)`. Use the exact same Dialog pattern as the existing ban confirmation in question-card-host.tsx.
- Show edit/delete icon buttons only when `canEdit` is true. Buttons: 44px min touch target, `text-muted-foreground` color, `hover:text-foreground` transition.
- Show "(edited)" badge after relative time when `question.editedAt` is set — `text-[11px] text-muted-foreground/60` with `title={new Date(question.editedAt * 1000).toLocaleString()}` for hover timestamp.

**question-card-host.tsx:**

- Add props: `onEdit?: (questionId: string, text: string) => void`, `onDelete?: (questionId: string) => void`
- Host ALWAYS sees edit/delete buttons (no 5-minute restriction)
- Add same inline edit UI pattern as participant card
- Add same delete Dialog pattern
- Show "(edited)" badge when `question.editedAt` is set
- Edit/delete buttons sit alongside existing ban/focus/restore action buttons

**reply-list.tsx:**

- Add props: `onEditReply?: (replyId: string, text: string) => void`, `onDeleteReply?: (replyId: string) => void`, `isHost?: boolean`
- For each reply: compute `canEdit` same as questions (isOwn + withinEditWindow OR isHost)
- Add inline edit state per reply (track which reply is being edited by id: `const [editingReplyId, setEditingReplyId] = useState<string | null>(null)`)
- Inline edit textarea + Save/Cancel, same pattern
- Delete Dialog same pattern, using `confirmDeleteReply` / `confirmDeleteReplyDesc` i18n keys
- Show "(edited)" badge per reply when `reply.editedAt` is set

**session-live-page.tsx (participant orchestrator):**

- Thread `handleEditQuestion`, `handleDeleteQuestion` from `useSessionMutations` to QuestionCard components as `onEdit`/`onDelete` props
- Thread `handleEditReply`, `handleDeleteReply` to ReplyList via QAPanel or directly

**session-live-host-page.tsx (host orchestrator):**

- Thread `handleHostEditQuestion`, `handleHostDeleteQuestion` from `useHostMutations` to QuestionCardHost as `onEdit`/`onDelete` props
- Thread `handleHostEditReply`, `handleHostDeleteReply` to ReplyList
- Thread `handleEditSnippet` to SnippetCard (for Task 2)
  </action>
  <verify>
  <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx tsc --noEmit -p packages/frontend/tsconfig.json 2>&1 | head -30</automated>
  <manual>Inspect question-card-participant.tsx for isEditing state, 5-min window timer, delete Dialog, edited badge</manual>
  </verify>
  <done>Participant can edit/delete own questions and replies within 5 minutes with auto-hiding buttons; host can edit/delete any question or reply at any time; edited posts show "(edited)" badge; delete uses Dialog not window.confirm; all props threaded from orchestrators</done>
  </task>

<task type="auto">
  <name>Task 2: Snippet edited badge, confirmed snippet edit, and i18n strings</name>
  <files>packages/frontend/src/components/session/snippet-card.tsx, packages/frontend/messages/en.json, packages/frontend/messages/es.json, packages/frontend/messages/pt.json</files>
  <action>
**snippet-card.tsx:**
- Add "(edited)" badge when `snippet.editedAt` is set — same style as question/reply: `text-[11px] text-muted-foreground/60` with timestamp tooltip.
- Update edit button visibility: currently only optimistic (pre-push) snippets show the edit button. Change condition so that confirmed snippets also show edit when `isHost` is true. The edit button for confirmed snippets calls `onEditSnippet(snippetId, content, language)` prop. Keep existing optimistic-only edit behavior (SNIPPET_EDIT_START/END) for pre-push snippets — the two systems coexist per research "State of the Art" section.
- Add prop: `onEditSnippet?: (snippetId: string, content: string, language?: string) => void`
- For confirmed snippet edit: same inline textarea pattern, but also include a language dropdown (reuse the language select from host-input.tsx if it's extractable, or replicate the small select with SUPPORTED_LANGUAGES). On save, call `onEditSnippet` with new content and language.

**i18n — packages/frontend/messages/en.json:**
Add to "session" namespace:

- `"editQuestion": "Edit"`
- `"deleteQuestion": "Delete"`
- `"editReply": "Edit"`
- `"deleteReply": "Delete"`
- `"edited": "(edited)"`
- `"editWindowExpired": "The 5-minute edit window has expired."`
- `"confirmDeleteQuestion": "Delete this question?"`
- `"confirmDeleteQuestionDesc": "This cannot be undone."`
- `"confirmDeleteReply": "Delete this reply?"`
- `"confirmDeleteReplyDesc": "This cannot be undone."`
- `"saveEdit": "Save"`
- `"cancelEdit": "Cancel"`

Add to "actionErrors" namespace:

- `"editWindowExpired": "The 5-minute edit window has expired."`
- `"failedEditQuestion": "Failed to edit question. Please try again."`
- `"failedDeleteQuestion": "Failed to delete question."`
- `"failedEditReply": "Failed to edit reply."`
- `"failedDeleteReply": "Failed to delete reply."`
- `"failedEditSnippet": "Failed to edit snippet."`

**i18n — packages/frontend/messages/es.json:**
Spanish translations for all keys above:

- `"edited": "(editado)"`
- `"confirmDeleteQuestion": "¿Eliminar esta pregunta?"`
- `"confirmDeleteQuestionDesc": "Esta acción no se puede deshacer."`
- etc.

**i18n — packages/frontend/messages/pt.json:**
Portuguese translations for all keys above:

- `"edited": "(editado)"`
- `"confirmDeleteQuestion": "Excluir esta pergunta?"`
- `"confirmDeleteQuestionDesc": "Esta ação não pode ser desfeita."`
- etc.
  </action>
  <verify>
  <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx tsc --noEmit -p packages/frontend/tsconfig.json && node -e "const en=require('./packages/frontend/messages/en.json'); console.log('edited:', !!en.session?.edited, 'confirmDelete:', !!en.session?.confirmDeleteQuestion)" 2>&1 | head -10</automated>
  <manual>Check snippet-card.tsx has edited badge and confirmed-snippet edit button; verify es.json and pt.json have all new keys</manual>
  </verify>
  <done>Snippet cards show "(edited)" badge; host can edit confirmed snippets (content + language); all i18n strings present in en/es/pt; TypeScript compiles cleanly</done>
  </task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for frontend package
- No `window.confirm` or `window.alert` calls (CLAUDE.md rule)
- Delete dialogs use `@base-ui/react/dialog`
- Edited badge visible on questions, replies, and snippets when editedAt is set
- 5-minute window auto-hides buttons without page reload
- All i18n keys present in en.json, es.json, pt.json
- `next build` completes without errors
</verification>

<success_criteria>
Full edit/delete UX is operational: participants can edit/delete within 5 minutes, host can edit/delete anything, all changes show edited badges, deletes have confirmation dialogs, and the UI works in English, Spanish, and Portuguese.
</success_criteria>

<output>
After completion, create `.planning/phases/17-edit-and-delete/17-03-SUMMARY.md`
</output>
