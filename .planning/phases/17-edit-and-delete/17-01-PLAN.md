---
phase: 17-edit-and-delete
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/types.ts
  - infra/schema.graphql
  - sst.config.ts
  - packages/functions/src/resolvers/index.ts
  - packages/functions/src/resolvers/qa.ts
  - packages/functions/src/resolvers/clipboard.ts
autonomous: true
requirements: [EDIT-11, EDIT-12, EDIT-13]

must_haves:
  truths:
    - "DynamoDB items support editedAt, deletedAt, editedContent without migration"
    - "GraphQL schema accepts editQuestion, editReply, deleteQuestion, deleteReply, editSnippet mutations"
    - "Lambda enforces 5-minute author window for participants and host superuser bypass"
    - "Soft-deleted items are filtered out by getSessionData"
    - "New event types broadcast through existing onSessionUpdate subscription"
  artifacts:
    - path: "packages/core/src/types.ts"
      provides: "editedAt, deletedAt on Question/Reply/Snippet interfaces; new SessionEventType values"
      contains: "QUESTION_EDITED"
    - path: "infra/schema.graphql"
      provides: "5 new mutations, 5 new event types, editedAt on Question/Reply/Snippet types"
      contains: "editQuestion"
    - path: "packages/functions/src/resolvers/qa.ts"
      provides: "editQuestion, editReply, deleteQuestion, deleteReply resolver functions"
      exports: ["editQuestion", "editReply", "deleteQuestion", "deleteReply"]
    - path: "packages/functions/src/resolvers/clipboard.ts"
      provides: "editSnippet resolver function"
      exports: ["editSnippet"]
  key_links:
    - from: "sst.config.ts"
      to: "infra/schema.graphql"
      via: "api.addResolver for 5 new mutations"
      pattern: "addResolver.*edit|delete"
    - from: "infra/schema.graphql"
      to: "packages/functions/src/resolvers/index.ts"
      via: "Lambda handler switch statement"
      pattern: "case.*editQuestion|deleteQuestion|editReply|deleteReply|editSnippet"
    - from: "packages/functions/src/resolvers/index.ts"
      to: "packages/functions/src/resolvers/qa.ts"
      via: "import and dispatch"
      pattern: "import.*editQuestion.*from.*qa"
---

<objective>
Add the backend infrastructure for edit and delete — DynamoDB schema extension (types), GraphQL schema with 5 new mutations and 5 new event types, Lambda resolvers with fingerprint + 5-minute window enforcement and host superuser bypass, and soft-delete filtering in getSessionData.

Purpose: Establish the backend contract that frontend plans depend on. All mutations must be callable via AppSync before any UI work begins.
Output: Working GraphQL mutations that can be tested via AppSync console — editQuestion, editReply, deleteQuestion, deleteReply, editSnippet.
</objective>

<execution_context>
@/Users/santiago.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/santiago.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-edit-and-delete/17-RESEARCH.md

@packages/core/src/types.ts
@infra/schema.graphql
@sst.config.ts
@packages/functions/src/resolvers/index.ts
@packages/functions/src/resolvers/qa.ts
@packages/functions/src/resolvers/clipboard.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types, GraphQL schema, and SST config for edit/delete</name>
  <files>packages/core/src/types.ts, infra/schema.graphql, sst.config.ts</files>
  <action>
**packages/core/src/types.ts:**
- Add `editedAt?: number` and `deletedAt?: number` to `QuestionItem`, `ReplyItem`, and `SnippetItem` interfaces
- Add `editedAt?: number` to `Question`, `Reply`, and `Snippet` application-level interfaces (deletedAt not needed at app level — filtered server-side)
- Add `QUESTION_EDITED`, `QUESTION_DELETED`, `REPLY_EDITED`, `REPLY_DELETED`, `SNIPPET_EDITED` to the `SessionEventType` enum
- Verify `ReplyItem` has `authorName?: string` — if missing, add it (Pitfall 8 from research)

**infra/schema.graphql:**

- Add `editedAt: AWSTimestamp` field to `Question`, `Reply`, and `Snippet` types
- Add `QUESTION_EDITED`, `QUESTION_DELETED`, `REPLY_EDITED`, `REPLY_DELETED`, `SNIPPET_EDITED` to `SessionEventType` enum
- Add 5 new mutations exactly as specified in research Code Examples section:
  - `editQuestion(sessionCode!, questionId!, text!, fingerprint?, hostSecretHash?): SessionUpdate!`
  - `deleteQuestion(sessionCode!, questionId!, fingerprint?, hostSecretHash?): SessionUpdate!`
  - `editReply(sessionCode!, replyId!, text!, fingerprint?, hostSecretHash?): SessionUpdate!`
  - `deleteReply(sessionCode!, replyId!, fingerprint?, hostSecretHash?): SessionUpdate!`
  - `editSnippet(sessionCode!, snippetId!, content!, language?, hostSecretHash!): SessionUpdate!`
- Add the 5 new mutation names to the `@aws_subscribe(mutations: [...])` list on `onSessionUpdate`

**sst.config.ts:**

- Add 5 `api.addResolver` calls for the new mutations, same pattern as existing resolvers:
  - `"Mutation editQuestion"`, `"Mutation deleteQuestion"`, `"Mutation editReply"`, `"Mutation deleteReply"`, `"Mutation editSnippet"`
    </action>
    <verify>
    <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx tsc --noEmit -p packages/core/tsconfig.json 2>&1 | head -20</automated>
    <manual>Verify schema.graphql has 5 new mutations and 5 new event types</manual>
    </verify>
    <done>TypeScript types include editedAt/deletedAt, GraphQL schema has 5 mutations + 5 event types, SST config registers all 5 resolvers, @aws_subscribe list includes new mutations</done>
    </task>

<task type="auto">
  <name>Task 2: Lambda resolvers — edit/delete with auth enforcement and soft-delete filter</name>
  <files>packages/functions/src/resolvers/index.ts, packages/functions/src/resolvers/qa.ts, packages/functions/src/resolvers/clipboard.ts</files>
  <action>
**packages/functions/src/resolvers/index.ts:**
- Extract `verifyHostSecret` from qa.ts (or clipboard.ts) into a shared exported function in index.ts or a new `shared.ts` file in the resolvers directory. Both qa.ts and clipboard.ts currently have identical copies — extract once, import in both + new resolvers.
- Add 5 new cases to the Lambda handler switch statement: `"editQuestion"`, `"deleteQuestion"`, `"editReply"`, `"deleteReply"`, `"editSnippet"` — dispatch to qa.ts and clipboard.ts respectively.
- In `getSessionData`: extend the existing TTL filter to also exclude items with `deletedAt` set: `(items).filter(item => (!item.TTL || item.TTL > now) && !item.deletedAt)`.
- In `getSessionData`: add `editedAt: item.editedAt ?? null` to the question, reply, and snippet mapping objects so edited indicators survive page reload (Pitfall 5).

**packages/functions/src/resolvers/qa.ts:**

- Add `editQuestion(args)`: GetCommand to fetch item, verify ownership (fingerprint match OR verifyHostSecret), enforce 5-minute window for participant path (`Date.now()/1000 - item.createdAt <= 300`), UpdateCommand SET `#text = :text, editedAt = :editedAt` with ExpressionAttributeNames for `text` (reserved word — Pitfall 1), return SessionUpdate with `QUESTION_EDITED` event type and payload `{ questionId, text, editedAt }`.
- Add `deleteQuestion(args)`: Same auth pattern, UpdateCommand SET `deletedAt = :now`, return `QUESTION_DELETED` with payload `{ questionId }`.
- Add `editReply(args)`: Same pattern as editQuestion but target `REPLY#replyId` SK.
- Add `deleteReply(args)`: Same pattern as deleteQuestion but target `REPLY#replyId` SK.
- Enforce text length limit (500 chars) on edit mutations, matching addQuestion/addReply limits.
- Import `verifyHostSecret` from the shared location instead of local definition.

**packages/functions/src/resolvers/clipboard.ts:**

- Add `editSnippet(args)`: verifyHostSecret (always required — host-only), UpdateCommand SET content, language (if provided), editedAt. Return `SNIPPET_EDITED` with payload `{ snippetId, content, language, editedAt }`.
- Import `verifyHostSecret` from the shared location instead of local definition.
  </action>
  <verify>
  <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx tsc --noEmit -p packages/functions/tsconfig.json 2>&1 | head -20</automated>
  <manual>Check that index.ts handler dispatches all 5 new mutations and getSessionData filters deletedAt</manual>
  </verify>
  <done>All 5 resolver functions implemented with auth enforcement; verifyHostSecret extracted to shared location; getSessionData filters soft-deleted items and maps editedAt; TypeScript compiles cleanly</done>
  </task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for both core and functions packages
- GraphQL schema has exactly 5 new mutations and 5 new event types
- @aws_subscribe list includes all 5 new mutation names
- sst.config.ts has 5 new addResolver calls
- getSessionData filters `deletedAt` items and maps `editedAt`
- verifyHostSecret is defined in one shared location, imported by qa.ts and clipboard.ts
</verification>

<success_criteria>
Backend is ready for frontend integration — all 5 mutations are schema-defined, resolver-implemented, and SST-registered. Soft-deleted items are filtered from SSR data. Edited timestamps are returned in session data.
</success_criteria>

<output>
After completion, create `.planning/phases/17-edit-and-delete/17-01-SUMMARY.md`
</output>
