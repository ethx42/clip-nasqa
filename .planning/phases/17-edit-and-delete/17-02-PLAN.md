---
phase: 17-edit-and-delete
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - packages/frontend/src/lib/graphql/mutations.ts
  - packages/frontend/src/lib/appsync-client.ts
  - packages/frontend/src/hooks/use-session-state.ts
  - packages/frontend/src/hooks/use-session-updates.ts
  - packages/frontend/src/hooks/use-session-mutations.ts
  - packages/frontend/src/hooks/use-host-mutations.ts
  - packages/frontend/src/actions/moderation.ts
  - packages/frontend/src/actions/snippet.ts
autonomous: true
requirements: [EDIT-01, EDIT-02, EDIT-03, EDIT-04, EDIT-05, EDIT-06, EDIT-08, EDIT-09]

must_haves:
  truths:
    - "Participant edit/delete mutations call AppSync directly via graphqlMutation (no Server Action hop)"
    - "Host edit/delete mutations go through Server Actions (hostSecretHash never in browser Network tab)"
    - "Reducer correctly updates state for all 5 new event types"
    - "Subscription handler dispatches new event types to reducer"
    - "Optimistic dispatch for edits — text updates before server confirms"
  artifacts:
    - path: "packages/frontend/src/lib/graphql/mutations.ts"
      provides: "EDIT_QUESTION, DELETE_QUESTION, EDIT_REPLY, DELETE_REPLY, EDIT_SNIPPET mutation strings"
      contains: "EDIT_QUESTION"
    - path: "packages/frontend/src/hooks/use-session-state.ts"
      provides: "QUESTION_EDITED, QUESTION_DELETED, REPLY_EDITED, REPLY_DELETED, SNIPPET_EDITED reducer actions"
      contains: "QUESTION_EDITED"
    - path: "packages/frontend/src/hooks/use-session-mutations.ts"
      provides: "handleEditQuestion, handleDeleteQuestion, handleEditReply, handleDeleteReply for participants"
      contains: "handleEditQuestion"
    - path: "packages/frontend/src/hooks/use-host-mutations.ts"
      provides: "handleHostEditQuestion, handleHostDeleteQuestion, handleHostEditReply, handleHostDeleteReply, handleEditSnippet for host"
      contains: "handleHostEditQuestion"
  key_links:
    - from: "packages/frontend/src/hooks/use-session-mutations.ts"
      to: "packages/frontend/src/lib/appsync-client.ts"
      via: "graphqlMutation for participant edit/delete"
      pattern: "graphqlMutation.*editQuestion"
    - from: "packages/frontend/src/hooks/use-host-mutations.ts"
      to: "packages/frontend/src/actions/moderation.ts"
      via: "Server Action for host edit/delete"
      pattern: "editQuestionAction|deleteQuestionAction"
    - from: "packages/frontend/src/hooks/use-session-updates.ts"
      to: "packages/frontend/src/hooks/use-session-state.ts"
      via: "dispatch new event types from subscription"
      pattern: "dispatch.*QUESTION_EDITED|QUESTION_DELETED"
---

<objective>
Wire the frontend state layer for edit and delete — GraphQL mutation strings, reducer actions for 5 new event types, subscription handler for real-time broadcast, participant mutation hooks (client-side via graphqlMutation), and host mutation hooks (via Server Actions).

Purpose: This is the plumbing layer. After this plan, the UI components in Plan 03 can call `handleEditQuestion()`, `handleDeleteQuestion()`, etc. and state will update correctly both optimistically and from subscription echo.
Output: All mutation hooks callable from components; reducer handles all new event types; subscription dispatches new events.
</objective>

<execution_context>
@/Users/santiago.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/santiago.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-edit-and-delete/17-RESEARCH.md
@.planning/phases/17-edit-and-delete/17-01-SUMMARY.md

@packages/frontend/src/lib/graphql/mutations.ts
@packages/frontend/src/lib/appsync-client.ts
@packages/frontend/src/hooks/use-session-state.ts
@packages/frontend/src/hooks/use-session-updates.ts
@packages/frontend/src/hooks/use-session-mutations.ts
@packages/frontend/src/hooks/use-host-mutations.ts
@packages/frontend/src/actions/moderation.ts
@packages/frontend/src/actions/snippet.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: GraphQL mutations, ParticipantMutationName, and reducer actions</name>
  <files>packages/frontend/src/lib/graphql/mutations.ts, packages/frontend/src/lib/appsync-client.ts, packages/frontend/src/hooks/use-session-state.ts, packages/frontend/src/hooks/use-session-updates.ts</files>
  <action>
**packages/frontend/src/lib/graphql/mutations.ts:**
- Add 5 mutation strings: `EDIT_QUESTION`, `DELETE_QUESTION`, `EDIT_REPLY`, `DELETE_REPLY`, `EDIT_SNIPPET`
- Each follows the exact pattern from research Code Examples — returns `{ eventType, sessionCode, payload }`
- editSnippet includes `content: String!`, `language: String` (optional), `hostSecretHash: String!`

**packages/frontend/src/lib/appsync-client.ts:**

- Add `"editQuestion" | "editReply" | "deleteQuestion" | "deleteReply"` to the `ParticipantMutationName` union type (Pitfall 7 from research)
- Do NOT add `editSnippet` — that's host-only via Server Action

**packages/frontend/src/hooks/use-session-state.ts:**

- Add 5 new action types to the `SessionAction` union:
  - `{ type: "QUESTION_EDITED"; payload: { questionId: string; text: string; editedAt: number } }`
  - `{ type: "QUESTION_DELETED"; payload: { questionId: string } }`
  - `{ type: "REPLY_EDITED"; payload: { replyId: string; text: string; editedAt: number } }`
  - `{ type: "REPLY_DELETED"; payload: { replyId: string } }`
  - `{ type: "SNIPPET_EDITED"; payload: { snippetId: string; content: string; language?: string; editedAt: number } }`
- Add reducer cases:
  - `QUESTION_EDITED`: map over questions, update text and editedAt for matching id
  - `QUESTION_DELETED`: filter out the question by id
  - `REPLY_EDITED`: map over questions, for each question map over replies, update matching reply's text and editedAt
  - `REPLY_DELETED`: map over questions, for each question filter out matching reply by id
  - `SNIPPET_EDITED`: map over snippets, update content, language (if provided), and editedAt for matching id

**packages/frontend/src/hooks/use-session-updates.ts:**

- Add cases to the subscription event handler switch for: `QUESTION_EDITED`, `QUESTION_DELETED`, `REPLY_EDITED`, `REPLY_DELETED`, `SNIPPET_EDITED`
- Parse payload JSON and dispatch corresponding actions to reducer
- Follow exact same pattern as existing event type handlers (e.g., QUESTION_UPDATED, SNIPPET_ADDED)
  </action>
  <verify>
  <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx tsc --noEmit -p packages/frontend/tsconfig.json 2>&1 | head -30</automated>
  <manual>Verify 5 new mutation strings exported, 5 new reducer cases added, subscription handler dispatches all 5</manual>
  </verify>
  <done>GraphQL mutation strings exported, ParticipantMutationName includes edit/delete, reducer handles all 5 event types, subscription handler dispatches them</done>
  </task>

<task type="auto">
  <name>Task 2: Participant and host mutation hooks + Server Actions</name>
  <files>packages/frontend/src/hooks/use-session-mutations.ts, packages/frontend/src/hooks/use-host-mutations.ts, packages/frontend/src/actions/moderation.ts, packages/frontend/src/actions/snippet.ts</files>
  <action>
**packages/frontend/src/hooks/use-session-mutations.ts:**
- Add `handleEditQuestion(questionId: string, text: string)`: optimistic dispatch `QUESTION_EDITED` with `{ questionId, text, editedAt: Math.floor(Date.now()/1000) }`, then call `safeClientMutation` with `graphqlMutation("editQuestion", { sessionCode, questionId, text, fingerprint })`. On failure, rollback by dispatching `QUESTION_EDITED` with original text.
- Add `handleDeleteQuestion(questionId: string)`: optimistic dispatch `QUESTION_DELETED`, then `graphqlMutation("deleteQuestion", { sessionCode, questionId, fingerprint })`. On failure, rollback by re-adding question (save original question before dispatch using questionsRef pattern).
- Add `handleEditReply(replyId: string, text: string)`: same pattern as editQuestion but `REPLY_EDITED` + `graphqlMutation("editReply", ...)`.
- Add `handleDeleteReply(replyId: string)`: same pattern as deleteQuestion but `REPLY_DELETED` + `graphqlMutation("deleteReply", ...)`.
- Return all 4 new handlers from the hook.

**packages/frontend/src/hooks/use-host-mutations.ts:**

- Add `handleHostEditQuestion(questionId: string, text: string)`: optimistic dispatch `QUESTION_EDITED`, call `editQuestionAction(sessionCode, questionId, text, hostSecretHash)` Server Action. Rollback on failure.
- Add `handleHostDeleteQuestion(questionId: string)`: optimistic dispatch `QUESTION_DELETED`, call `deleteQuestionAction(...)` Server Action. Rollback on failure.
- Add `handleHostEditReply(replyId: string, text: string)`: same pattern, `editReplyAction`.
- Add `handleHostDeleteReply(replyId: string)`: same pattern, `deleteReplyAction`.
- Add `handleEditSnippet(snippetId: string, content: string, language?: string)`: optimistic dispatch `SNIPPET_EDITED`, call `editSnippetAction(...)` Server Action. Rollback on failure.
- Return all 5 new handlers from the hook.

**packages/frontend/src/actions/moderation.ts:**

- Add 4 new Server Actions using `"use server"` and `appsyncMutation`:
  - `editQuestionAction(sessionCode, questionId, text, hostSecretHash)` — calls editQuestion mutation
  - `deleteQuestionAction(sessionCode, questionId, hostSecretHash)` — calls deleteQuestion mutation
  - `editReplyAction(sessionCode, replyId, text, hostSecretHash)` — calls editReply mutation
  - `deleteReplyAction(sessionCode, replyId, hostSecretHash)` — calls deleteReply mutation
- Follow existing pattern in moderation.ts: lazy `getTranslations` (error paths only per Phase 15-02 decision), return `ActionResult`.

**packages/frontend/src/actions/snippet.ts:**

- Add `editSnippetAction(sessionCode, snippetId, content, language, hostSecretHash)` — calls editSnippet mutation, returns `ActionResult`.
- Follow existing pattern: lazy `getTranslations` in error path only.
  </action>
  <verify>
  <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx tsc --noEmit -p packages/frontend/tsconfig.json 2>&1 | head -30</automated>
  <manual>Verify use-session-mutations exports 4 new handlers, use-host-mutations exports 5 new handlers, moderation.ts has 4 new actions, snippet.ts has 1 new action</manual>
  </verify>
  <done>Participant hooks call AppSync directly (no Server Action), host hooks call Server Actions (hostSecretHash stays server-side), all handlers do optimistic dispatch with rollback, all Server Actions follow lazy i18n pattern</done>
  </task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for frontend package
- Participant mutations use `graphqlMutation` (not Server Actions)
- Host mutations use Server Actions (not direct AppSync)
- All 5 reducer cases handle their respective event types correctly
- Subscription handler processes all 5 new event types
</verification>

<success_criteria>
All mutation hooks are callable from components. State updates optimistically and from subscription. The transport split (client vs Server Action) respects the security boundary established in Phase 15.
</success_criteria>

<output>
After completion, create `.planning/phases/17-edit-and-delete/17-02-SUMMARY.md`
</output>
