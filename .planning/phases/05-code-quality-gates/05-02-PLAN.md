---
phase: 05-code-quality-gates
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - packages/frontend/src/**/*.ts
  - packages/frontend/src/**/*.tsx
  - packages/core/src/**/*.ts
  - packages/functions/src/**/*.ts
  - "*.json"
  - "*.md"
autonomous: true
requirements:
  - QUAL-03

must_haves:
  truths:
    - "All four TypeScript compile surfaces (root, core, frontend, functions) pass tsc --noEmit with strict mode"
    - "Every source file in the repo is consistently formatted by Prettier"
    - "A test commit with a lint error is rejected by the pre-commit hook"
    - "A test commit with unformatted code is auto-formatted by the pre-commit hook before completing"
  artifacts:
    - path: "packages/core/tsconfig.json"
      provides: "Core package TypeScript config extending root strict"
      contains: "extends"
    - path: "packages/functions/tsconfig.json"
      provides: "Functions package TypeScript config extending root strict"
      contains: "extends"
    - path: "packages/frontend/tsconfig.json"
      provides: "Frontend TypeScript config with strict mode"
      contains: "strict"
  key_links:
    - from: "tsconfig.json"
      to: "packages/*/tsconfig.json"
      via: "extends field in each package tsconfig"
      pattern: "extends.*\\.\\./"
    - from: ".husky/pre-commit"
      to: "staged files"
      via: "lint-staged runs eslint + prettier on commit"
      pattern: "lint-staged"
---

<objective>
Apply initial Prettier formatting across the entire codebase, verify TypeScript strict mode passes in all packages, and confirm the pre-commit hook works end-to-end.

Purpose: After this plan, every file is consistently formatted, TypeScript strict is verified, and the commit hook is proven functional.
Output: Formatted codebase, verified strict TypeScript, working quality gate.
</objective>

<execution_context>
@/Users/santiago.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/santiago.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-code-quality-gates/05-RESEARCH.md
@.planning/phases/05-code-quality-gates/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify TypeScript strict mode and fix any type errors</name>
  <files>
    packages/core/tsconfig.json
    packages/functions/tsconfig.json
    packages/frontend/tsconfig.json
    tsconfig.json
  </files>
  <action>
Step 1 — Verify root tsconfig.json has `"strict": true`. Confirm each package extends root or has its own `"strict": true`.

Step 2 — Run typecheck across all compile surfaces:

```bash
# Core + Functions
npm run typecheck

# Frontend (regenerate .next types first to clear stale validator references per Pitfall 3)
rm -rf packages/frontend/.next/types
npx tsc --noEmit -p packages/frontend/tsconfig.json

# Root (sst.config.ts)
npx tsc --noEmit
```

Step 3 — If any TypeScript errors appear:

- `.next/types/validator.ts` errors referencing deleted debug pages: delete `.next/types/` and re-run. If errors persist, the debug page routes were deleted but their type entries remain — `next build` or `next dev` will regenerate correctly. For the typecheck to pass now without building, add the deleted paths to tsconfig exclude or fix the references.
- Genuine strict mode errors: fix them (likely `noImplicitAny` or `strictNullChecks` violations).

Step 4 — Confirm all three pass cleanly with exit code 0.
</action>
<verify>
<automated>cd /Users/santiago.torres/codebases/personal/nasqa-clip && npm run typecheck && npx tsc --noEmit -p packages/frontend/tsconfig.json && npx tsc --noEmit</automated>
</verify>
<done>All four TypeScript compile surfaces (root, core, frontend, functions) pass `tsc --noEmit` under strict mode with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Apply initial Prettier formatting and verify pre-commit hook</name>
  <files>
    packages/frontend/src/**/*.ts
    packages/frontend/src/**/*.tsx
    packages/core/src/**/*.ts
    packages/functions/src/**/*.ts
  </files>
  <action>
Step 1 — Run Prettier across the entire codebase to establish consistent formatting:
```bash
npx prettier --write "packages/**/*.{ts,tsx}" "packages/**/*.{json,css}" "*.json"
```
This will reformat all source files according to .prettierrc.json (including import sorting via @ianvs/prettier-plugin-sort-imports).

Step 2 — Verify Prettier reports all files as formatted:

```bash
npx prettier --check "packages/**/*.{ts,tsx}"
```

Must exit 0 (all files match).

Step 3 — Re-run ESLint after formatting to confirm no conflicts:

```bash
cd packages/frontend && npx eslint --config eslint.config.mjs src/ --max-warnings 0
```

Step 4 — Re-run typecheck after formatting to confirm import reordering did not break anything:

```bash
npm run typecheck && npx tsc --noEmit -p packages/frontend/tsconfig.json
```

Step 5 — Commit the formatting changes as a dedicated commit: `style(05-02): apply initial Prettier formatting`

Step 6 — Verify the pre-commit hook works end-to-end:

- Create a temporary test file with intentional formatting issues
- Stage it
- Attempt `git commit` — lint-staged should auto-format
- Clean up the test file

Note: The commit in Step 5 will itself trigger the pre-commit hook, which serves as a real-world validation. If the commit succeeds, the hook is working.
</action>
<verify>
<automated>cd /Users/santiago.torres/codebases/personal/nasqa-clip && npx prettier --check "packages/\*_/_.{ts,tsx}" && cd packages/frontend && npx eslint --config eslint.config.mjs src/ --max-warnings 0</automated>
</verify>
<done>Every source file is Prettier-formatted with sorted imports. ESLint passes with zero errors/warnings post-formatting. TypeScript compiles cleanly. The pre-commit hook has been exercised via a real commit and confirmed functional.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p packages/frontend/tsconfig.json` exits 0
2. `npm run typecheck` exits 0
3. `npx prettier --check "packages/**/*.{ts,tsx}"` exits 0
4. `cd packages/frontend && npx eslint --config eslint.config.mjs src/ --max-warnings 0` exits 0
5. Pre-commit hook triggers on git commit (verified by successful commit through the hook)
</verification>

<success_criteria>

- TypeScript strict mode verified across all 4 compile surfaces (root, core, frontend, functions)
- All source files pass Prettier --check with zero inconsistencies
- ESLint and Prettier do not conflict (no back-and-forth formatting)
- Pre-commit hook proven functional via a real commit
- No regressions — lint, typecheck, and format all pass simultaneously
  </success_criteria>

<output>
After completion, create `.planning/phases/05-code-quality-gates/05-02-SUMMARY.md`
</output>
