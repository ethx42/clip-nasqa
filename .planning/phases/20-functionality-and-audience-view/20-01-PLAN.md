---
phase: 20-functionality-and-audience-view
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/schema.graphql
  - sst.config.ts
  - packages/core/src/types.ts
  - packages/functions/src/resolvers/qa.ts
  - packages/functions/src/resolvers/clipboard.ts
  - packages/functions/src/resolvers/index.ts
  - packages/frontend/src/lib/linkify.tsx
  - packages/frontend/src/lib/render-markdown.tsx
  - packages/frontend/src/actions/moderation.ts
  - packages/frontend/src/hooks/use-host-mutations.ts
  - packages/frontend/src/components/session/question-card-host.tsx
  - packages/frontend/src/components/session/question-card-participant.tsx
  - packages/frontend/src/components/session/reply-list.tsx
  - packages/frontend/src/hooks/use-session-state.ts
autonomous: true
requirements:
  - FUNC-01
  - FUNC-02

must_haves:
  truths:
    - "Host deletes a question via hard delete and the item no longer exists in DynamoDB after page reload"
    - "Hard deleting a question also removes all its replies from DynamoDB"
    - "Hard delete works on questions, replies, and snippets"
    - "Participant types multi-line text with Shift+Enter and line breaks render in the feed"
    - "Bold, italic, inline code, and links render correctly in Q&A text"
  artifacts:
    - path: "packages/functions/src/resolvers/qa.ts"
      provides: "hardDeleteQuestion and hardDeleteReply resolvers using DeleteCommand"
      contains: "DeleteCommand"
    - path: "packages/functions/src/resolvers/clipboard.ts"
      provides: "hardDeleteSnippet resolver using DeleteCommand"
      contains: "DeleteCommand"
    - path: "infra/schema.graphql"
      provides: "hardDeleteQuestion, hardDeleteReply, hardDeleteSnippet mutations"
      contains: "hardDeleteQuestion"
    - path: "packages/frontend/src/lib/render-markdown.tsx"
      provides: "renderMarkdown function for basic inline markdown"
      exports: ["renderMarkdown"]
    - path: "packages/frontend/src/actions/moderation.ts"
      provides: "hardDeleteQuestionAction, hardDeleteReplyAction, hardDeleteSnippetAction Server Actions"
      contains: "hardDeleteQuestionAction"
  key_links:
    - from: "packages/frontend/src/hooks/use-host-mutations.ts"
      to: "packages/frontend/src/actions/moderation.ts"
      via: "hardDeleteQuestionAction import and call"
      pattern: "hardDeleteQuestionAction"
    - from: "packages/frontend/src/components/session/question-card-host.tsx"
      to: "packages/frontend/src/lib/render-markdown.tsx"
      via: "renderMarkdown replacing linkifyText"
      pattern: "renderMarkdown"
    - from: "packages/frontend/src/components/session/question-card-participant.tsx"
      to: "packages/frontend/src/lib/render-markdown.tsx"
      via: "renderMarkdown replacing linkifyText"
      pattern: "renderMarkdown"
---

<objective>
Implement superuser hard delete (permanent DB removal) for all content types and basic markdown rendering in Q&A text.

Purpose: Host can permanently remove content from DynamoDB (not just soft-delete), and Q&A text supports line breaks, bold, italic, inline code, and links for richer communication.
Output: Backend hard delete mutations, frontend wiring, and a lightweight markdown renderer used in all Q&A text display.
</objective>

<execution_context>
@/Users/santiago.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/santiago.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/functions/src/resolvers/qa.ts
@packages/functions/src/resolvers/clipboard.ts
@packages/functions/src/resolvers/shared.ts
@infra/schema.graphql
@sst.config.ts
@packages/core/src/types.ts
@packages/frontend/src/actions/moderation.ts
@packages/frontend/src/hooks/use-host-mutations.ts
@packages/frontend/src/hooks/use-session-state.ts
@packages/frontend/src/components/session/question-card-host.tsx
@packages/frontend/src/components/session/question-card-participant.tsx
@packages/frontend/src/components/session/reply-list.tsx
@packages/frontend/src/lib/linkify.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend hard delete mutations for questions, replies, and snippets</name>
  <files>
    infra/schema.graphql
    sst.config.ts
    packages/core/src/types.ts
    packages/functions/src/resolvers/qa.ts
    packages/functions/src/resolvers/clipboard.ts
    packages/functions/src/resolvers/index.ts
  </files>
  <action>
    Add three new GraphQL mutations to schema.graphql:
    - `hardDeleteQuestion(sessionCode: String!, questionId: String!, hostSecretHash: String!): SessionUpdate!`
    - `hardDeleteReply(sessionCode: String!, replyId: String!, hostSecretHash: String!): SessionUpdate!`
    - `hardDeleteSnippet(sessionCode: String!, snippetId: String!, hostSecretHash: String!): SessionUpdate!`

    Note: these are HOST-ONLY (require hostSecretHash, no fingerprint path). No 5-minute window — host can hard delete at any time.

    Add the three mutation names to the `@aws_subscribe(mutations: [...])` list in the Subscription type.

    In packages/core/src/types.ts:
    - Add `QUESTION_HARD_DELETED`, `REPLY_HARD_DELETED`, `SNIPPET_HARD_DELETED` to SessionEventType enum
    - Add `HardDeleteQuestionArgs`, `HardDeleteReplyArgs`, `HardDeleteSnippetArgs` interfaces (sessionCode, id, hostSecretHash)

    In packages/functions/src/resolvers/qa.ts:
    - Add `hardDeleteQuestion` resolver: verifyHostSecret, then use `DeleteCommand` (not UpdateCommand) to remove `PK: SESSION#${sessionCode}, SK: QUESTION#${questionId}`. ALSO query and batch-delete all replies for that question (SK begins_with `REPLY#` AND has `questionId` attribute matching). Return event with type `QUESTION_HARD_DELETED` and payload `{ questionId }`.
    - Add `hardDeleteReply` resolver: verifyHostSecret, then `DeleteCommand` to remove `PK: SESSION#${sessionCode}, SK: REPLY#${replyId}`. Return `REPLY_HARD_DELETED` with `{ replyId }`.

    In packages/functions/src/resolvers/clipboard.ts:
    - Add `hardDeleteSnippet` resolver: verifyHostSecret, then `DeleteCommand` to remove `PK: SESSION#${sessionCode}, SK: SNIPPET#${snippetId}`. Return `SNIPPET_HARD_DELETED` with `{ snippetId }`.

    In packages/functions/src/resolvers/index.ts:
    - Export the three new handlers.

    In sst.config.ts:
    - Register the three new resolver functions mapping GraphQL mutations to Lambda handlers, following the existing pattern.

    Import `DeleteCommand` from `@aws-sdk/lib-dynamodb` in qa.ts and clipboard.ts (alongside existing UpdateCommand, GetCommand, etc.).

    For the cascade delete in hardDeleteQuestion: query all items where PK = `SESSION#${sessionCode}` and SK begins_with `REPLY#`, filter by `questionId` attribute equals the target questionId, then batch delete with `BatchWriteCommand` (chunks of 25). If no replies exist, skip the batch write.

  </action>
  <verify>
    <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx tsc --noEmit -p packages/functions/tsconfig.json && grep -c "hardDelete" infra/schema.graphql | grep -q "3"</automated>
    <manual>Verify schema.graphql has three hardDelete mutations and they are in the subscribe list</manual>
  </verify>
  <done>Three hardDelete mutations exist in GraphQL schema, Lambda resolvers use DeleteCommand for permanent removal, cascade deletes replies when hard-deleting a question, all types are defined, SST config registers all three handlers</done>
</task>

<task type="auto">
  <name>Task 2: Frontend hard delete wiring and basic markdown rendering in Q&A</name>
  <files>
    packages/frontend/src/lib/render-markdown.tsx
    packages/frontend/src/lib/linkify.tsx
    packages/frontend/src/actions/moderation.ts
    packages/frontend/src/hooks/use-host-mutations.ts
    packages/frontend/src/hooks/use-session-state.ts
    packages/frontend/src/components/session/question-card-host.tsx
    packages/frontend/src/components/session/question-card-participant.tsx
    packages/frontend/src/components/session/reply-list.tsx
  </files>
  <action>
    **Markdown renderer (packages/frontend/src/lib/render-markdown.tsx):**
    Create a lightweight `renderMarkdown(text: string): React.ReactNode` function that handles — in this priority order:
    1. Split text on `\n` to create line breaks (`<br />` between lines)
    2. `**bold**` → `<strong>`
    3. `*italic*` or `_italic_` → `<em>`
    4. `` `inline code` `` → `<code>` with appropriate styling (bg-muted rounded px-1 font-mono text-[0.9em])
    5. URLs (reuse the existing URL regex from linkify.tsx) → `<a>` tags with same styling

    Do NOT use a library — this is intentionally hand-rolled for minimal bundle impact (per user decision: "lightweight markdown parser, no full AST"). Process patterns with simple regex replacements, generating React elements with unique keys. The function returns `React.ReactNode`.

    Keep linkify.tsx as-is (still used in shiki-block.tsx) but update all Q&A text rendering to use renderMarkdown instead.

    **Server Actions (packages/frontend/src/actions/moderation.ts):**
    Add three new Server Actions following the existing pattern (e.g., deleteQuestionAction):
    - `hardDeleteQuestionAction({ sessionCode, questionId, hostSecretHash })` — calls `HARD_DELETE_QUESTION` mutation string
    - `hardDeleteReplyAction({ sessionCode, replyId, hostSecretHash })` — calls `HARD_DELETE_REPLY` mutation string
    - `hardDeleteSnippetAction({ sessionCode, snippetId, hostSecretHash })` — calls `HARD_DELETE_SNIPPET` mutation string

    Add the GraphQL mutation strings as constants at the top of the file (same pattern as existing DELETE_QUESTION etc.):
    ```
    const HARD_DELETE_QUESTION = `mutation HardDeleteQuestion($sessionCode: String!, $questionId: String!, $hostSecretHash: String!) { hardDeleteQuestion(sessionCode: $sessionCode, questionId: $questionId, hostSecretHash: $hostSecretHash) { eventType sessionCode payload } }`;
    ```

    **Host mutations hook (packages/frontend/src/hooks/use-host-mutations.ts):**
    Add `handleHardDeleteQuestion`, `handleHardDeleteReply`, `handleHardDeleteSnippet` to the hook:
    - Each does optimistic dispatch (reuse existing QUESTION_DELETED, REPLY_DELETED, SNIPPET_DELETED actions — the UI effect is the same: item disappears)
    - For handleHardDeleteQuestion: also optimistically remove all replies with matching questionId
    - Call the corresponding Server Action
    - On failure: rollback by restoring the removed items
    - Export the three new handlers from the hook

    **Session state reducer (packages/frontend/src/hooks/use-session-state.ts):**
    Add handler for `QUESTION_HARD_DELETED`, `REPLY_HARD_DELETED`, `SNIPPET_HARD_DELETED` event types in the subscription handler. These should behave identically to their soft-delete counterparts (remove from state). For QUESTION_HARD_DELETED, also remove all replies with that questionId.

    **Host question card (question-card-host.tsx):**
    - In the overflow popover menu (alongside Focus, Ban), add a "Permanently Delete" option with destructive styling. This calls a new `onHardDelete` prop.
    - The existing delete button (Trash2 icon) continues to call the existing soft-delete (onDelete prop).
    - Add a confirmation dialog for hard delete (reuse existing dialog pattern from the soft-delete confirmation). The dialog text should warn that this is permanent and cannot be undone.
    - Replace `linkifyText(question.text)` with `renderMarkdown(question.text)` in the text display.
    - Thread the `onHardDelete` prop from QuestionCardBaseProps.

    **Participant question card (question-card-participant.tsx):**
    - Replace `linkifyText(question.text)` with `renderMarkdown(question.text)` in the text display. No other changes needed — participants cannot hard delete.

    **Reply list (reply-list.tsx):**
    - Replace `linkifyText(reply.text)` with `renderMarkdown(reply.text)` in the reply text display.
    - For host view: add `onHardDeleteReply` prop, wire a "Permanently Delete" option in the reply overflow/action bar (host only), with confirmation dialog.

    **Prop threading:**
    - Add `onHardDelete?: (questionId: string) => void` to QuestionCardBaseProps in question-card-shared.tsx
    - Add `onHardDeleteReply?: (replyId: string) => void` to ReplyRow props in reply-list.tsx
    - Thread from session-live-host-page.tsx where useHostMutations is called

    Add i18n keys for hard delete in en.json, es.json, pt.json:
    - `session.hardDelete`: "Permanently Delete" / "Eliminar permanentemente" / "Excluir permanentemente"
    - `session.hardDeleteConfirm`: "This will permanently remove this content and cannot be undone." / equivalent translations
    - `session.hardDeleteQuestion`: "Permanently delete question" / equivalent
    - `session.hardDeleteReply`: "Permanently delete reply" / equivalent

  </action>
  <verify>
    <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx tsc --noEmit -p packages/frontend/tsconfig.json && grep -c "renderMarkdown" packages/frontend/src/lib/render-markdown.tsx | grep -q "1"</automated>
    <manual>Verify hard delete appears in overflow menu on host question cards; verify multi-line text with bold/italic renders correctly</manual>
  </verify>
  <done>Host can hard delete questions (with cascade), replies, and snippets via overflow menu with confirmation dialog; Q&A text renders line breaks, bold, italic, inline code, and links via renderMarkdown; all TypeScript compiles</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for both packages/functions and packages/frontend
- `infra/schema.graphql` contains hardDeleteQuestion, hardDeleteReply, hardDeleteSnippet mutations
- `packages/frontend/src/lib/render-markdown.tsx` exports renderMarkdown
- Question text with `\n`, `**bold**`, `*italic*`, `` `code` ``, and URLs renders correctly
- Hard delete uses DeleteCommand (not UpdateCommand with deletedAt)
</verification>

<success_criteria>

- Host can hard delete a question and it is permanently removed from DynamoDB (not soft-deleted)
- Hard deleting a question cascades to remove all its replies
- Hard delete is host-only — no participant path, no time window
- Q&A text preserves line breaks and supports basic markdown formatting
- Existing soft-delete still works unchanged
  </success_criteria>

<output>
After completion, create `.planning/phases/20-functionality-and-audience-view/20-01-SUMMARY.md`
</output>
