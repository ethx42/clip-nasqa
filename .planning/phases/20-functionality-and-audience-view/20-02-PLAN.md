---
phase: 20-functionality-and-audience-view
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/components/session/host-toolbar.tsx
  - packages/frontend/src/components/session/session-shell.tsx
  - packages/frontend/src/components/session/session-live-page.tsx
  - packages/frontend/src/components/session/session-live-host-page.tsx
autonomous: true
requirements:
  - FUNC-03
  - AUD-01
  - AUD-02

must_haves:
  truths:
    - "Share dialog shows the 6-digit session code as a large copyable chip above the QR code"
    - "Audience view displays a 'Clipboard' heading above the clipboard list"
    - "Audience view displays a 'Q&A' heading above the question feed"
    - "Section headers are visible without any interaction"
    - "Participant can collapse the Q&A panel and clipboard expands to fill the space"
    - "Collapsed Q&A shows a count badge in the header"
  artifacts:
    - path: "packages/frontend/src/components/session/host-toolbar.tsx"
      provides: "Session code chip in share popover"
      contains: "formatCode"
    - path: "packages/frontend/src/components/session/session-shell.tsx"
      provides: "Section headers and collapsible Q&A drawer"
      contains: "Collapsible"
  key_links:
    - from: "packages/frontend/src/components/session/host-toolbar.tsx"
      to: "@/lib/format-code"
      via: "formatCode import for session code display"
      pattern: "formatCode"
    - from: "packages/frontend/src/components/session/session-shell.tsx"
      to: "section headers"
      via: "h2 headings for Clipboard and Q&A sections"
      pattern: "Clipboard|Q&A"
---

<objective>
Add session code to share dialog, section headers in audience view, and collapsible Q&A drawer for participants.

Purpose: Share dialog becomes voice-friendly with a prominent session code, audience view has clear section delineation, and participants can collapse Q&A to prioritize clipboard content.
Output: Updated share popover, section headers in SessionShell, and collapsible Q&A with chevron toggle and count badge.
</objective>

<execution_context>
@/Users/santiago.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/santiago.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/frontend/src/components/session/host-toolbar.tsx
@packages/frontend/src/components/session/session-shell.tsx
@packages/frontend/src/components/session/session-live-page.tsx
@packages/frontend/src/components/session/session-live-host-page.tsx
@packages/frontend/src/lib/format-code.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Session code in share dialog and section headers in audience view</name>
  <files>
    packages/frontend/src/components/session/host-toolbar.tsx
    packages/frontend/src/components/session/session-shell.tsx
  </files>
  <action>
    **Share dialog session code (host-toolbar.tsx):**
    Add `sessionCode: string` to HostToolbarProps. In the share Popover.Popup, add a session code chip ABOVE the QR code:
    - Large mono-spaced display: `font-mono text-3xl font-bold tracking-[0.2em] text-foreground text-center`
    - Format with `formatCode()` from `@/lib/format-code` (already formats as "482 913" with space)
    - Wrap in a clickable container that copies the code to clipboard on click
    - Include a small copy icon (Copy from lucide-react) to the right of the digits
    - Use the CopyButton pattern but styled as a chip: `bg-muted/50 rounded-xl px-4 py-3 flex items-center justify-center gap-3 cursor-pointer hover:bg-muted transition-colors`
    - On click: copy the raw 6-digit code (no spaces) to clipboard, show a brief "Copied!" toast or visual feedback (reuse existing copy pattern)
    - Add a small label above: `text-xs font-semibold uppercase tracking-widest text-muted-foreground` saying "Session Code" (use i18n key `session.sessionCode`)

    Thread `sessionCode` from session-live-host-page.tsx into HostToolbar.

    **Section headers (session-shell.tsx):**
    In the DESKTOP two-column layout, add headings above each section:
    - Before clipboardSlot: `<h2 className="mb-3 text-xs font-semibold uppercase tracking-widest text-muted-foreground">{t("clipboard")}</h2>`
    - Before qaSlot: `<h2 className="mb-3 text-xs font-semibold uppercase tracking-widest text-muted-foreground">{t("qa")}</h2>`

    These headings use the existing `session.clipboard` and `session.qa` i18n keys. They are always visible (no interaction needed). On mobile, the tab bar already shows the section name so no duplicate heading is needed.

    Add i18n keys:
    - `session.sessionCode`: "Session Code" / "Codigo de sesion" / "Codigo da sessao"

  </action>
  <verify>
    <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx tsc --noEmit -p packages/frontend/tsconfig.json && grep -q "sessionCode" packages/frontend/src/components/session/host-toolbar.tsx && grep -q "h2" packages/frontend/src/components/session/session-shell.tsx</automated>
    <manual>Share popover shows 6-digit code with copy button above QR; desktop layout shows Clipboard and Q&A headings</manual>
  </verify>
  <done>Share dialog prominently displays session code as a large copyable chip above QR code; desktop audience view shows "Clipboard" and "Q&A" section headers above their respective panels</done>
</task>

<task type="auto">
  <name>Task 2: Collapsible Q&A drawer in participant view</name>
  <files>
    packages/frontend/src/components/session/session-shell.tsx
    packages/frontend/src/components/session/session-live-page.tsx
    packages/frontend/src/components/session/session-live-host-page.tsx
  </files>
  <action>
    **Collapsible Q&A in desktop layout (session-shell.tsx):**
    In the desktop two-column layout, make the Q&A section collapsible:
    - Add `const [qaCollapsed, setQaCollapsed] = useState(false)` state
    - Replace the Q&A section header with a clickable toggle: the h2 heading (from Task 1) gets a ChevronDown/ChevronUp icon and becomes a button
    - Header layout: `<button onClick={toggle} className="mb-3 flex items-center gap-2 text-xs font-semibold uppercase tracking-widest text-muted-foreground hover:text-foreground transition-colors">`
      - Text: `{t("qa")}`
      - When collapsed: show count badge `({questionCount})` using `text-xs tabular-nums` next to the heading text
      - ChevronRight when collapsed, ChevronDown when expanded (lucide-react icons, h-3.5 w-3.5)
    - When collapsed: hide the qaSlot content. The clipboard column expands to fill the full width: change grid from `lg:grid-cols-2` to `lg:grid-cols-1` when collapsed.
    - Layout when collapsed: clipboard takes full width, Q&A header sits below it as a single-line bar that can be clicked to expand again
    - Animate the collapse with `transition-all duration-200` on the container

    Actually, better approach for desktop: use a flex layout with explicit widths:
    - Expanded: clipboard section `flex-1` and Q&A section `flex-1` (50/50 split, same as current grid-cols-2)
    - Collapsed: clipboard section `flex-1` and Q&A section `w-auto` (just the header line)
    - The Q&A column when collapsed shows only the heading bar with the toggle chevron and count badge, no content below
    - Use `overflow-hidden` and `transition-[flex]` for smooth animation

    The Clipboard heading is NOT collapsible — clipboard is always visible. Only Q&A is collapsible.

    **Host view consideration:** The host page (session-live-host-page.tsx) also uses SessionShell. The host should NOT have a collapsible Q&A — they always need full access. Add an `allowCollapseQA?: boolean` prop to SessionShell, default false. Set it to true only in session-live-page.tsx (participant view). When false, the Q&A heading is a plain h2 (not a button), no collapse behavior.

    **Mobile behavior:** On mobile, the tab bar already handles switching between clipboard and Q&A — no collapsible behavior needed on mobile. The collapse state only applies to the desktop `lg:` breakpoint two-column layout.

    Add i18n keys:
    - `session.questionsCount`: "{count} questions" / "{count} preguntas" / "{count} perguntas" (for collapsed badge)
    - Or simpler: just show the number in parentheses, no extra i18n needed since it is just `({questionCount})`

  </action>
  <verify>
    <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx tsc --noEmit -p packages/frontend/tsconfig.json && grep -q "qaCollapsed\|allowCollapseQA" packages/frontend/src/components/session/session-shell.tsx</automated>
    <manual>On desktop participant view, clicking the Q&A header collapses the panel; clipboard fills the space; count badge shows; clicking again expands. Host view Q&A is not collapsible.</manual>
  </verify>
  <done>Q&A section on desktop participant view is collapsible via header click; collapsed state shows count badge; clipboard expands to fill vacated space; host view is not collapsible; mobile is unaffected</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for packages/frontend
- Share popover shows session code chip with copy functionality above QR code
- Desktop layout has "Clipboard" and "Q&A" section headings
- Participant desktop view can collapse Q&A and clipboard fills the space
- Host desktop view cannot collapse Q&A
- Mobile layout is unchanged (tab bar, no section headers)
</verification>

<success_criteria>

- Share dialog prominently displays the 6-digit session code as a copyable chip
- Desktop audience view shows "Clipboard" and "Q&A" section headers
- Participant can collapse Q&A drawer; clipboard expands to fill space
- Collapsed Q&A shows count badge
- Host view Q&A is always expanded
  </success_criteria>

<output>
After completion, create `.planning/phases/20-functionality-and-audience-view/20-02-SUMMARY.md`
</output>
