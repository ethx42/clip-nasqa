---
phase: 12-component-decomposition
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/components/session/snippet-card.tsx
  - packages/frontend/src/components/session/clipboard-panel.tsx
  - packages/frontend/src/components/session/snippet-hero.tsx
autonomous: true
requirements: [STRUC-03]

must_haves:
  truths:
    - "SnippetCard can be imported and rendered in a Vitest component test without pulling in ClipboardPanel"
    - "SnippetCard supports both hero and compact variants via a variant prop"
    - "ClipboardPanel renders snippets by importing SnippetCard from its own module"
    - "Delete confirmation stays in ClipboardPanel — SnippetCard only fires onDelete callback"
  artifacts:
    - path: "packages/frontend/src/components/session/snippet-card.tsx"
      provides: "Standalone SnippetCard client component with hero/compact variants"
      exports: ["SnippetCard"]
      min_lines: 50
  key_links:
    - from: "packages/frontend/src/components/session/clipboard-panel.tsx"
      to: "packages/frontend/src/components/session/snippet-card.tsx"
      via: "import { SnippetCard }"
      pattern: "from.*[\"']\\./snippet-card[\"']"
---

<objective>
Consolidate the two SnippetCard implementations into one standalone client component file.

Purpose: The richer inline SnippetCard inside clipboard-panel.tsx and the simpler server component snippet-card.tsx are merged into a single client component that supports both hero and compact variants. This makes SnippetCard independently importable and testable.
Output: One `snippet-card.tsx` exporting a self-contained `SnippetCard` component; `clipboard-panel.tsx` imports it; `snippet-hero.tsx` deleted or redirected.
</objective>

<execution_context>
@/Users/santiago.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/santiago.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/frontend/src/components/session/clipboard-panel.tsx
@packages/frontend/src/components/session/snippet-card.tsx
@packages/frontend/src/components/session/snippet-hero.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract standalone SnippetCard from clipboard-panel.tsx</name>
  <files>
    packages/frontend/src/components/session/snippet-card.tsx
    packages/frontend/src/components/session/clipboard-panel.tsx
  </files>
  <action>
Per user decision: consolidate by extracting the richer inline SnippetCard from clipboard-panel.tsx into snippet-card.tsx, replacing the simpler server component version.

**Rewrite `snippet-card.tsx`** as a client component (`"use client"`) containing:

- The full SnippetCard function currently defined inline in clipboard-panel.tsx (lines 36-134)
- Props interface: `{ snippet: SnippetWithHtml; variant: "hero" | "compact"; isHost: boolean; onDelete?: () => void; }`
- Export the `SnippetWithHtml` interface (extends Snippet with optional `highlightedHtml`)
- Keep all existing behavior: lazy `renderHighlight` on mount/expand, expand/collapse for long content, CopyButton, delete button (fires onDelete callback — does NOT own dialog)
- `"use client"` directive at top (uses useState, useEffect)

**Update `clipboard-panel.tsx`:**

- Remove the inline SnippetCard function definition (lines 36-134) and the SnippetWithHtml interface (lines 19-21)
- Add `import { SnippetCard, type SnippetWithHtml } from "./snippet-card";`
- Keep all ClipboardPanel logic unchanged — the Dialog, scroll handling, HostInput, etc. stay in ClipboardPanel
- Remove imports that were only used by the inline SnippetCard if they are no longer needed in ClipboardPanel (check: `renderHighlight` import likely moves to snippet-card.tsx only)

**Verify no behavior change:** The ClipboardPanel should render identically — same props passed to SnippetCard, same delete confirmation flow (ClipboardPanel owns the Dialog, SnippetCard fires onDelete).
</action>
<verify>
<automated>cd packages/frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
</verify>
<done>SnippetCard is exported from its own file, ClipboardPanel imports it, no inline SnippetCard definition remains in clipboard-panel.tsx, TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Remove snippet-hero.tsx and update any imports</name>
  <files>
    packages/frontend/src/components/session/snippet-hero.tsx
  </files>
  <action>
Per user decision: the simpler server component snippet-hero.tsx is superseded by the consolidated SnippetCard (which handles the hero variant via `variant="hero"`).

1. Search for any imports of `snippet-hero` across the codebase:
   `grep -r "snippet-hero" packages/frontend/src/`

   If any file imports SnippetHero, replace the import with `import { SnippetCard } from "./snippet-card"` and update the JSX to use `<SnippetCard snippet={...} variant="hero" isHost={false} />` (mapping old props to new interface — note: `snippetNumber` prop from SnippetHero is not used in the consolidated SnippetCard, and the old SnippetHero was only used in SSR context which no longer applies since ClipboardPanel handles all rendering).

2. Delete `snippet-hero.tsx`.

3. If snippet-hero.tsx is not imported anywhere (likely — the ClipboardPanel inline version replaced it), simply delete the file.

4. Run TypeScript check to confirm no broken imports.
   </action>
   <verify>
   <automated>cd packages/frontend && npx tsc --noEmit --pretty 2>&1 | head -20 && test ! -f src/components/session/snippet-hero.tsx && echo "snippet-hero.tsx deleted"</automated>
   </verify>
   <done>snippet-hero.tsx is deleted, no broken imports, SnippetCard is the single source of truth for snippet rendering</done>
   </task>

</tasks>

<verification>
- `grep -r "SnippetCard" packages/frontend/src/components/session/clipboard-panel.tsx` shows import (not inline definition)
- `grep -c "function SnippetCard" packages/frontend/src/components/session/clipboard-panel.tsx` returns 0
- `ls packages/frontend/src/components/session/snippet-hero.tsx` fails (file deleted)
- All frontend tests pass: `cd packages/frontend && npx vitest run`
</verification>

<success_criteria>

- SnippetCard is a standalone export from snippet-card.tsx with hero/compact variants
- ClipboardPanel imports SnippetCard — no inline definition
- snippet-hero.tsx is deleted
- Delete confirmation dialog remains in ClipboardPanel (SnippetCard fires onDelete only)
- TypeScript compiles with zero new errors
  </success_criteria>

<output>
After completion, create `.planning/phases/12-component-decomposition/12-02-SUMMARY.md`
</output>
