---
phase: 12-component-decomposition
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/lib/sort-questions.ts
  - packages/frontend/src/__tests__/sort-questions.test.ts
  - packages/frontend/src/components/session/qa-panel.tsx
autonomous: true
requirements: [STRUC-04]

must_haves:
  truths:
    - "Questions always appear in the same order regardless of which consumer renders them"
    - "Focused questions always appear first, then by upvotes descending, then by recency descending"
    - "Sort logic is importable and testable without any React dependency"
  artifacts:
    - path: "packages/frontend/src/lib/sort-questions.ts"
      provides: "Pure sortQuestions function"
      exports: ["sortQuestions"]
    - path: "packages/frontend/src/__tests__/sort-questions.test.ts"
      provides: "Unit tests for sort utility"
      min_lines: 40
  key_links:
    - from: "packages/frontend/src/components/session/qa-panel.tsx"
      to: "packages/frontend/src/lib/sort-questions.ts"
      via: "import { sortQuestions }"
      pattern: "sortQuestions"
---

<objective>
Extract the Q&A sort logic from QAPanel into a pure, tested utility function.

Purpose: Eliminate the risk of sort order divergence between consumers by making sort logic exist in exactly one place — a pure function with no React dependency that can be tested with plain arrays.
Output: `sort-questions.ts` utility with TDD coverage, QAPanel refactored to consume it.
</objective>

<execution_context>
@/Users/santiago.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/santiago.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/frontend/src/components/session/qa-panel.tsx
@packages/core/src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD — Create sortQuestions pure utility</name>
  <files>
    packages/frontend/src/lib/sort-questions.ts
    packages/frontend/src/__tests__/sort-questions.test.ts
  </files>
  <action>
RED phase — Create test file first:

Create `packages/frontend/src/__tests__/sort-questions.test.ts` with tests covering:

1. Focused question sorts first regardless of vote count
2. Higher upvoteCount sorts before lower upvoteCount (when neither focused)
3. Equal upvoteCount breaks tie by createdAt descending (newer first)
4. Multiple focused questions maintain relative vote/recency order among themselves
5. Empty array returns empty array
6. Single item returns single item

Use plain `Question` objects from `@nasqa/core` — no React, no mocking needed. Import `sortQuestions` from `@/lib/sort-questions`.

Run tests — they MUST fail (module does not exist yet).

GREEN phase — Create the utility:

Create `packages/frontend/src/lib/sort-questions.ts`:

- Export a single `sortQuestions(questions: Question[]): Question[]` function
- Sort order: focused first, then upvoteCount descending, then createdAt descending (matches current QAPanel inline logic exactly)
- Returns a new sorted array (does not mutate input)
- No React imports, no hooks, no side effects — pure function only
- Debounce logic stays OUT of this utility (that is QAPanel's concern)

Run tests — they MUST pass.
</action>
<verify>
<automated>cd packages/frontend && npx vitest run src/**tests**/sort-questions.test.ts</automated>
</verify>
<done>sortQuestions passes all 6+ test cases, is a pure function with zero React dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline sort logic in QAPanel with sortQuestions import</name>
  <files>packages/frontend/src/components/session/qa-panel.tsx</files>
  <action>
In `qa-panel.tsx`:

1. Add `import { sortQuestions } from "@/lib/sort-questions";`
2. Replace the `sortedQuestions` useMemo body (lines ~80-95) with a call to `sortQuestions(questions)` — but keep the debounced ordering approach:
   - The useMemo still takes `[questions, debouncedQuestions]` as deps
   - Build the `orderMap` from `debouncedQuestions` as before
   - Pass `questions` through `sortQuestions()` but the orderMap trick needs to remain for debounce behavior

   Actually, looking at the existing code more carefully: the debounce uses `debouncedQuestions` for the ORDER but renders the LATEST `questions` for content. The sortQuestions utility should accept an optional `orderSource` parameter OR the QAPanel should map questions with debounced order values before calling sortQuestions.

   Simplest approach per user decision ("debounce stays in consumer"):
   - `sortQuestions` sorts by the fields on the Question objects it receives
   - QAPanel creates synthetic question objects with debounced upvoteCount/isFocused values merged onto latest questions, then passes to `sortQuestions`
   - This keeps the utility pure and the debounce concern in QAPanel

3. Remove the inline sort comparator from the useMemo — replace with `sortQuestions(questionsWithDebouncedOrder)`
4. Verify no duplicate sort logic remains in qa-panel.tsx

Do NOT touch the debounce timer, the debouncedQuestions state, or any other QAPanel logic.
</action>
<verify>
<automated>cd packages/frontend && npx vitest run src/**tests**/sort-questions.test.ts && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
</verify>
<done>QAPanel imports sortQuestions, zero inline sort comparators remain, TypeScript compiles, sort behavior unchanged</done>
</task>

</tasks>

<verification>
- `grep -r "\.sort(" packages/frontend/src/components/session/qa-panel.tsx` returns 0 matches (no inline sort)
- `grep "sortQuestions" packages/frontend/src/components/session/qa-panel.tsx` returns 1+ matches (import + usage)
- All frontend tests pass: `cd packages/frontend && npx vitest run`
</verification>

<success_criteria>

- sortQuestions is a pure function in lib/sort-questions.ts with 6+ passing unit tests
- QAPanel delegates all sorting to sortQuestions — no inline comparator
- Debounce logic remains in QAPanel, not in the utility
- Existing sort order (focused first, upvotes desc, recency desc) is preserved
  </success_criteria>

<output>
After completion, create `.planning/phases/12-component-decomposition/12-01-SUMMARY.md`
</output>
