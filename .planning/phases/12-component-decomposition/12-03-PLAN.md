---
phase: 12-component-decomposition
plan: "03"
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - packages/frontend/src/components/session/question-card.tsx
  - packages/frontend/src/components/session/question-card-host.tsx
  - packages/frontend/src/components/session/question-card-participant.tsx
  - packages/frontend/src/components/session/question-card-shared.tsx
  - packages/frontend/src/components/session/qa-panel.tsx
  - packages/frontend/src/__tests__/question-card.test.tsx
autonomous: true
requirements: [STRUC-05]

must_haves:
  truths:
    - "QuestionCard renders a host variant with moderation controls when used from the host page"
    - "QuestionCard renders a participant variant without moderation controls when used from the participant page"
    - "Both variants share the same card layout shell, vote column, content body, and reply system"
    - "QAPanel selects the correct variant internally based on its isHost prop"
    - "State transitions between normal, hidden, and banned animate correctly"
  artifacts:
    - path: "packages/frontend/src/components/session/question-card-host.tsx"
      provides: "Host variant with moderation toolbar and ban dialog"
      exports: ["QuestionCardHost"]
      min_lines: 40
    - path: "packages/frontend/src/components/session/question-card-participant.tsx"
      provides: "Participant variant without moderation controls"
      exports: ["QuestionCardParticipant"]
      min_lines: 30
    - path: "packages/frontend/src/components/session/question-card-shared.tsx"
      provides: "Shared base components and utilities for both variants"
      exports: ["formatDisplayName", "VoteColumn", "QuestionContent", "ReplySection"]
      min_lines: 60
    - path: "packages/frontend/src/components/session/question-card.tsx"
      provides: "Re-export facade that selects variant based on isHost"
      exports: ["QuestionCard"]
  key_links:
    - from: "packages/frontend/src/components/session/question-card-host.tsx"
      to: "packages/frontend/src/components/session/question-card-shared.tsx"
      via: "import shared components"
      pattern: "from.*question-card-shared"
    - from: "packages/frontend/src/components/session/question-card-participant.tsx"
      to: "packages/frontend/src/components/session/question-card-shared.tsx"
      via: "import shared components"
      pattern: "from.*question-card-shared"
    - from: "packages/frontend/src/components/session/qa-panel.tsx"
      to: "packages/frontend/src/components/session/question-card.tsx"
      via: "import { QuestionCard } — facade routes to correct variant"
      pattern: "from.*question-card"
---

<objective>
Split QuestionCard into host and participant variants with a shared base, and wire state transition animations.

Purpose: The monolithic QuestionCard (430 lines) mixes participant-only rendering with host-only moderation controls. Splitting into variants makes each independently importable, reduces bundle size for participants (no moderation UI), and enables distinct animation behaviors per state transition.
Output: QuestionCardHost, QuestionCardParticipant, shared base module, facade QuestionCard, updated QAPanel with AnimatePresence state transitions.
</objective>

<execution_context>
@/Users/santiago.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/santiago.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-component-decomposition/12-01-SUMMARY.md
@packages/frontend/src/components/session/question-card.tsx
@packages/frontend/src/components/session/qa-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared base module and split QuestionCard variants</name>
  <files>
    packages/frontend/src/components/session/question-card-shared.tsx
    packages/frontend/src/components/session/question-card-host.tsx
    packages/frontend/src/components/session/question-card-participant.tsx
    packages/frontend/src/components/session/question-card.tsx
    packages/frontend/src/__tests__/question-card.test.tsx
  </files>
  <action>
Per user decision: two separate files with composition-based shared base (imported components, not inheritance).

**Create `question-card-shared.tsx`** — shared pieces both variants import:

- `"use client"` directive
- `formatDisplayName` function (currently in question-card.tsx lines 37-43)
- `VoteColumn` component: the upvote/downvote button column (lines 180-220 of current file). Props: `{ question, isVoted, isDownvoted, onUpvoteClick, onDownvoteClick }`
- `QuestionContent` component: author row (avatar + name + time) + question body text + action row (reply count/button) + ReactionBar. Props: `{ question, replies, isHost, fingerprint, isOwn, showHiddenContent, onReply, onToggleReplies, showReplies, showReplyInput, replyText, onReplyTextChange, onReplySubmit, onReplyKeyDown, onToggleReplyInput, replyCharLimit, replyCounterThreshold, tSession, tIdentity, tCommon, t }`. This is the largest shared piece — the entire content column minus the host moderation icons.

  Alternatively (simpler and per user's "composition" hint): keep QuestionContent as a render block rather than a separate component. Extract only the truly reusable pieces:
  - `formatDisplayName` (pure function)
  - `VoteColumn` (self-contained component)
  - `BannedTombstone` component: the banned state return (lines 128-134)
  - `HiddenCollapsed` component: the community-hidden state return (lines 137-159), takes `{ question, isHost, showContent, onShowContent, onRestore, t }`
  - Shared types: `QuestionCardBaseProps` interface with the common props both variants need
  - `REPLY_CHAR_LIMIT` and `REPLY_COUNTER_THRESHOLD` constants

**Create `question-card-participant.tsx`** (`"use client"`):

- Import shared pieces from `./question-card-shared`
- `QuestionCardParticipant` component with props matching current QuestionCard minus host-only callbacks (`onFocus`, `onBanQuestion`, `onBanParticipant`, `onRestore` are optional/absent)
- Renders: BannedTombstone (if banned), HiddenCollapsed (if hidden), or the normal card layout with VoteColumn + content column
- NO moderation toolbar, NO ban confirmation dialog, NO focus toggle
- The hidden state's restore button is NOT shown (participant can only toggle show/hide of hidden content, not restore)

**Create `question-card-host.tsx`** (`"use client"`):

- Import shared pieces from `./question-card-shared`
- `QuestionCardHost` component with full props including `onFocus`, `onBanQuestion`, `onBanParticipant`, `onRestore`
- Renders same layout as participant PLUS:
  - Host moderation toolbar in the author row (focus toggle, ban question, ban participant buttons) — lines 268-299 of current file
  - Ban confirmation dialog (Dialog from @base-ui/react — lines 401-429 of current file), self-contained per user decision
  - Restore button visible in HiddenCollapsed state

**Rewrite `question-card.tsx`** as a thin facade:

- `"use client"` directive
- Import `QuestionCardHost` and `QuestionCardParticipant`
- Export `QuestionCard` that receives the full props (same interface as current) and renders `isHost ? <QuestionCardHost ...> : <QuestionCardParticipant ...>`
- This preserves backward compatibility — QAPanel and tests keep importing from `./question-card`

**Update `question-card.test.tsx`:**

- The existing tests should still pass since QuestionCard facade preserves the same interface
- Add one test: render with `isHost=true` and verify a moderation button is present (e.g., ban question button)
- Add one test: render with `isHost=false` and verify NO moderation buttons appear

State transition animations per user decision:

- Normal to hidden (community downvotes): animate height collapse to tombstone row. Use `framer-motion`'s `AnimatePresence` with `mode="wait"` inside each variant — wrap the three states (banned, hidden-collapsed, normal) so transitions between them animate the height change with a fade.
- Normal to banned (host action): same collapse-to-tombstone pattern.
- The `motion.div` wrapper with `layout` prop on the card handles position changes from re-sorting (this is already present in QAPanel's AnimatePresence).
- Internal state transitions (normal <-> hidden <-> banned) are owned by the variant components. Use `motion.div` with `key` based on the visual state (`banned`, `hidden`, `normal`) and `initial/animate/exit` for height + opacity transitions.
- Timing: 200ms for collapse/expand, ease-out curve. Per user decision, exact timing is Claude's discretion.

**IMPORTANT:** Validate the concern from STATE.md: "Validate AnimatePresence compound key approach under rapid state transitions." Use a single `AnimatePresence mode="wait"` wrapping the three conditional returns, keyed by visual state string. This avoids the compound key issue — only one child renders at a time.
</action>
<verify>
<automated>cd packages/frontend && npx vitest run src/**tests**/question-card.test.tsx && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
</verify>
<done>

- QuestionCardHost and QuestionCardParticipant are independently importable
- QuestionCard facade routes to correct variant based on isHost
- All existing question-card tests pass
- New tests confirm host variant shows moderation controls and participant variant does not
- State transitions (normal/hidden/banned) animate height collapse with fade
- TypeScript compiles with zero new errors
  </done>
  </task>

</tasks>

<verification>
- `grep -c "QuestionCardHost" packages/frontend/src/components/session/question-card-host.tsx` returns 1+ (exported)
- `grep -c "QuestionCardParticipant" packages/frontend/src/components/session/question-card-participant.tsx` returns 1+ (exported)
- `grep "isHost" packages/frontend/src/components/session/question-card.tsx` shows the facade routing
- All frontend tests pass: `cd packages/frontend && npx vitest run`
- No moderation imports (Ban, ShieldX) in question-card-participant.tsx
</verification>

<success_criteria>

- QuestionCardHost has moderation toolbar + ban dialog
- QuestionCardParticipant has no moderation controls
- Shared base provides VoteColumn, BannedTombstone, HiddenCollapsed, formatDisplayName
- QuestionCard facade is backward-compatible (QAPanel unchanged except internal variant routing)
- State transitions animate (collapse to tombstone on ban/hide)
- Existing tests pass, new host/participant differentiation tests pass
  </success_criteria>

<output>
After completion, create `.planning/phases/12-component-decomposition/12-03-SUMMARY.md`
</output>
