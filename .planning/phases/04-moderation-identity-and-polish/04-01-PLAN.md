---
phase: 04-moderation-identity-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/schema.graphql
  - packages/functions/src/resolvers/moderation.ts
  - packages/functions/src/resolvers/rate-limit.ts
  - packages/functions/src/resolvers/index.ts
  - packages/functions/src/resolvers/qa.ts
  - packages/functions/src/resolvers/clipboard.ts
  - packages/core/src/types.ts
  - sst.config.ts
autonomous: true
requirements:
  - MOD-02
  - MOD-03
  - MOD-04
  - MOD-05
  - MOD-06
  - INFRA-05

must_haves:
  truths:
    - "banQuestion mutation sets isBanned=true and isHidden=true on the question item and broadcasts QUESTION_UPDATED"
    - "banParticipant mutation creates/updates BAN#fingerprint item with isBanned=true and broadcasts PARTICIPANT_BANNED"
    - "downvoteQuestion mutation atomically updates downvoteCount and downvoters set, removes from voters if present, and auto-hides when downvotes >= 50% of total votes"
    - "After 3 host-banned posts, the participant's BAN item automatically gets isBanned=true (auto-ban)"
    - "addQuestion is rate-limited to 3/min per fingerprint; pushSnippet is rate-limited to 10/min per hostSecretHash"
    - "addQuestion and addReply check BAN#fingerprint item and reject if participant is banned"
  artifacts:
    - path: "packages/functions/src/resolvers/moderation.ts"
      provides: "banQuestion, banParticipant, downvoteQuestion resolver handlers"
    - path: "packages/functions/src/resolvers/rate-limit.ts"
      provides: "checkRateLimit utility with DynamoDB minute-bucket pattern"
    - path: "infra/schema.graphql"
      provides: "banQuestion, banParticipant, downvoteQuestion mutation definitions"
    - path: "sst.config.ts"
      provides: "Resolver registrations for new mutations"
  key_links:
    - from: "sst.config.ts"
      to: "packages/functions/src/resolvers/index.ts"
      via: "Lambda data source resolver registration for new mutations"
      pattern: "banQuestion|banParticipant|downvoteQuestion"
    - from: "packages/functions/src/resolvers/index.ts"
      to: "packages/functions/src/resolvers/moderation.ts"
      via: "switch case dispatch on fieldName"
      pattern: "case.*ban|downvote"
    - from: "packages/functions/src/resolvers/qa.ts"
      to: "packages/functions/src/resolvers/rate-limit.ts"
      via: "checkRateLimit call before addQuestion write"
      pattern: "checkRateLimit"
---

<objective>
Add moderation and rate-limiting backend: GraphQL mutations for banning questions, banning participants, and downvoting with auto-hide; DynamoDB minute-bucket rate limiting; and ban enforcement in existing resolvers.

Purpose: Enable host moderation controls and community self-moderation at the API layer before wiring UI.
Output: New Lambda resolver handlers, updated GraphQL schema, rate limiting utility, ban checks in existing resolvers.
</objective>

<execution_context>
@/Users/santiago.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/santiago.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-moderation-identity-and-polish/04-RESEARCH.md

@infra/schema.graphql
@packages/functions/src/resolvers/index.ts
@packages/functions/src/resolvers/qa.ts
@packages/functions/src/resolvers/clipboard.ts
@packages/core/src/types.ts
@sst.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: GraphQL schema extensions + moderation resolver handlers + SST wiring</name>
  <files>
    infra/schema.graphql
    packages/functions/src/resolvers/moderation.ts
    packages/functions/src/resolvers/index.ts
    packages/core/src/types.ts
    sst.config.ts
  </files>
  <action>
1. **GraphQL schema** (`infra/schema.graphql`): Add three new mutations:
   - `banQuestion(sessionSlug: String!, hostSecretHash: String!, questionId: String!): SessionUpdate!`
   - `banParticipant(sessionSlug: String!, hostSecretHash: String!, fingerprint: String!): SessionUpdate!`
   - `downvoteQuestion(sessionSlug: String!, questionId: String!, fingerprint: String!, remove: Boolean): SessionUpdate!`
   Add `authorName: String` to addQuestion and addReply mutation arguments (needed for IDENT-02 in Plan 02).
   Update the `@aws_subscribe(mutations: [...])` list on `onSessionUpdate` to include all three new mutation names.

2. **Core types** (`packages/core/src/types.ts`): Add interfaces for the new mutation arguments:
   - `BanQuestionArgs { sessionSlug: string; hostSecretHash: string; questionId: string }`
   - `BanParticipantArgs { sessionSlug: string; hostSecretHash: string; fingerprint: string }`
   - `DownvoteQuestionArgs { sessionSlug: string; questionId: string; fingerprint: string; remove?: boolean }`
   Add a `BanItem` interface: `{ PK: string; SK: string; fingerprint: string; isBanned: boolean; bannedPostCount: number; TTL: number }`
   Ensure `AddQuestionArgs` and `AddReplyArgs` include optional `authorName?: string`.

3. **Moderation resolver** (`packages/functions/src/resolvers/moderation.ts`): Create new file with three exported handler functions:
   - `handleBanQuestion(args: BanQuestionArgs)`: Verify host secret (GetCommand on SESSION#slug METADATA, compare hostSecretHash). Then UpdateCommand on the question item: SET `isBanned = :true, isHidden = :true`. Get the question's `fingerprint` field. Increment `bannedPostCount` on `BAN#fingerprint` item (ADD bannedPostCount :one, SET isBanned = if_not_exists(isBanned, :false)). If returnValues show bannedPostCount >= 3, do a second UpdateCommand to SET isBanned = :true on the BAN item and broadcast PARTICIPANT_BANNED. Always broadcast QUESTION_UPDATED with the updated question fields. Set TTL on BAN item to current epoch + 86400.
   - `handleBanParticipant(args: BanParticipantArgs)`: Verify host secret. PutCommand or UpdateCommand on `SESSION#slug / BAN#fingerprint` with `isBanned = true`, `bannedPostCount = if_not_exists(0)`, `TTL = now + 86400`. Broadcast PARTICIPANT_BANNED event with `{ fingerprint }` payload.
   - `handleDownvoteQuestion(args: DownvoteQuestionArgs)`: If `remove` is true, use UpdateCommand: `ADD downvoteCount :negOne DELETE downvoters :fpSet`. If `remove` is false, use single UpdateCommand: `ADD downvoteCount :one, upvoteCount :negOne DELETE voters :fpSet` (remove upvote if present — DELETE on non-existent set member is a no-op) and `ADD downvoters :fpSet`. Use ConditionExpression to prevent double-downvote (contains/NOT contains on downvoters set). After update, compute `shouldHide = downvoteCount / (upvoteCount + downvoteCount) >= 0.5` from ReturnValues. If shouldHide differs from current isHidden, do a second UpdateCommand to SET isHidden. Broadcast QUESTION_UPDATED with updated fields.

   All handlers follow the existing pattern: return `{ eventType, sessionSlug, payload: JSON.stringify(...) }`.

4. **Lambda dispatcher** (`packages/functions/src/resolvers/index.ts`): Add switch cases for `banQuestion`, `banParticipant`, `downvoteQuestion` that import and call the handlers from `moderation.ts`.

5. **SST config** (`sst.config.ts`): Register resolver mappings for the three new mutations to the existing Lambda data source (same pattern as existing mutations like `pushSnippet`, `addQuestion`).
  </action>
  <verify>
    <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx tsc --noEmit --project packages/functions/tsconfig.json && npx tsc --noEmit --project packages/core/tsconfig.json</automated>
    <manual>Verify schema.graphql has three new mutations and @aws_subscribe includes them</manual>
  </verify>
  <done>Three new GraphQL mutations are defined, resolver handlers implement ban/downvote/auto-hide/auto-ban logic, Lambda dispatcher routes to them, SST registers resolvers — all type-checking passes.</done>
</task>

<task type="auto">
  <name>Task 2: Rate limiting utility + ban enforcement in existing resolvers</name>
  <files>
    packages/functions/src/resolvers/rate-limit.ts
    packages/functions/src/resolvers/qa.ts
    packages/functions/src/resolvers/clipboard.ts
  </files>
  <action>
1. **Rate limit utility** (`packages/functions/src/resolvers/rate-limit.ts`): Create new file exporting `checkRateLimit(key: string, limit: number, windowSeconds?: number): Promise<void>`. Implementation:
   - Compute bucket: `Math.floor(Date.now() / (windowSeconds * 1000)) * windowSeconds` (floor to window boundary in epoch seconds)
   - TTL: `bucket + windowSeconds * 2`
   - UpdateCommand on `PK=RATELIMIT#${key}, SK=BUCKET#${bucket}`: `ADD #count :one SET #ttl = if_not_exists(#ttl, :ttl)` with ConditionExpression `attribute_not_exists(#count) OR #count < :limit`
   - Catch ConditionalCheckFailedException → throw `new Error('RATE_LIMIT_EXCEEDED')`
   - Import docClient and tableName from the existing shared DynamoDB setup in the resolvers package.

2. **Ban check utility** (same file or separate `ban-check.ts` — use same file for simplicity): Export `checkNotBanned(sessionSlug: string, fingerprint: string): Promise<void>`. GetCommand on `SESSION#slug / BAN#fingerprint`. If `result.Item?.isBanned === true`, throw `new Error('PARTICIPANT_BANNED')`.

3. **Update addQuestion** (`packages/functions/src/resolvers/qa.ts`): Before the PutCommand that creates the question, add two calls:
   - `await checkNotBanned(args.sessionSlug, args.fingerprint)` — reject banned participants
   - `await checkRateLimit(args.fingerprint, 3, 60)` — 3 questions per minute
   Also: ensure `authorName` from args is written to the DynamoDB item (it may already be there — verify and add if missing).

4. **Update addReply** (same file): Before the PutCommand, add `checkNotBanned(args.sessionSlug, args.fingerprint)`. No rate limit on replies per requirements. Ensure `authorName` from args is written to DynamoDB.

5. **Update pushSnippet** (`packages/functions/src/resolvers/clipboard.ts`): Before the PutCommand, add `await checkRateLimit(args.hostSecretHash, 10, 60)` — 10 snippets per minute, keyed by hostSecretHash (not fingerprint, since host doesn't need fingerprint for clipboard).
  </action>
  <verify>
    <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx tsc --noEmit --project packages/functions/tsconfig.json</automated>
    <manual>Verify rate-limit.ts has checkRateLimit + checkNotBanned, qa.ts calls both before writes, clipboard.ts calls checkRateLimit before pushSnippet write</manual>
  </verify>
  <done>Rate limiting is enforced (3 questions/min per participant, 10 snippets/min per host), banned participants are rejected from addQuestion and addReply, authorName is written to DynamoDB items.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for packages/functions and packages/core
- `npm run lint --workspace=packages/frontend` passes (no frontend changes in this plan)
- infra/schema.graphql has banQuestion, banParticipant, downvoteQuestion mutations
- @aws_subscribe list includes the three new mutation names
- sst.config.ts has resolver registrations for new mutations
- rate-limit.ts exports checkRateLimit with DynamoDB minute-bucket pattern
- qa.ts calls checkNotBanned + checkRateLimit before addQuestion write
- clipboard.ts calls checkRateLimit before pushSnippet write
</verification>

<success_criteria>
All moderation backend logic is implemented and type-safe: ban question, ban participant (with auto-ban after 3 strikes), downvote with mutual exclusivity and 50%-voter auto-hide, rate limiting on questions and snippets, and ban enforcement on question/reply submission.
</success_criteria>

<output>
After completion, create `.planning/phases/04-moderation-identity-and-polish/04-01-SUMMARY.md`
</output>
