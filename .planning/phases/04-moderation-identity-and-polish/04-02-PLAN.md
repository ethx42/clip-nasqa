---
phase: 04-moderation-identity-and-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/i18n/routing.ts
  - packages/frontend/src/i18n/navigation.ts
  - packages/frontend/src/middleware.ts
  - packages/frontend/src/components/language-switcher.tsx
  - packages/frontend/src/hooks/use-identity.ts
  - packages/frontend/src/components/session/join-modal.tsx
  - packages/frontend/src/components/session/identity-editor.tsx
  - packages/frontend/messages/en.json
  - packages/frontend/messages/es.json
  - packages/frontend/messages/pt.json
autonomous: true
requirements:
  - MOD-01
  - IDENT-01
  - IDENT-02
  - IDENT-03
  - I18N-01
  - I18N-02
  - I18N-03
  - I18N-04
  - I18N-05

must_haves:
  truths:
    - "Language switcher (EN|ES|PT) in session header changes locale and persists via NEXT_LOCALE cookie with 1-year maxAge"
    - "All UI chrome (labels, placeholders, buttons, tooltips) renders in the selected locale"
    - "Join modal appears on first session entry with optional name + email fields and a Skip button"
    - "Display name and email persist in localStorage across sessions; join screen pre-fills with saved values"
    - "Email is never sent to the server — stays in localStorage only"
    - "Profile icon in session header opens identity editor to change name/email mid-session"
    - "Translation files contain keys for moderation, identity, and all new UI elements"
  artifacts:
    - path: "packages/frontend/src/i18n/routing.ts"
      provides: "defineRouting with locales, defaultLocale, localeDetection, localeCookie"
    - path: "packages/frontend/src/i18n/navigation.ts"
      provides: "createNavigation exports: Link, redirect, usePathname, useRouter"
    - path: "packages/frontend/src/components/language-switcher.tsx"
      provides: "EN|ES|PT toggle buttons using useRouter from i18n/navigation"
    - path: "packages/frontend/src/hooks/use-identity.ts"
      provides: "useIdentity hook managing nasqa_identity in localStorage"
    - path: "packages/frontend/src/components/session/join-modal.tsx"
      provides: "JoinModal with optional name + email + Skip"
    - path: "packages/frontend/messages/en.json"
      provides: "Complete English translations for all UI chrome"
    - path: "packages/frontend/messages/es.json"
      provides: "Complete Spanish translations"
    - path: "packages/frontend/messages/pt.json"
      provides: "Complete Portuguese translations"
  key_links:
    - from: "packages/frontend/src/components/language-switcher.tsx"
      to: "packages/frontend/src/i18n/navigation.ts"
      via: "useRouter().replace(pathname, { locale })"
      pattern: "router\\.replace.*locale"
    - from: "packages/frontend/src/middleware.ts"
      to: "packages/frontend/src/i18n/routing.ts"
      via: "createMiddleware(routing)"
      pattern: "import.*routing"
    - from: "packages/frontend/src/hooks/use-identity.ts"
      to: "localStorage"
      via: "nasqa_identity key read/write"
      pattern: "nasqa_identity"
---

<objective>
Set up i18n routing infrastructure (defineRouting + createNavigation + persistent locale cookie), build language switcher component, complete all translation files for en/es/pt, and create the optional identity system (useIdentity hook, join modal, identity editor).

Purpose: Enable locale switching with persistence and optional participant identity before wiring into session pages.
Output: i18n routing files, language switcher, complete translation files, identity hook, join modal, identity editor.
</objective>

<execution_context>
@/Users/santiago.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/santiago.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-moderation-identity-and-polish/04-RESEARCH.md

@packages/frontend/src/middleware.ts
@packages/frontend/src/i18n/request.ts
@packages/frontend/src/app/[locale]/layout.tsx
@packages/frontend/messages/en.json
@packages/frontend/src/hooks/use-fingerprint.ts
@packages/frontend/src/components/session/session-shell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n routing + language switcher + complete translations</name>
  <files>
    packages/frontend/src/i18n/routing.ts
    packages/frontend/src/i18n/navigation.ts
    packages/frontend/src/middleware.ts
    packages/frontend/src/components/language-switcher.tsx
    packages/frontend/messages/en.json
    packages/frontend/messages/es.json
    packages/frontend/messages/pt.json
  </files>
  <action>
1. **i18n routing** (`src/i18n/routing.ts`): Create new file using `defineRouting` from `next-intl/routing`:
   ```
   locales: ['en', 'es', 'pt'], defaultLocale: 'en', localeDetection: true, localeCookie: { maxAge: 365 * 24 * 60 * 60 }
   ```
   This centralizes locale config and enables 1-year persistent cookie.

2. **i18n navigation** (`src/i18n/navigation.ts`): Create new file using `createNavigation` from `next-intl/navigation` with the routing config. Export `{ Link, redirect, usePathname, useRouter }`.

3. **Update middleware** (`src/middleware.ts`): Refactor to import `routing` from `@/i18n/routing` and pass it to `createMiddleware(routing)`. Remove any inline locale config that's now in routing.ts.

4. **Language switcher** (`src/components/language-switcher.tsx`): Create 'use client' component. Import `useLocale` from `next-intl` and `useRouter`, `usePathname` from `@/i18n/navigation`. Render three buttons (EN | ES | PT) with the active locale highlighted (text-foreground vs text-muted-foreground). On click, call `router.replace(pathname, { locale: newLocale })`. Style: `flex items-center gap-1 text-sm` — lightweight, fits in header alongside theme toggle.

5. **Complete translations**: Read existing `en.json` to understand current key structure. Add ALL missing keys for Phase 4 features in all three files:
   - `moderation` namespace: keys for "This question was removed", "Hidden by community", "Show", "Ban participant", "Ban question", "You've been blocked from posting in this session", "Confirm ban", "Cancel", "Are you sure you want to ban this participant?"
   - `identity` namespace: keys for "What's your name?", "Display name (optional)", "Email (optional)", "Skip", "Join session", "Edit profile", "Anonymous"
   - `common` namespace additions: "Rate limit exceeded", "Try again in a moment"
   - `session` namespace additions: any missing session UI keys
   - Ensure en.json has all keys first, then translate to es.json (Spanish) and pt.json (Portuguese). Claude generates translations directly — good enough for v1 per user decision.
   - Keep all existing keys intact. Translation covers UI chrome only (I18N-04).
  </action>
  <verify>
    <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx tsc --noEmit --project packages/frontend/tsconfig.json 2>&1 | head -20</automated>
    <manual>Verify routing.ts has defineRouting with localeCookie, navigation.ts exports useRouter, middleware imports routing, language-switcher.tsx uses useRouter from @/i18n/navigation, all three message files have matching key sets</manual>
  </verify>
  <done>i18n routing is centralized with persistent locale cookie, language switcher renders EN|ES|PT buttons, middleware uses shared routing config, all three translation files are complete with moderation/identity/session keys.</done>
</task>

<task type="auto">
  <name>Task 2: useIdentity hook + join modal + identity editor</name>
  <files>
    packages/frontend/src/hooks/use-identity.ts
    packages/frontend/src/components/session/join-modal.tsx
    packages/frontend/src/components/session/identity-editor.tsx
  </files>
  <action>
1. **useIdentity hook** (`src/hooks/use-identity.ts`): Create 'use client' hook managing `nasqa_identity` in localStorage:
   - State: `{ name?: string; email?: string }`
   - On mount: read from `localStorage.getItem('nasqa_identity')`, JSON.parse, set state
   - `setIdentity(name?: string, email?: string)`: update state + `localStorage.setItem('nasqa_identity', JSON.stringify({ name, email }))`
   - `clearIdentity()`: remove from localStorage, reset state
   - Return: `{ name, email, setIdentity, clearIdentity, hasIdentity: !!name }`
   - Email is NEVER sent to the server (IDENT-03) — it stays in this hook and localStorage only.

2. **JoinModal** (`src/components/session/join-modal.tsx`): 'use client' component using `@base-ui/react` Dialog primitive.
   - Props: `{ sessionSlug: string; open: boolean; onClose: () => void }`
   - Shows when participant first enters a session (controlled by parent via `open` prop).
   - Two optional fields: "Display name" (text input, max 50 chars) and "Email" (email input, max 100 chars) — both pre-filled from useIdentity if values exist.
   - Two buttons: "Join session" (saves identity, closes modal) and "Skip" (closes modal without saving).
   - Track `nasqa_join_shown_${sessionSlug}` in sessionStorage to avoid showing on every page load within the same browser session.
   - Use `useTranslations('identity')` for all labels.
   - Style: centered modal with backdrop, clean form layout, emerald accent on primary button. Use existing Tailwind patterns from the codebase.

3. **IdentityEditor** (`src/components/session/identity-editor.tsx`): 'use client' component for mid-session name/email editing.
   - Small popover/dropdown triggered by a User icon button in the session header area.
   - Same fields as JoinModal: display name + email, pre-filled from useIdentity.
   - "Save" button updates identity. "Clear" button removes identity.
   - Use `useTranslations('identity')` for labels.
   - Use `@base-ui/react` Popover or Dialog — whichever fits the "profile icon in header" pattern better. Popover is more lightweight.
  </action>
  <verify>
    <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx tsc --noEmit --project packages/frontend/tsconfig.json 2>&1 | head -20</automated>
    <manual>Verify useIdentity reads/writes nasqa_identity in localStorage, JoinModal has optional name+email+Skip, IdentityEditor has save/clear functionality</manual>
  </verify>
  <done>useIdentity hook manages localStorage identity, JoinModal shows on first session entry with optional fields, IdentityEditor allows mid-session name/email changes — email never leaves the client.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit --project packages/frontend/tsconfig.json` passes
- routing.ts exports routing config with localeCookie maxAge
- navigation.ts exports Link, redirect, usePathname, useRouter
- middleware.ts imports routing from @/i18n/routing
- language-switcher.tsx uses useRouter from @/i18n/navigation (not from next-intl directly)
- All three message files have identical key structures with localized values
- useIdentity hook reads/writes nasqa_identity localStorage key
- JoinModal uses sessionStorage flag to show once per browser session per slug
- Email is never included in any server-bound data
</verification>

<success_criteria>
Language switcher works with persistent locale cookie, all UI chrome has en/es/pt translations, identity system (hook + join modal + editor) is built and ready to wire into session pages.
</success_criteria>

<output>
After completion, create `.planning/phases/04-moderation-identity-and-polish/04-02-SUMMARY.md`
</output>
