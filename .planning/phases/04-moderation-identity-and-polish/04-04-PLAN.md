---
phase: 04-moderation-identity-and-polish
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified:
  - packages/frontend/src/components/session/session-shell.tsx
  - packages/frontend/src/app/[locale]/session/[slug]/page.tsx
  - packages/frontend/src/app/[locale]/session/[slug]/host/page.tsx
  - packages/frontend/src/app/[locale]/page.tsx
  - packages/frontend/src/actions/qa.ts
  - packages/frontend/src/lib/graphql/mutations.ts
  - packages/frontend/src/components/session/qa-input.tsx
  - packages/frontend/src/components/session/reply-list.tsx
autonomous: false
requirements:
  - IDENT-01
  - IDENT-02
  - I18N-01

must_haves:
  truths:
    - "Language switcher (EN|ES|PT) appears in session header and on the landing page"
    - "JoinModal appears on first visit to a session; display name flows through to question/reply submission"
    - "IdentityEditor icon in session header allows mid-session name editing"
    - "authorName from identity is passed through addQuestionAction and addReplyAction to the server"
    - "Questions and replies show the author's display name instead of 'Anonymous' when set"
    - "All UI chrome renders correctly in en, es, and pt"
  artifacts:
    - path: "packages/frontend/src/components/session/session-shell.tsx"
      provides: "LanguageSwitcher + IdentityEditor in session header"
    - path: "packages/frontend/src/app/[locale]/session/[slug]/page.tsx"
      provides: "JoinModal rendered for participants"
    - path: "packages/frontend/src/actions/qa.ts"
      provides: "authorName parameter threaded through addQuestionAction and addReplyAction"
  key_links:
    - from: "packages/frontend/src/components/session/session-shell.tsx"
      to: "packages/frontend/src/components/language-switcher.tsx"
      via: "import and render in header bar"
      pattern: "LanguageSwitcher"
    - from: "packages/frontend/src/app/[locale]/session/[slug]/page.tsx"
      to: "packages/frontend/src/components/session/join-modal.tsx"
      via: "JoinModal rendered with session slug"
      pattern: "JoinModal"
    - from: "packages/frontend/src/actions/qa.ts"
      to: "packages/frontend/src/lib/graphql/mutations.ts"
      via: "authorName variable added to ADD_QUESTION and ADD_REPLY mutations"
      pattern: "authorName"
    - from: "packages/frontend/src/components/session/qa-input.tsx"
      to: "packages/frontend/src/hooks/use-identity.ts"
      via: "useIdentity provides authorName for addQuestionAction call"
      pattern: "useIdentity.*authorName"
    - from: "packages/frontend/src/components/session/reply-list.tsx"
      to: "packages/frontend/src/hooks/use-identity.ts"
      via: "useIdentity provides authorName for addReplyAction call"
      pattern: "useIdentity.*authorName"
---

<objective>
Wire identity and language switcher into session pages: add LanguageSwitcher and IdentityEditor to session header, render JoinModal on participant session entry, thread authorName through question/reply Server Actions and GraphQL mutations, and do a final human verification of all Phase 4 features.

Purpose: Complete the integration of all Phase 4 subsystems into the live application and verify everything works end-to-end.
Output: Fully wired session pages with moderation, identity, and i18n; human-verified production readiness.
</objective>

<execution_context>
@/Users/santiago.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/santiago.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-moderation-identity-and-polish/04-RESEARCH.md
@.planning/phases/04-moderation-identity-and-polish/04-02-SUMMARY.md
@.planning/phases/04-moderation-identity-and-polish/04-03-SUMMARY.md

@packages/frontend/src/components/session/session-shell.tsx
@packages/frontend/src/app/[locale]/session/[slug]/page.tsx
@packages/frontend/src/app/[locale]/session/[slug]/host/page.tsx
@packages/frontend/src/app/[locale]/page.tsx
@packages/frontend/src/actions/qa.ts
@packages/frontend/src/lib/graphql/mutations.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire identity + language switcher into session pages + thread authorName</name>
  <files>
    packages/frontend/src/components/session/session-shell.tsx
    packages/frontend/src/app/[locale]/session/[slug]/page.tsx
    packages/frontend/src/app/[locale]/session/[slug]/host/page.tsx
    packages/frontend/src/app/[locale]/page.tsx
    packages/frontend/src/actions/qa.ts
    packages/frontend/src/lib/graphql/mutations.ts
    packages/frontend/src/components/session/qa-input.tsx
    packages/frontend/src/components/session/reply-list.tsx
  </files>
  <action>
1. **Session header** (`session-shell.tsx`): Import and render `LanguageSwitcher` in the session header bar, alongside the existing ThemeToggle. Import and render `IdentityEditor` as a User icon button in the header (next to LanguageSwitcher). The header should show: [LiveIndicator] [spacer] [IdentityEditor] [LanguageSwitcher] [ThemeToggle].

2. **Participant session page** (`page.tsx`): Import `JoinModal` and `useIdentity`. On mount, check sessionStorage for `nasqa_join_shown_${slug}` flag. If absent, show JoinModal. On modal close (either "Join" or "Skip"), set the sessionStorage flag. Pass the `name` from useIdentity into the QAPanel/QAInput so it can be threaded through to addQuestionAction.

3. **Host session page** (`host/page.tsx`): Host doesn't need JoinModal (they're the speaker), but should still have LanguageSwitcher and IdentityEditor in the header (these come from SessionShell, so no extra work needed).

4. **Landing page** (`[locale]/page.tsx`): Add `LanguageSwitcher` to the landing page header/navbar so users can switch language before entering a session.

5. **Thread authorName through mutations** (`actions/qa.ts` + `mutations.ts`):
   - Update `ADD_QUESTION` GraphQL mutation string to include `$authorName: String` variable and pass it: `addQuestion(sessionSlug: $sessionSlug, body: $body, fingerprint: $fingerprint, authorName: $authorName)`.
   - Update `ADD_REPLY` GraphQL mutation string similarly with `$authorName: String`.
   - Update `addQuestionAction` to accept `authorName?: string` parameter and pass it in variables.
   - Update `addReplyAction` to accept `authorName?: string` parameter and pass it in variables.
   - Update the QAInput component call sites to pass `authorName` from useIdentity when calling addQuestionAction.
   - Update the reply submission in question-card.tsx or reply-list.tsx to pass `authorName` from useIdentity when calling addReplyAction.

6. **Display authorName in QuestionCard**: Verify that QuestionCard already renders `question.authorName` — if it currently shows "Anonymous", update it to show `question.authorName || t('identity.anonymous')` using the translated string.
   </action>
   <verify>
   <automated>cd /Users/santiago.torres/codebases/personal/nasqa-clip && npx tsc --noEmit --project packages/frontend/tsconfig.json && npm run lint --workspace=packages/frontend 2>&1 | tail -5</automated>
   <manual>Verify LanguageSwitcher appears in session header, JoinModal opens on first session visit, authorName flows through to questions</manual>
   </verify>
   <done>LanguageSwitcher and IdentityEditor appear in session header and landing page, JoinModal shows once per session, authorName threads through addQuestion/addReply Server Actions and GraphQL mutations, QuestionCard displays author name.</done>
   </task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Phase 4</name>
  <files></files>
  <action>
Deploy the application (`npm run deploy`) and present the verification checklist to the user. The user needs two browser windows (or incognito) to test moderation, identity, and i18n across host and participant views.
  </action>
  <verify>User confirms all verification areas pass or identifies specific issues</verify>
  <done>User types "approved" to complete Phase 4, or describes issues for gap closure</done>
  <what-built>
    Complete Phase 4: community moderation (downvotes, host bans, auto-hide, auto-ban), optional participant identity (join modal, identity editor, display name on questions), i18n for en/es/pt (language switcher, complete translations), and server-side rate limiting.
  </what-built>
  <how-to-verify>
    **Deploy first:** Run `npm run deploy` to deploy all changes to AWS.

    **1. i18n (I18N-01 through I18N-05):**
    - Visit landing page — click EN | ES | PT buttons in header
    - Verify all UI text changes language (labels, buttons, placeholders)
    - Refresh page — verify locale persists (cookie-based)
    - Create a session — verify session UI is in selected locale

    **2. Identity (IDENT-01 through IDENT-03):**
    - Open a session URL in a new browser/incognito — verify JoinModal appears
    - Enter a display name and email, click "Join session"
    - Submit a question — verify your display name appears on the question card
    - Click the User icon in header — verify IdentityEditor shows with saved name/email
    - Change name, submit another question — verify new name appears
    - Click "Skip" in JoinModal in another incognito window — verify questions show "Anonymous"
    - Refresh page — verify JoinModal does NOT appear again (sessionStorage flag)

    **3. Moderation - Host ban (MOD-02, MOD-03, MOD-06):**
    - Open host view — hover over a question card
    - Click ban-question icon — verify question shows "This question was removed" for all participants
    - Click ban-participant icon on another question — verify confirmation dialog appears
    - Confirm ban — verify the participant's input disables with "You've been blocked" message
    - Submit 3 questions from a participant, have host ban each — verify auto-ban kicks in after 3rd

    **4. Community downvoting (MOD-04, MOD-05):**
    - Open participant view — click thumbs-down on a question
    - Verify downvote count increases and upvote is removed if previously upvoted
    - Click thumbs-down again — verify it retracts (toggleable)
    - Click upvote on a downvoted question — verify downvote is removed (mutually exclusive)
    - Create a question and have multiple participants downvote until >= 50% threshold
    - Verify question shows "Hidden by community [show]" for all participants
    - Click [show] — verify content is visible
    - As host, click "Restore" on hidden question — verify it becomes visible again

    **5. Rate limiting (INFRA-05):**
    - As participant, submit questions rapidly (>3 in one minute)
    - Verify 4th question is rejected with rate limit message
    - As host, push snippets rapidly (>10 in one minute)
    - Verify 11th snippet is rejected with rate limit message

    **6. Cross-cutting:**
    - Verify all moderation messages appear in the selected locale (not just English)
    - Verify real-time updates still work (subscription not broken by new mutations)
    - Verify existing features (clipboard, Q&A, upvoting, focus) still work correctly

  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 4, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for all packages
- `npm run lint --workspace=packages/frontend` passes
- LanguageSwitcher renders in session header and landing page
- JoinModal appears on first session visit, respects sessionStorage flag
- authorName flows through addQuestion/addReply to DynamoDB
- QuestionCard shows author name when available
- All new UI text uses useTranslations with keys from message files
- `npm run deploy` succeeds
- Human verification covers all 16 requirements
</verification>

<success_criteria>
All Phase 4 features are integrated and human-verified: moderation (ban, downvote, auto-hide, auto-ban), identity (join modal, display name, privacy), i18n (3 locales, switcher, persistence), and rate limiting — the product is production-ready.
</success_criteria>

<output>
After completion, create `.planning/phases/04-moderation-identity-and-polish/04-04-SUMMARY.md`
</output>
