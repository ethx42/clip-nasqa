---
phase: 19-segments-visual-overhaul-and-voting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/components/session/question-card-shared.tsx
  - packages/frontend/src/components/session/question-card-participant.tsx
  - packages/frontend/src/components/session/question-card-host.tsx
  - packages/frontend/src/components/session/reply-list.tsx
  - packages/frontend/src/components/session/snippet-card.tsx
  - packages/frontend/src/components/session/clipboard-panel.tsx
  - packages/frontend/src/components/session/qa-panel.tsx
  - packages/frontend/src/components/session/copy-button.tsx
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - VIS-04
  - VIS-05

must_haves:
  truths:
    - "Q&A questions render as flat segments separated by dividers, not elevated cards with borders and shadows"
    - "Clipboard snippets render as flat segments separated by dividers, not rounded cards"
    - "Corner radii on segments and buttons are visibly reduced (rounded-lg max, no rounded-xl or rounded-2xl)"
    - "Padding on segments is tighter (p-3 instead of p-4/p-5)"
    - "All icon buttons within a question segment are grouped in one consistent action bar at the right of the author row"
    - "All icon buttons within a snippet segment are grouped in one consistent action bar"
    - "Copy-content button in snippet segments is icon-only (no text label)"
    - "Snippet number badge is first in visual hierarchy (leftmost in the header row)"
    - "All existing content and actions remain accessible after the visual change"
  artifacts:
    - path: "packages/frontend/src/components/session/question-card-participant.tsx"
      provides: "Flat segment layout for participant question cards"
      contains: "divide"
    - path: "packages/frontend/src/components/session/question-card-host.tsx"
      provides: "Flat segment layout for host question cards"
      contains: "divide"
    - path: "packages/frontend/src/components/session/snippet-card.tsx"
      provides: "Flat segment layout for clipboard snippets"
      contains: "divide"
    - path: "packages/frontend/src/components/session/qa-panel.tsx"
      provides: "Divider-separated question list"
      contains: "divide-y"
  key_links:
    - from: "packages/frontend/src/components/session/qa-panel.tsx"
      to: "packages/frontend/src/components/session/question-card.tsx"
      via: "QuestionCard rendered in divide-y container"
      pattern: "divide-y"
    - from: "packages/frontend/src/components/session/clipboard-panel.tsx"
      to: "packages/frontend/src/components/session/snippet-card.tsx"
      via: "SnippetCard rendered in divide-y container"
      pattern: "divide-y"
---

<objective>
Convert Q&A and clipboard items from elevated card style to Slack-style flat segments with reduced geometry and consistent icon button grouping.

Purpose: Establish the visual foundation for Phase 19 — all content items shift from bordered/shadowed cards to flat segments separated by dividers, with tighter rounding, reduced padding, and consistently grouped action buttons.

Output: Updated question card components (host + participant), snippet card, reply list, clipboard panel, and QA panel with flat segment styling.
</objective>

<execution_context>
@/Users/santiago.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/santiago.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/frontend/DESIGN_SYSTEM.md
@packages/frontend/src/components/session/question-card-shared.tsx
@packages/frontend/src/components/session/question-card-participant.tsx
@packages/frontend/src/components/session/question-card-host.tsx
@packages/frontend/src/components/session/snippet-card.tsx
@packages/frontend/src/components/session/clipboard-panel.tsx
@packages/frontend/src/components/session/qa-panel.tsx
@packages/frontend/src/components/session/reply-list.tsx
@packages/frontend/src/components/session/copy-button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert Q&A cards to flat segments with consistent action bar</name>
  <files>
    packages/frontend/src/components/session/question-card-shared.tsx
    packages/frontend/src/components/session/question-card-participant.tsx
    packages/frontend/src/components/session/question-card-host.tsx
    packages/frontend/src/components/session/reply-list.tsx
    packages/frontend/src/components/session/qa-panel.tsx
  </files>
  <action>
    **Goal:** Replace card-based Q&A layout with flat Slack-style segments.

    **question-card-shared.tsx (BannedTombstone, HiddenCollapsed):**
    - Replace `rounded-xl border border-border bg-card p-5` with `rounded-lg bg-transparent py-3 px-1` — remove border and card background
    - `HiddenCollapsed`: same treatment — `rounded-xl border border-border bg-card p-4` becomes `rounded-lg py-3 px-1`
    - Reduce all `rounded-xl` to `rounded-lg` in VoteColumn buttons

    **question-card-participant.tsx:**
    - Remove the outer card wrapper: replace `rounded-xl border border-border bg-card p-4` with `py-3 px-1` (no border, no bg-card, no shadow)
    - Focused state: keep `ring-2 ring-indigo-500/50` but remove `border-indigo-500/30 shadow-[...]` — use a subtle `bg-indigo-500/5 rounded-lg` instead
    - Reduce `rounded-xl` to `rounded-lg` on all buttons and textareas
    - Reduce `rounded-2xl` to `rounded-lg` on delete confirmation dialog content div
    - Tighten padding: `p-4` to `py-3 px-1` on segment wrapper
    - Keep the existing action button layout (edit/delete appear on hover for own questions) — they're already grouped

    **question-card-host.tsx:**
    - Same card-to-segment conversion as participant: `rounded-xl border border-border bg-card p-4` becomes `py-3 px-1`
    - Same focused state simplification
    - Reduce all `rounded-xl` to `rounded-lg`
    - Reduce `rounded-2xl` to `rounded-lg` on ban and delete confirmation dialog content divs
    - Host action buttons (edit, delete, focus, ban-question, ban-participant) are already in one `div` — ensure they stay grouped and consistent with participant layout

    **reply-list.tsx (ReplyRow):**
    - Reduce `rounded-xl` to `rounded-lg` on textareas and buttons
    - Reduce `rounded-2xl` to `rounded-lg` on delete confirmation dialog content div
    - Keep the `border-l-2` indent — replies remain visually nested

    **qa-panel.tsx:**
    - Replace `<div className="space-y-3 p-4">` (the question list container) with `<div className="divide-y divide-border px-3">` — use CSS dividers between questions instead of gap spacing
    - Remove `space-y-3` to let `divide-y` handle separation
    - Each `motion.div` wrapping a QuestionCard should keep layout animation but no longer needs margin handling (dividers handle visual separation)

  </action>
  <verify>
    <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx next build 2>&1 | tail -5</automated>
    <manual>Open a session page in browser — questions appear as flat segments separated by thin lines, no card borders or shadows. Host view shows all moderation icons grouped together. Replies remain indented with border-l-2.</manual>
  </verify>
  <done>Q&A questions render as flat divider-separated segments with reduced rounding (rounded-lg), no card borders or background, and action buttons grouped in one bar. Replies keep their indented style but with reduced rounding. All existing functionality preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Convert clipboard snippets to flat segments with icon-only copy and consistent button grouping</name>
  <files>
    packages/frontend/src/components/session/snippet-card.tsx
    packages/frontend/src/components/session/clipboard-panel.tsx
    packages/frontend/src/components/session/copy-button.tsx
  </files>
  <action>
    **Goal:** Replace snippet card style with flat segments; make copy button icon-only; reorder header to put snippet number first.

    **snippet-card.tsx:**
    - Replace outer `rounded-2xl border bg-card ${padding}` with `py-3 px-1` — remove border, bg-card, and the large rounding
    - Highlighted state: keep `ring-2 ring-indigo-500/30` but use `bg-indigo-500/5 rounded-lg` instead of `border-indigo-500`
    - Failed state: keep `border-destructive` semantic but move to a subtle bg treatment: `bg-destructive/5 rounded-lg`
    - Normal hover: remove `hover:border-indigo-500/20` (no border to hover)
    - Reduce `rounded-xl` to `rounded-lg` on code block containers, edit textareas, and inline buttons
    - Reduce `rounded-2xl` on edit inline buttons to `rounded-lg`
    - **Reorder header row:** Put snippet number badge FIRST (leftmost), then language badge, then relative time, then edited badge, then failed indicator — currently number is second after language
    - **CopyButton is already icon-only** (just icon, label via aria-label/title) — verify it has no text label. It doesn't — good, no change needed for VIS-04 on copy-button itself
    - **Action button grouping:** All action buttons (copy, edit, retry, delete, dismiss) are already in one `div` on the right — keep them grouped, ensure consistent `gap-1` spacing

    **clipboard-panel.tsx:**
    - Replace `<div className="flex flex-col gap-3 px-1">` (snippet list) with `<div className="divide-y divide-border px-1">` — CSS dividers between snippets
    - Remove gap-3 so dividers handle visual separation
    - Keep AnimatePresence and motion.div wrapping — they handle enter/exit animations
    - Reduce `rounded-xl` and `rounded-2xl` to `rounded-lg` on the confirmation dialog content div

    **copy-button.tsx:**
    - Reduce `rounded-lg` stays as-is (already correct)
    - No text label exists — already icon-only. VIS-04 is satisfied.

  </action>
  <verify>
    <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx next build 2>&1 | tail -5</automated>
    <manual>Open a session with clipboard snippets — snippets appear as flat segments separated by dividers. Snippet number (#1, #2) appears first in the header. Copy is icon-only. All buttons grouped on the right. Hero snippet and compact snippets both use the new style.</manual>
  </verify>
  <done>Clipboard snippets render as flat divider-separated segments with reduced rounding, snippet number first in visual hierarchy, icon-only copy button, and all action buttons grouped consistently. All existing functionality (expand/collapse, edit, delete, retry, Shiki highlighting) preserved.</done>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. Visual: Open a session page — Q&A shows flat segments with dividers, no card borders
3. Visual: Clipboard shows flat segments with dividers, snippet number first
4. All icon buttons are grouped in consistent action bars per segment type
5. Copy button is icon-only everywhere
6. All existing interactions work: voting, replying, editing, deleting, banning, focusing, expanding snippets
7. Dark mode and light mode both look correct
</verification>

<success_criteria>

- No `rounded-xl` or `rounded-2xl` remains on segment containers or action buttons (only on code block interiors if needed)
- No `border border-border bg-card` on question or snippet segment wrappers
- `divide-y divide-border` used in QA panel and clipboard panel list containers
- Snippet number badge is first element in snippet header row
- Build passes cleanly
  </success_criteria>

<output>
After completion, create `.planning/phases/19-segments-visual-overhaul-and-voting/19-01-SUMMARY.md`
</output>
