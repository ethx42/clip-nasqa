---
phase: 19-segments-visual-overhaul-and-voting
plan: 02
type: execute
wave: 2
depends_on:
  - "19-01"
files_modified:
  - packages/frontend/src/components/session/question-card-shared.tsx
  - packages/frontend/src/components/session/question-card-participant.tsx
  - packages/frontend/src/components/session/question-card-host.tsx
  - packages/frontend/src/components/ui/icon-button.tsx
  - packages/frontend/src/components/session/snippet-card.tsx
  - packages/frontend/src/components/session/reply-list.tsx
  - packages/frontend/src/components/session/clipboard-panel.tsx
  - packages/frontend/src/components/session/copy-button.tsx
  - packages/frontend/src/components/session/host-input.tsx
  - packages/frontend/src/components/session/host-toolbar.tsx
  - packages/frontend/src/components/app-header.tsx
  - packages/frontend/src/components/hamburger-menu.tsx
autonomous: false
requirements:
  - FUNC-04
  - FUNC-05
  - FUNC-06

must_haves:
  truths:
    - "Voting shows up button, numeric count, and down button in horizontal order on every question"
    - "The horizontal voting layout is identical on host and participant views"
    - "Pin (focus) and ban icons in the host action bar are visually aligned and ordered consistently: edit, delete, focus, ban-question, ban-participant"
    - "Every icon-only button in the entire app shows a tooltip on hover explaining its action"
    - "Tooltips appear on hover with a brief delay and disappear on mouse leave"
  artifacts:
    - path: "packages/frontend/src/components/ui/icon-button.tsx"
      provides: "Reusable IconButton wrapper with built-in @base-ui/react Tooltip"
      contains: "Tooltip"
    - path: "packages/frontend/src/components/session/question-card-shared.tsx"
      provides: "Horizontal VoteRow replacing vertical VoteColumn"
      contains: "flex-row"
  key_links:
    - from: "packages/frontend/src/components/ui/icon-button.tsx"
      to: "@base-ui/react/tooltip"
      via: "import { Tooltip } from @base-ui/react/tooltip"
      pattern: "Tooltip.Provider"
    - from: "packages/frontend/src/components/session/question-card-shared.tsx"
      to: "packages/frontend/src/components/ui/icon-button.tsx"
      via: "VoteRow uses IconButton for up/down buttons"
      pattern: "IconButton"
---

<objective>
Implement Reddit-style horizontal voting, fix pin/ban alignment, and add tooltips to every icon-only button in the app.

Purpose: Complete the interactive surface consistency — voting controls use the universally recognized up/count/down horizontal pattern, host moderation icons are properly ordered, and every icon button is self-documenting via tooltip.

Output: IconButton component with @base-ui Tooltip integration, horizontal VoteRow, aligned host action bar, and tooltips on all icon-only buttons across the app.
</objective>

<execution_context>
@/Users/santiago.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/santiago.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-segments-visual-overhaul-and-voting/19-01-SUMMARY.md
@packages/frontend/DESIGN_SYSTEM.md
@packages/frontend/src/components/session/question-card-shared.tsx
@packages/frontend/src/components/session/question-card-participant.tsx
@packages/frontend/src/components/session/question-card-host.tsx
@packages/frontend/src/components/session/snippet-card.tsx
@packages/frontend/src/components/session/reply-list.tsx
@packages/frontend/src/components/session/copy-button.tsx
@packages/frontend/src/components/session/clipboard-panel.tsx
@packages/frontend/src/components/session/host-input.tsx
@packages/frontend/src/components/session/host-toolbar.tsx
@packages/frontend/src/components/app-header.tsx
@packages/frontend/src/components/hamburger-menu.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IconButton with Tooltip, convert voting to horizontal, fix host action bar order</name>
  <files>
    packages/frontend/src/components/ui/icon-button.tsx
    packages/frontend/src/components/session/question-card-shared.tsx
    packages/frontend/src/components/session/question-card-participant.tsx
    packages/frontend/src/components/session/question-card-host.tsx
    packages/frontend/src/components/session/reply-list.tsx
  </files>
  <action>
    **1. Create IconButton component** (`packages/frontend/src/components/ui/icon-button.tsx`):
    - Uses `@base-ui/react/tooltip` (already in dependencies as `@base-ui/react`)
    - Props: `tooltip: string` (required), `children: ReactNode` (icon), `onClick`, `className`, `disabled`, `aria-label`, `aria-pressed`, standard button HTML attributes
    - Renders: `<Tooltip.Provider>` wrapping `<Tooltip.Root>` + `<Tooltip.Trigger>` (the button) + `<Tooltip.Portal>` + `<Tooltip.Positioner>` + `<Tooltip.Popup>` (styled tooltip)
    - Tooltip delay: 400ms (brief hover delay via `delay` prop on Tooltip.Root)
    - Tooltip style: `rounded-md bg-foreground text-background px-2 py-1 text-xs font-medium shadow-md z-50` — dark on light, light on dark (uses semantic tokens)
    - Button style: base classes `inline-flex items-center justify-center rounded-lg p-1.5 text-muted-foreground transition-colors hover:bg-accent hover:text-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-indigo-500/50 min-h-[44px] min-w-[44px]` — mergeable via `cn()` with passed className
    - Export as named export: `export function IconButton(...)`
    - IMPORTANT: Check @base-ui/react Tooltip API. The Tooltip component from @base-ui/react uses: `<Tooltip.Provider>`, `<Tooltip.Root>`, `<Tooltip.Trigger>`, `<Tooltip.Positioner>`, `<Tooltip.Popup>`. Use `delay={400}` on `<Tooltip.Root>`.

    **2. Convert VoteColumn to VoteRow** (`question-card-shared.tsx`):
    - Rename `VoteColumn` to `VoteRow` (export both names for backwards compat during transition, but only use VoteRow in updated cards)
    - Change layout from `flex flex-col items-center gap-1 pt-0.5` to `flex flex-row items-center gap-1`
    - Layout: [upvote button] [upvote count] [middot separator] [downvote count] [downvote button] — Reddit-style horizontal
    - Use IconButton for upvote and downvote buttons (import from `@/components/ui/icon-button`)
    - Upvote icon: keep `ChevronUp` (h-4 w-4)
    - Downvote icon: change `ThumbsDown` to `ChevronDown` for visual symmetry with ChevronUp
    - Count display: `text-sm font-semibold tabular-nums` — tighter than before
    - Remove the `mt-1` gap between up and down sections (horizontal now, no vertical gap needed)

    **3. Update question cards to use VoteRow:**
    - In `question-card-participant.tsx` and `question-card-host.tsx`:
      - Replace `<VoteColumn ... />` with `<VoteRow ... />`
      - Move VoteRow from the left column to inside the content area, below the question text, before the action row. The old layout was `flex gap-4` with VoteColumn on left + content on right. New layout: single column — author row, question text, vote row (horizontal), action row, reaction bar.
      - Remove the outer `flex gap-4` that created the two-column layout
      - This makes the segment layout single-column like Slack messages

    **4. Fix host action bar ordering** (`question-card-host.tsx`):
    - Ensure consistent order: edit, delete, focus, ban-question, ban-participant
    - Replace raw `<button>` elements with `<IconButton>` component for all action buttons
    - Each gets a `tooltip` prop with the translated label (same text currently in `title` attribute)
    - Remove redundant `title` and `aria-label` on buttons since IconButton handles both via tooltip + aria

    **5. Update participant action buttons** (`question-card-participant.tsx`):
    - Replace raw `<button>` for edit/delete with `<IconButton>` with tooltip

    **6. Update reply action buttons** (`reply-list.tsx`):
    - Replace raw `<button>` for edit/delete with `<IconButton>` with tooltip

  </action>
  <verify>
    <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx next build 2>&1 | tail -5</automated>
    <manual>Open host session — voting is horizontal (up, count, down), host actions ordered edit/delete/focus/ban/ban-participant. Hover any icon button to see tooltip.</manual>
  </verify>
  <done>VoteRow renders horizontally on all questions. Host action bar ordered consistently. All icon buttons in Q&A use IconButton with tooltip. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add tooltips to all remaining icon-only buttons across the app</name>
  <files>
    packages/frontend/src/components/session/snippet-card.tsx
    packages/frontend/src/components/session/copy-button.tsx
    packages/frontend/src/components/session/clipboard-panel.tsx
    packages/frontend/src/components/session/host-input.tsx
    packages/frontend/src/components/session/host-toolbar.tsx
    packages/frontend/src/components/app-header.tsx
    packages/frontend/src/components/hamburger-menu.tsx
  </files>
  <action>
    **Goal:** Every icon-only button in the entire app must show a tooltip on hover. Audit all components and replace raw `<button>` with `<IconButton>` where the button has only an icon (no visible text).

    **snippet-card.tsx:**
    - Replace icon-only buttons (copy, edit, retry, delete, dismiss) with `<IconButton>` from `@/components/ui/icon-button`
    - Each gets `tooltip={t("...")}` matching the existing title/aria-label text
    - The snippet number button (`#{snippetNumber}`) has visible text — NOT icon-only, so it keeps `title` attribute but does NOT get IconButton

    **copy-button.tsx:**
    - Convert to use IconButton internally OR make CopyButton accept a `tooltip` prop and wrap with Tooltip
    - Simplest: refactor CopyButton to render `<IconButton tooltip={label}>` around the icon
    - Keep the copied/not-copied state toggle (Check vs Copy icon)
    - Since CopyButton manages its own click handler and state, wrap the icon swap inside IconButton: `<IconButton tooltip={copied ? copiedLabel : label} onClick={handleCopy}>{copied ? <Check .../> : <Copy .../>}</IconButton>`
    - Add `copiedLabel` — use a new i18n key or just the existing label (label is "Copy" which is fine even after copy since tooltip disappears)

    **clipboard-panel.tsx:**
    - The "Clear all" button has text ("Clear all") — NOT icon-only, no tooltip needed
    - No other icon-only buttons in this file

    **host-input.tsx:**
    - Check for any icon-only buttons (language selector dropdown trigger, push button). The push button likely has text — audit and add tooltip only if icon-only.

    **host-toolbar.tsx:**
    - Check for icon-only buttons (share, QR). Convert any icon-only buttons to IconButton with tooltip.

    **app-header.tsx:**
    - Logo link: has visible "CLIP" text — not icon-only, skip
    - Profile icon (IdentityEditor trigger): may be icon-only — if so, wrap with tooltip
    - Any other icon-only buttons — audit and convert

    **hamburger-menu.tsx:**
    - Hamburger trigger button: icon-only (Menu icon) — convert to IconButton with tooltip "Menu" or localized equivalent
    - Theme toggle inside menu: has label text — not icon-only, skip

    **IMPORTANT:** Do not add tooltips to buttons that already have visible text labels. Only icon-only buttons get the IconButton treatment. Buttons with both icon and text keep their existing implementation.

  </action>
  <verify>
    <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx next build 2>&1 | tail -5</automated>
    <manual>Systematically hover every icon-only button in the app: copy buttons on snippets, edit/delete on Q&A, edit/delete on snippets, retry/dismiss on failed snippets, hamburger menu, share button. Each must show a tooltip on hover.</manual>
  </verify>
  <done>Every icon-only button in the app shows a descriptive tooltip on hover. No icon-only button is left without a tooltip. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of segments, voting, and tooltips</name>
  <files>packages/frontend/src/components/session/question-card-participant.tsx</files>
  <action>
    Human visual verification of the complete Phase 19 visual overhaul. No code changes — verify what was built in Tasks 1-2 plus Plan 01.

    What was built:
    1. Q&A questions and clipboard snippets render as flat Slack-style segments with dividers
    2. Reduced rounding and padding throughout
    3. Reddit-style horizontal voting (up, count, down)
    4. Consistent icon button grouping and ordering
    5. Tooltips on every icon-only button

    How to verify:
    1. Open participant view — questions are flat segments with dividers, voting is horizontal, tooltips appear on icon buttons
    2. Open host view — same plus host action bar ordered: edit, delete, focus, ban-question, ban-participant
    3. Clipboard snippets are flat segments, snippet number first, copy icon-only with tooltip
    4. Dark mode correct, mobile 44px touch targets maintained
    5. Hover hamburger menu icon — tooltip appears

  </action>
  <verify>Visual inspection by human</verify>
  <done>Human confirms all visual changes are correct and approves</done>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. Every icon-only button in the app has a tooltip (FUNC-06)
3. Voting is horizontal Reddit-style on all questions (FUNC-04)
4. Host action bar is consistently ordered (FUNC-05)
5. All interactions work: voting, replying, editing, deleting, banning, focusing
6. Dark mode and light mode both correct
7. WCAG AA contrast on tooltip text
</verification>

<success_criteria>

- `IconButton` component exists with @base-ui Tooltip integration
- `VoteRow` renders horizontally in all question cards
- Host action bar order is: edit, delete, focus, ban-question, ban-participant
- Every icon-only button uses IconButton (grep for raw `<button>` with only icon children should find none that lack tooltip)
- Human visual verification passes
  </success_criteria>

<output>
After completion, create `.planning/phases/19-segments-visual-overhaul-and-voting/19-02-SUMMARY.md`
</output>
