---
phase: 13-ux-polish-and-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/components/session/question-card-shared.tsx
  - packages/frontend/src/__tests__/question-card.test.tsx
autonomous: true
requirements:
  - UXINT-01
  - A11Y-01

must_haves:
  truths:
    - "Upvote button appears filled (solid Indigo) when the user has voted; outline-only when unvoted"
    - "Downvote button appears filled (muted gray) when active; outline-only when unvoted"
    - "Switching between upvote and downvote unfills the previous button instantly"
    - "aria-pressed=true is present on upvote button when voted; aria-pressed=false when not"
    - "aria-pressed=true is present on downvote button when downvoted; aria-pressed=false when not"
    - "Vote buttons have hover brightness increase and press scale-0.95 for responsive feel"
    - "Focus ring uses focus-visible only"
  artifacts:
    - path: "packages/frontend/src/components/session/question-card-shared.tsx"
      provides: "VoteColumn with filled/outline toggle, aria-pressed, hover/press states"
      contains: "aria-pressed"
    - path: "packages/frontend/src/__tests__/question-card.test.tsx"
      provides: "Tests verifying aria-pressed on vote buttons"
      contains: "aria-pressed"
  key_links:
    - from: "packages/frontend/src/components/session/question-card-shared.tsx"
      to: "packages/frontend/src/components/session/question-card-participant.tsx"
      via: "VoteColumn import"
      pattern: "VoteColumn"
---

<objective>
Update VoteColumn to show filled/outline toggle states on vote buttons and add aria-pressed for screen reader accessibility.

Purpose: Vote buttons must communicate their state honestly -- filled = active, outline = inactive. Screen readers must announce toggle state via aria-pressed.
Output: Updated VoteColumn component and tests confirming aria-pressed behavior.
</objective>

<execution_context>
@/Users/santiago.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/santiago.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ux-polish-and-accessibility/13-CONTEXT.md
@packages/frontend/src/components/session/question-card-shared.tsx
@packages/frontend/src/__tests__/question-card.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: VoteColumn filled states, aria-pressed, and micro-interactions</name>
  <files>packages/frontend/src/components/session/question-card-shared.tsx</files>
  <action>
Update the VoteColumn component in question-card-shared.tsx:

1. **Upvote button filled state (UXINT-01 decision):**
   - When `isVoted=true`: solid Indigo background (`bg-indigo-600 text-white` in light, `bg-indigo-500 text-white` in dark). Icon should appear "filled" by using the solid background to create the filled effect.
   - When `isVoted=false`: outline-only (`text-muted-foreground`), no background fill.
   - Replace the current amber-500 color with Indigo per user decision.

2. **Downvote button filled state (UXINT-01 decision):**
   - When `isDownvoted=true`: muted gray fill (`bg-muted text-foreground`). Downvote is secondary, not destructive.
   - When `isDownvoted=false`: outline-only (`text-muted-foreground`).
   - Replace the current `text-destructive` with muted gray per user decision.

3. **aria-pressed (A11Y-01):**
   - Add `aria-pressed={isVoted}` to the upvote button.
   - Add `aria-pressed={isDownvoted}` to the downvote button.
   - Keep existing `aria-label` values alongside aria-pressed.

4. **Micro-interactions (decision: hover + press states):**
   - Add `hover:brightness-110` (or equivalent Tailwind class like `hover:bg-indigo-500` when voted) for subtle hover brightening.
   - Add `active:scale-95` with `transition-transform` for press feedback.
   - Add `focus-visible:ring-2 focus-visible:ring-indigo-500/50 focus-visible:outline-none` for keyboard focus ring (hidden on mouse/touch per decision).

5. **Vote count colors:**
   - Upvote count: `text-indigo-600 dark:text-indigo-400` when voted, `text-muted-foreground` when not.
   - Downvote count: `text-foreground` when downvoted, `text-muted-foreground` when not.

Do NOT add any animation delay on mutual exclusion switching -- state changes must be instant per user decision.
</action>
<verify>
<automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx vitest run packages/frontend/src/**tests**/question-card.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
<manual>Existing tests still pass with the updated VoteColumn (vote handler calls unchanged).</manual>
</verify>
<done>VoteColumn renders filled Indigo on upvote-active, filled muted-gray on downvote-active, outline on inactive; aria-pressed toggles on both buttons; hover brightness and active:scale-95 present; focus-visible ring on keyboard only.</done>
</task>

<task type="auto">
  <name>Task 2: Add aria-pressed tests for vote buttons</name>
  <files>packages/frontend/src/__tests__/question-card.test.tsx</files>
  <action>
Add test cases to the existing QuestionCard test suite in question-card.test.tsx:

1. **"upvote button has aria-pressed=false when not voted":**
   - Render with `votedQuestionIds={new Set()}`.
   - Assert: upvote button has `aria-pressed="false"`.

2. **"upvote button has aria-pressed=true when voted":**
   - Render with `votedQuestionIds={new Set(["q1"])}`.
   - Assert: upvote button has `aria-pressed="true"`.

3. **"downvote button has aria-pressed=false when not downvoted":**
   - Render with `downvotedQuestionIds={new Set()}`.
   - Assert: downvote button has `aria-pressed="false"`.

4. **"downvote button has aria-pressed=true when downvoted":**
   - Render with `downvotedQuestionIds={new Set(["q1"])}`.
   - Assert: downvote button has `aria-pressed="true"`.

Update the `renderCard` helper to accept `votedQuestionIds` and `downvotedQuestionIds` as optional overrides (default to empty Set as before).

Use `screen.getByRole("button", { name: /upvote question/i })` and check `.getAttribute("aria-pressed")`.
</action>
<verify>
<automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx vitest run packages/frontend/src/**tests**/question-card.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
</verify>
<done>Four new tests pass confirming aria-pressed toggles correctly on both vote buttons based on voted/downvoted state.</done>
</task>

</tasks>

<verification>
All existing and new tests in question-card.test.tsx pass. VoteColumn renders correct visual states and aria-pressed values.
</verification>

<success_criteria>

- VoteColumn uses filled Indigo for active upvote, filled muted-gray for active downvote, outline for inactive
- aria-pressed={true|false} present on both vote buttons
- Hover, active:scale-95, and focus-visible:ring micro-interactions present
- All question-card tests pass (existing + 4 new aria-pressed tests)
  </success_criteria>

<output>
After completion, create `.planning/phases/13-ux-polish-and-accessibility/13-01-SUMMARY.md`
</output>
