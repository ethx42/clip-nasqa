---
phase: 13-ux-polish-and-accessibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/components/session/qa-input.tsx
  - packages/frontend/src/components/session/question-card-participant.tsx
  - packages/frontend/src/__tests__/qa-input.test.tsx
autonomous: true
requirements:
  - UXINT-02
  - UXINT-03

must_haves:
  truths:
    - "Participant sees their Notionist pixel avatar and name above the question input before typing"
    - "Anonymous participants see a default pixel avatar with text 'Anonymous' above the input"
    - "Identity row is always visible -- not toggled by focus or hidden behind interaction"
    - "Identity row is display-only, not tappable"
    - "Participant's own questions show a 3px left-border accent in brand Indigo"
    - "The accent is visible only to the author -- other participants do not see it"
    - "No background tint or 'You' badge on own questions -- border only"
  artifacts:
    - path: "packages/frontend/src/components/session/qa-input.tsx"
      provides: "Identity row with PixelAvatar and name above input"
      contains: "PixelAvatar"
    - path: "packages/frontend/src/components/session/question-card-participant.tsx"
      provides: "3px left-border accent on own questions"
      contains: "border-l"
    - path: "packages/frontend/src/__tests__/qa-input.test.tsx"
      provides: "Tests for identity row rendering"
      contains: "Anonymous"
  key_links:
    - from: "packages/frontend/src/components/session/qa-input.tsx"
      to: "packages/frontend/src/hooks/use-identity.ts"
      via: "useIdentity hook import"
      pattern: "useIdentity"
    - from: "packages/frontend/src/components/session/qa-input.tsx"
      to: "packages/frontend/src/components/session/pixel-avatar.tsx"
      via: "PixelAvatar component import"
      pattern: "PixelAvatar"
    - from: "packages/frontend/src/components/session/qa-input.tsx"
      to: "packages/frontend/src/hooks/use-fingerprint.ts"
      via: "useFingerprint hook for avatar seed"
      pattern: "useFingerprint"
---

<objective>
Add identity chip above question input and 3px left-border accent on own questions in the feed.

Purpose: Participants should see who they are before posting (identity transparency) and easily locate their own contributions in the feed (spatial recognition).
Output: Updated QAInput with identity row, updated QuestionCardParticipant with left-border accent, and tests.
</objective>

<execution_context>
@/Users/santiago.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/santiago.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ux-polish-and-accessibility/13-CONTEXT.md
@packages/frontend/src/components/session/qa-input.tsx
@packages/frontend/src/components/session/question-card-participant.tsx
@packages/frontend/src/hooks/use-identity.ts
@packages/frontend/src/hooks/use-fingerprint.ts
@packages/frontend/src/components/session/pixel-avatar.tsx
@packages/frontend/src/__tests__/qa-input.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Identity row above question input</name>
  <files>packages/frontend/src/components/session/qa-input.tsx, packages/frontend/src/__tests__/qa-input.test.tsx</files>
  <action>
Update QAInput component to display an identity row above the text input:

1. **New props:**
   - Add `fingerprint: string` prop to QAInputProps. The fingerprint is needed as the seed for PixelAvatar. QAInput is a "use client" component, so it can receive this prop from QAPanel which already has it.
   - Add `authorName?: string` prop to QAInputProps. QAPanel already receives this data flow from the page orchestrator via useIdentity.

   NOTE: Do NOT call useIdentity or useFingerprint inside QAInput -- these hooks are already called in the page orchestrators (session-live-page.tsx / session-live-host-page.tsx). Instead, accept `fingerprint` and `authorName` as props. QAPanel will need to thread these through. Add `fingerprint` and `authorName` to QAPanelProps in qa-panel.tsx and pass them to QAInput.

   CORRECTION: Actually, since QAInput is a client component and useIdentity/useFingerprint are client hooks, it IS valid to call them directly inside QAInput to keep the interface clean. Choose whichever approach is simpler. Given that QAPanel already has `fingerprint` as a prop, threading it is simpler for fingerprint. For identity name, useIdentity is already called in the page orchestrators. The simplest approach: add `fingerprint` and `authorName` props to QAInput, update QAPanel to pass them through. QAPanel already has `fingerprint`. For `authorName`, add it as a new prop on QAPanelProps.

2. **Identity row rendering:**
   - Above the input area (before the `<div className="flex items-end gap-3">` block), render:
     ```
     <div className="flex items-center gap-2 mb-2 px-1">
       <PixelAvatar seed={fingerprint} size={20} className="rounded-full shrink-0" />
       <span className="text-xs text-muted-foreground truncate">
         {authorName || t("anonymous")}  // use identity.anonymous translation key
       </span>
     </div>
     ```
   - Avatar size: 20px (Claude's discretion -- small enough to not dominate, large enough to be recognizable).
   - The row is always visible, not conditional on focus.
   - The row is NOT interactive -- no onClick, no cursor-pointer, no hover states. It's purely informational.
   - Use `tIdentity("anonymous")` translation for anonymous state. Add `const tIdentity = useTranslations("identity");` in QAInput.

3. **Banned state:** Do NOT show identity row when `isBanned=true` (the banned UI already replaces everything).

4. **Update QAPanel** (qa-panel.tsx): Thread `fingerprint` (already available) and a new `authorName` prop to QAInput. Add `authorName?: string` to QAPanelProps.

5. **Update page orchestrators:** In both session-live-page.tsx and session-live-host-page.tsx, pass `authorName` to QAPanel. The page orchestrators already call `const { name: authorName } = useIdentity()` and pass it to useSessionMutations. Just add `authorName={authorName}` to the QAPanel JSX.

6. **Update qa-input.test.tsx:**
   - Update `renderInput` helper to pass `fingerprint="test-fp"` and optionally `authorName`.
   - Add test: "renders identity row with Anonymous when no authorName" -- assert text "Anonymous" is visible.
   - Add test: "renders identity row with author name when provided" -- pass `authorName="Santiago"`, assert "Santiago" visible.
   - Add test: "does not render identity row when banned" -- pass `isBanned=true`, assert "Anonymous" NOT visible.
     </action>
     <verify>
     <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx vitest run packages/frontend/src/**tests**/qa-input.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
     </verify>
     <done>Identity row with PixelAvatar and name/Anonymous renders above the question input. Three new tests pass. QAPanel and page orchestrators thread fingerprint and authorName to QAInput.</done>
     </task>

<task type="auto">
  <name>Task 2: Own-question left-border accent</name>
  <files>packages/frontend/src/components/session/question-card-participant.tsx</files>
  <action>
Add a 3px left-border accent in brand Indigo to the participant's own questions:

1. In QuestionCardParticipant, the `isOwn` variable is already computed: `const isOwn = question.fingerprint === fingerprint;`

2. On the card container div (the one with `className="group relative rounded-xl border border-border bg-card p-4 transition-all"`), add conditional left border when `isOwn`:

   ```
   cn(
     "group relative rounded-xl border border-border bg-card p-4 transition-all",
     question.isFocused && "ring-2 ring-indigo-500/50 border-indigo-500/30 ...",
     isOwn && "border-l-[3px] border-l-indigo-500",
   )
   ```

3. The accent is ONLY a left border -- no background tint, no badge. Per user decision: "Border only -- no background tint, no 'You' badge."

4. The `isOwn` check uses `question.fingerprint === fingerprint` which is the viewer's fingerprint. Other participants viewing the same question will NOT see the accent because their fingerprint won't match. This satisfies "Visible only to the author."

5. Ensure the 3px left border doesn't cause layout shift -- since the card already has `border border-border` (1px all sides), the left side going to 3px is a 2px increase. Use `border-l-[3px]` which overrides the left portion of the existing `border`. No padding adjustment needed -- the visual shift is minimal and acceptable.
   </action>
   <verify>
   <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx vitest run packages/frontend/src/**tests**/question-card.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
   <manual>All existing question-card tests still pass. The border-l class is applied conditionally.</manual>
   </verify>
   <done>Participant's own questions display a 3px left-border in Indigo. Other participants' questions have no accent. No background tint or badge added.</done>
   </task>

</tasks>

<verification>
All tests pass: qa-input.test.tsx (existing + 3 new identity row tests), question-card.test.tsx (existing tests unbroken). TypeScript compilation succeeds.
</verification>

<success_criteria>

- Identity row with PixelAvatar + name/Anonymous renders above question input, always visible, not interactive
- QAPanel and page orchestrators correctly thread fingerprint and authorName to QAInput
- Own questions show 3px Indigo left-border accent, visible only to the author
- No background tint, no badge, no decorative transitions added
- All tests pass
  </success_criteria>

<output>
After completion, create `.planning/phases/13-ux-polish-and-accessibility/13-02-SUMMARY.md`
</output>
