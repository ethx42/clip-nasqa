---
phase: 09-reactions-data-model-and-backend
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - packages/functions/src/__tests__/reactions.test.ts
  - packages/core/src/__tests__/schemas.test.ts
autonomous: true
requirements: [RXN-01, RXN-02, RXN-03, RXN-05, RXN-06, RXN-07, RXN-09, RXN-14]

must_haves:
  truths:
    - "Unit tests verify toggle-on increments count and adds fingerprint to reactors set"
    - "Unit tests verify toggle-off decrements count and removes fingerprint from reactors set"
    - "Unit tests verify banned participant is rejected"
    - "Unit tests verify rate limit uses reaction# namespace"
    - "Unit tests verify response contains counts but not reactor fingerprints"
    - "Unit tests verify emojiKeySchema rejects invalid emoji keys"
    - "All tests pass with pnpm test"
  artifacts:
    - path: "packages/functions/src/__tests__/reactions.test.ts"
      provides: "handleReact resolver unit tests"
      min_lines: 80
    - path: "packages/core/src/__tests__/schemas.test.ts"
      provides: "EMOJI_PALETTE and emojiKeySchema tests"
      min_lines: 20
  key_links:
    - from: "packages/functions/src/__tests__/reactions.test.ts"
      to: "packages/functions/src/resolvers/reactions.ts"
      via: "import handleReact"
      pattern: "import.*handleReact.*reactions"
---

<objective>
Add unit tests for the handleReact resolver and EMOJI_PALETTE/emojiKeySchema validation to ensure correctness of toggle behavior, dedup, rate limiting, ban enforcement, and response shape.

Purpose: Verify all reaction backend behavior before deployment — toggle-on/off, dedup, rate limiting, ban check, and response privacy (no reactor fingerprints).
Output: Comprehensive test files that pass with `pnpm test`.
</objective>

<execution_context>
@/Users/santiago.torres/.claude/get-shit-done/workflows/execute-plan.md
@/Users/santiago.torres/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-reactions-data-model-and-backend/09-CONTEXT.md
@.planning/phases/09-reactions-data-model-and-backend/09-RESEARCH.md
@.planning/phases/09-reactions-data-model-and-backend/09-01-SUMMARY.md
@packages/functions/src/__tests__/qa.test.ts
@packages/functions/src/__tests__/setup.ts
@packages/functions/src/resolvers/reactions.ts
@packages/core/src/schemas.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add EMOJI_PALETTE and emojiKeySchema unit tests</name>
  <files>packages/core/src/__tests__/schemas.test.ts</files>
  <action>
Add tests to the existing `packages/core/src/__tests__/schemas.test.ts` file (or create it if it doesn't exist). Follow the existing test patterns in the project.

Test cases for EMOJI_PALETTE:

1. Has exactly 6 entries
2. Each entry has `key`, `emoji`, and `label` string properties
3. Keys are in expected order: thumbsup, heart, party, laugh, thinking, eyes
4. All emoji strings are non-empty

Test cases for emojiKeySchema:

1. Accepts all 6 valid keys: "thumbsup", "heart", "party", "laugh", "thinking", "eyes"
2. Rejects invalid keys: "fire", "sad", "", "THUMBSUP" (case-sensitive)
3. Rejects non-string values: null, undefined, 42

Test cases for EMOJI_KEYS:

1. Is an array of length 6
2. Contains all palette keys in order
   </action>
   <verify>
   <automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx vitest run packages/core/src/**tests**/schemas.test.ts --reporter=verbose 2>&1 | tail -30</automated>
   </verify>
   <done>All EMOJI_PALETTE, emojiKeySchema, and EMOJI_KEYS tests pass</done>
   </task>

<task type="auto">
  <name>Task 2: Add handleReact resolver unit tests</name>
  <files>packages/functions/src/__tests__/reactions.test.ts</files>
  <action>
Create `packages/functions/src/__tests__/reactions.test.ts` following the exact mock pattern from `qa.test.ts`:
- Set `process.env.TABLE_NAME = "test-table"` before imports
- Mock `DynamoDBDocumentClient` with `mockClient`
- Reset mock in `beforeEach`
- Default mock: GetCommand resolves `{ Item: undefined }` (no ban), UpdateCommand resolves with realistic ALL_NEW attributes

Test cases (group in describe blocks):

**describe("handleReact — toggle on")**:

1. Returns REACTION_UPDATED event type and correct sessionSlug
2. Payload contains `counts` object with all 6 emoji keys
3. Payload contains `reactedByMe` object with the toggled emoji set to true
4. Payload contains `targetId`, `targetType`, and `emoji`
5. Works for targetType QUESTION (SK is QUESTION#id)
6. Works for targetType REPLY (SK is REPLY#id)

**describe("handleReact — toggle off")**:

1. When toggle-on throws ConditionalCheckFailedException, sends toggle-off UpdateCommand
2. Toggle-off result has `reactedByMe[emoji]` set to false
3. When toggle-off also throws CCF (race condition no-op), returns without error

**describe("handleReact — validation")**:

1. Rejects invalid emoji key with INVALID_EMOJI error
2. Rejects banned participant (mock GetCommand to return `{ Item: { isBanned: true } }`) with PARTICIPANT_BANNED error
3. Rate limit call uses `reaction#fingerprint` key — verify UpdateCommand was called with PK containing `RATELIMIT#reaction#`

**describe("handleReact — privacy")**:

1. Response payload (JSON.parse of payload) does NOT contain any key matching `reactors` or `rxn_*_reactors`
2. `counts` values are non-negative (Math.max guard)

Mock patterns for toggle-on success: `UpdateCommand` resolves with `Attributes` containing `rxn_thumbsup_count: 1` and `rxn_thumbsup_reactors: new Set(["fp"])`.

Mock pattern for toggle-on failure (triggers toggle-off): First `UpdateCommand` rejects with `new ConditionalCheckFailedException({ message: "cond", $metadata: {} })`, second resolves with attributes.

Use `VALID_FINGERPRINT = "123e4567-e89b-12d3-a456-426614174000"` consistent with qa.test.ts.
</action>
<verify>
<automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && npx vitest run packages/functions/src/**tests**/reactions.test.ts --reporter=verbose 2>&1 | tail -40</automated>
</verify>
<done>All handleReact test cases pass: toggle-on, toggle-off, validation, ban check, rate limit namespace, and privacy (no reactor fingerprints in response)</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite to verify no regressions</name>
  <files></files>
  <action>
Run the full test suite across all packages to confirm the new tests pass and no existing tests are broken by the changes to schemas.ts and types.ts.

Run: `pnpm test` from repo root.

If any existing tests fail due to the new EMOJI_PALETTE or REACTION_UPDATED additions, investigate — these are additive changes and should not break existing tests. If a snapshot or assertion relies on enum length, update it.
</action>
<verify>
<automated>cd /Users/santiago.torres/codebases/personal/nasqa-live && pnpm test 2>&1 | tail -20</automated>
</verify>
<done>All tests pass across all packages — no regressions from core type additions or new resolver</done>
</task>

</tasks>

<verification>
- `pnpm test` exits 0 with all tests passing
- reactions.test.ts covers toggle-on, toggle-off, validation, ban, rate limit, privacy
- schemas.test.ts covers EMOJI_PALETTE structure and emojiKeySchema validation
</verification>

<success_criteria>

- All new tests pass (RXN-01, RXN-02, RXN-03, RXN-05, RXN-06, RXN-07, RXN-09, RXN-14 verified via tests)
- No regressions in existing test suite
- Toggle behavior is verified for both toggle-on and toggle-off paths
- Privacy constraint verified: no reactor fingerprints in response payloads
  </success_criteria>

<output>
After completion, create `.planning/phases/09-reactions-data-model-and-backend/09-03-SUMMARY.md`
</output>
