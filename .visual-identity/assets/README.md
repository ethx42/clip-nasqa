# Asset Metadata

This directory holds metadata files for generated assets — not the binary files themselves.

Binary assets (PNG, SVG, WebP) live in `packages/frontend/public/images/`.

Each `.md` file here documents:

- The master prompt used
- Tool and parameters
- Where the asset is placed
- Dark/light mode variants
- Iteration history
